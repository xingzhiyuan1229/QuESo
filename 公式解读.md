# 论文公式全文解读

**论文原题：** *Robust numerical integration of embedded solids described in boundary representation*

**作者：** Manuel Mefmer, Stefan Kollmannsberger, Roland Wuchner, Kai-Uwe Bletzinger

---

> **阅读说明**
>
> 本文档按照论文章节顺序，对论文中出现的全部编号公式及重要数学关系进行逐条解读与严格推导，并明确指出每一推导步骤所依赖的数学事实。符号体系完全沿用论文原文，仅在必要时补充说明。
>
> 公式格式约定：
> - 独立公式采用 `\[ ... \]` 展示；
> - 行内符号与表达式采用 `\( ... \)` 展示。

---

## 第 2 节　嵌入式方法（Embedded methods）

### 基础记号与域分解

论文首先建立嵌入式有限元方法的问题框架。令 \(\Omega \subset \mathbb{R}^3\) 为待求解偏微分方程的物理域，其边界为

\[
\Gamma = \partial\Omega,
\]

并将边界分为 Dirichlet 边界 \(\Gamma_D\) 与 Neumann 边界 \(\Gamma_N\)，满足

\[
\Gamma_D \cup \Gamma_N = \Gamma, \qquad \Gamma_D \cap \Gamma_N = \emptyset.
\]

背景网格（background partition）记为 \(\mathbf{P}_{\mathrm{bg}}\)，由轴对齐六面体单元（Axis-Aligned Bounding Box, AABB）\(B \in \mathbf{P}_{\mathrm{bg}}\) 构成。将 \(\Omega\) 嵌入 \(\mathbf{P}_{\mathrm{bg}}\) 后，各单元分为三类：

- **切割单元** \(B_{\mathrm{cut}}\)：几何边界 \(\Gamma\) 穿过该单元；
- **内部单元** \(B_{\mathrm{in}}\)：完全在 \(\Omega\) 内部；
- **外部单元** \(B_{\mathrm{out}}\)：完全在 \(\Omega\) 外部。

活跃背景网格定义为

\[
\mathbf{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}},
\]

即去掉所有外部单元后保留的单元集合。

---

### 公式 (1)：体积分的单元分解

#### 公式原文

\[
\int_{\Omega}(\cdot)\,\mathrm{d}\Omega
= \sum_{B \in \mathcal{P}} \int_{B \cap \Omega}(\cdot)\,\mathrm{d}\Omega.
\tag{1}
\]

#### 公式含义

公式 (1) 将物理域 \(\Omega\) 上的全局体积分，拆解为活跃背景网格 \(\mathcal{P}\) 中每个单元对应子域 \(B \cap \Omega\) 上的局部积分之和。这是有限元方法"单元分解"思想的数学基础。

#### 严格推导

**数学事实 1（测度的可加性 / Lebesgue 积分的线性性）：**
设可测集 \(\{S_k\}_{k=1}^{N}\) 两两不相交（a.e. 意义），且 \(\bigcup_{k=1}^N S_k = S\)，则对任意可积函数 \(h\)，

\[
\int_S h\,\mathrm{d}V = \sum_{k=1}^{N} \int_{S_k} h\,\mathrm{d}V.
\]

**推导步骤：**

**步骤 1** — 构造不相交覆盖。

背景网格 \(\mathbf{P}_{\mathrm{bg}}\) 由轴对齐六面体单元 \(B\) 构成，相邻单元仅共享二维面或一维棱，对三维 Lebesgue 测度贡献为零（即 \(\mu_3(\partial B) = 0\)）。因此各 \(B \cap \Omega\) 之间 a.e. 不相交：

\[
(B_i \cap \Omega) \cap (B_j \cap \Omega) = (B_i \cap B_j) \cap \Omega,
\quad \mu_3\bigl((B_i \cap B_j)\bigr) = 0 \quad (i \neq j).
\]

**步骤 2** — 验证覆盖完整性。

由于 \(\mathbf{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}}\) 且外部单元与 \(\Omega\) 无交（\(B_{\mathrm{out}} \cap \Omega = \emptyset\)），内部单元和切割单元的交集恰好覆盖 \(\Omega\)：

\[
\Omega = \bigcup_{B \in \mathcal{P}} (B \cap \Omega) \quad \text{(a.e.)}.
\]

**步骤 3** — 应用数学事实 1。

将数学事实 1 代入，取 \(S_k = B_k \cap \Omega\)，\(S = \Omega\)，立得

\[
\int_{\Omega}(\cdot)\,\mathrm{d}\Omega
= \sum_{B \in \mathcal{P}} \int_{B \cap \Omega}(\cdot)\,\mathrm{d}\Omega. \quad \checkmark
\]

#### 实践意义

对于内部单元，\(B_{\mathrm{in}} \cap \Omega = B_{\mathrm{in}}\) 恒成立，可直接使用标准 Gauss 积分公式。对于切割单元，\(B_{\mathrm{cut}} \cap \Omega\) 的几何形状复杂，需要后续矩拟合方法（第 3 节）处理。

---

### 公式 (2)：边界积分的单元分解

#### 公式原文

\[
\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma
= \sum_{B \in \mathcal{P}} \sum_{F_\beta \in \Gamma_\beta}
\int_{F_\beta}(\cdot)\,\mathrm{d}\Gamma,
\qquad \beta \in \{D, N\},
\tag{2}
\]

其中 \(F_D\) 和 \(F_N\) 分别是定义在 \(\Gamma_D\) 和 \(\Gamma_N\) 上的 Dirichlet 和 Neumann 边界条件面片。

#### 公式含义

公式 (2) 将边界 \(\Gamma_\beta\) 上的全局曲面积分（弱形式中的边界项，如 Neumann 荷载、Dirichlet 弱约束项）分解为逐单元框架下各三角面片 \(F_\beta\) 上的局部积分之和，与式 (1) 形式完全对称。

#### 严格推导

**数学事实 2（曲面积分的可加性）：**
设有向曲面 \(\Sigma\) 由有限个两两不相交（a.e.）的子面片 \(\{\sigma_k\}\) 拼成，且 \(\bigcup_k \sigma_k = \Sigma\)（a.e.），则

\[
\int_\Sigma h\,\mathrm{d}\Gamma = \sum_k \int_{\sigma_k} h\,\mathrm{d}\Gamma.
\]

**推导步骤：**

**步骤 1** — STL 面片的离散结构。

STL 格式将 \(\Gamma_\beta\) 表示为有限个有向三角面片 \(\{F_\beta^{(k)}\}\)，相邻面片仅共享边（一维，曲面测度为零）。

**步骤 2** — 按背景单元聚类。

将 \(\{F_\beta^{(k)}\}\) 按其所属背景单元 \(B\) 聚类：剪裁（clipping）算法保证每个面片唯一归属于某个 \(B\)，形成两层求和结构 \(\sum_{B \in \mathcal{P}} \sum_{F_\beta \in \Gamma_\beta \cap B}\)。

**步骤 3** — 应用数学事实 2。

对曲面测度应用可加性，立得式 (2)。 \(\checkmark\)

#### 实践意义

\(\Gamma_{c,c} = B_{\mathrm{cut}} \cap \Gamma\) 是 STL 被切割单元裁剪后的局部面片集合，直接用于施加本质边界条件（Dirichlet 条件）和自然边界条件（Neumann 荷载）。这也是算法第 5 节中裁剪（clipping）步骤的直接产物。

---

## 第 3 节　高效积分规则的构造

### 3.1 节　矩拟合（Moment fitting）

#### 背景

切割单元 \(\Omega_c = B_{\mathrm{cut}} \cap \Omega\) 的形状任意复杂，无法直接使用标准 Gauss 积分。矩拟合方法的核心思想是：在已知一组参考积分值（"矩"）的前提下，求解最优积分权重，将数值积分问题转化为线性代数问题。

---

### 公式 (3)：矩拟合方程组

#### 公式原文

\[
\underbrace{
\begin{bmatrix}
f_1(\pmb{x}_1) & \cdots & f_1(\pmb{x}_{n_q})\\
\vdots & \ddots & \vdots \\
f_m(\pmb{x}_1) & \cdots & f_m(\pmb{x}_{n_q})
\end{bmatrix}
}_{A \,\in\, \mathbb{R}^{m \times n_q}}
\underbrace{
\begin{bmatrix}
\omega_1\\ \vdots \\ \omega_{n_q}
\end{bmatrix}
}_{\boldsymbol{\omega}}
=
\underbrace{
\begin{bmatrix}
\displaystyle\int_{\Omega_c} f_1(\pmb{x})\,\mathrm{d}\Omega \\
\vdots \\
\displaystyle\int_{\Omega_c} f_m(\pmb{x})\,\mathrm{d}\Omega
\end{bmatrix}
}_{\mathbf{b}},
\tag{3}
\]

其中 \(\pmb{x}_i\) 为第 \(i\) 个积分点的坐标，\(\omega_i\) 为其对应权重，\(f_j\) 为矩拟合基函数，\(m\) 为基函数数目，\(n_q\) 为积分点总数。

#### 公式含义

式 (3) 是矩拟合方法的核心线性方程组 \(A\boldsymbol{\omega} = \mathbf{b}\)：

- 矩阵 \(A\)（\(m \times n_q\)）：基函数在各积分点处的值，完全由点位 \(\{\pmb{x}_i\}\) 和基函数 \(\{f_j\}\) 决定；
- 向量 \(\boldsymbol{\omega}\)（\(n_q \times 1\)）：待求的积分权重；
- 向量 \(\mathbf{b}\)（\(m \times 1\)）：各基函数在 \(\Omega_c\) 上的精确积分值，称为"矩"（moments）。

#### 严格推导

**数学事实 3（数值积分的精确性条件）：**
一个 \(n_q\) 点积分公式

\[
\int_{\Omega_c} h(\pmb{x})\,\mathrm{d}\Omega \approx \sum_{i=1}^{n_q} \omega_i\, h(\pmb{x}_i)
\]

对函数空间 \(\mathrm{span}\{f_1, \dots, f_m\}\) 精确，当且仅当对每个 \(j = 1, \dots, m\)：

\[
\sum_{i=1}^{n_q} \omega_i\, f_j(\pmb{x}_i) = \int_{\Omega_c} f_j(\pmb{x})\,\mathrm{d}\Omega.
\]

**推导步骤：**

**步骤 1 — 原始非线性问题。**

最理想地，对目标被积函数（有限元基函数的多项式组合）\(u(\pmb{x})\) 要求：

\[
\int_{\Omega_c} u(\pmb{x})\,\mathrm{d}\Omega = \sum_{i=1}^{n_q} \omega_i\, u(\pmb{x}_i).
\]

此时 \(\omega_i\) 和 \(\pmb{x}_i\) 均未知，关于 \(\pmb{x}_i\) 高度非线性，难以直接求解。

**步骤 2 — 线性化策略（预先固定积分点位）。**

**数学事实 4（点位预设线性化）：** 将积分点坐标 \(\{\pmb{x}_i\}_{i=1}^{n_q}\) 预先指定（例如从标准 Gauss-Legendre 点候选集中选取），则矩阵 \(A\) 中的元素 \(A_{ji} = f_j(\pmb{x}_i)\) 完全已知，式 (3) 对 \(\boldsymbol{\omega}\) 成为线性系统。

**步骤 3 — 写出分量方程。**

将精确性条件（数学事实 3）对 \(j = 1, \dots, m\) 逐条列出：

\[
\sum_{i=1}^{n_q} f_j(\pmb{x}_i)\,\omega_i = \int_{\Omega_c} f_j(\pmb{x})\,\mathrm{d}\Omega, \quad j = 1, \dots, m.
\]

**步骤 4 — 矩阵化。**

令 \(A_{ji} = f_j(\pmb{x}_i)\)，则 \(m\) 个方程恰好写成矩阵形式

\[
A\boldsymbol{\omega} = \mathbf{b},
\]

即式 (3)。\(\checkmark\)

**步骤 5 — 求解策略。**

通常 \(n_q < m\)（超定系统），论文采用 **非负最小二乘（NNLS）** 求解器，自动保证 \(\omega_i \geq 0\)，同时配合**点剔除算法（point elimination）**，将积分点数压缩到

\[
n_q \leq (p+1)^d,
\]

其中 \(p\) 为基函数多项式阶次，\(d\) 为积分空间维数（三维情形 \(d=3\)）。

#### 关键说明

- 式 (3) 右边向量 \(\mathbf{b}\)（即"矩"）必须精确计算，这是整个矩拟合流程精度的瓶颈所在，也是本论文核心贡献所在（见第 3.2、3.3 节）。
- 若 \(n_q = m\) 且 \(A\) 满秩，则式 (3) 有唯一解；若 \(n_q > m\)，则系统欠定，通过点剔除降维。

---

### 公式 (4)：矩拟合基函数空间（Legendre 张量积）

#### 公式原文

\[
F = \bigl\{ f_j(\pmb{x}) = L_r(x)\,L_s(y)\,L_t(z);\; r, s, t = 0, 1, 2, \dots, p \bigr\},
\tag{4}
\]

其中 \(L_r\) 为第 \(r\) 阶 Legendre 多项式。

#### 公式含义

式 (4) 定义了矩拟合方程 (3) 所使用的基函数空间 \(F\)：以三个方向的 Legendre 多项式的张量积为基底，在参数立方体 \([-1,1]^3\) 上构建正交多项式空间。

#### 严格推导与分析

**数学事实 5（Legendre 多项式的正交性）：**

\[
\int_{-1}^{1} L_r(\xi)\,L_s(\xi)\,\mathrm{d}\xi = \frac{2}{2r+1}\,\delta_{rs},
\]

其中 \(\delta_{rs}\) 为 Kronecker delta。

**数学事实 6（张量积空间的正交性）：**

在参数立方体 \(\hat{\Omega} = [-1,1]^3\) 上，张量积基函数满足：

\[
\int_{\hat{\Omega}} L_r(x)L_s(y)L_t(z) \cdot L_{r'}(x)L_{s'}(y)L_{t'}(z)\,\mathrm{d}x\,\mathrm{d}y\,\mathrm{d}z
= \frac{8}{(2r+1)(2s+1)(2t+1)}\,\delta_{rr'}\delta_{ss'}\delta_{tt'}.
\]

**推导步骤：**

**步骤 1 — 空间维数。**

\(r, s, t\) 各从 \(0\) 到 \(p\)，故总基函数个数为

\[
m = (p+1)^3.
\]

**步骤 2 — 条件数优化。**

由数学事实 6，在完整单元 \(B_{\mathrm{in}}\) 上，矩拟合矩阵 \(A^T A\) 接近对角矩阵，条件数远小于 Vandermonde 型单项式基 \(\{x^r y^s z^t\}\)，NNLS 求解数值稳定。

**步骤 3 — 积分点数上界。**

由 [36] 的结果，采用点剔除后，可构造具有 \(n_q \leq (p+1)^3\) 个积分点的积分规则，权重均为正，且点位限制在物理域内，这对几何和材料非线性分析尤为有利。

**步骤 4 — Legendre 多项式的前几阶。**

\[
L_0(\xi) = 1, \quad
L_1(\xi) = \xi, \quad
L_2(\xi) = \tfrac{1}{2}(3\xi^2 - 1), \quad
L_3(\xi) = \tfrac{1}{2}(5\xi^3 - 3\xi), \quad \dots
\]

由三项递推公式生成：

\[
(r+1)L_{r+1}(\xi) = (2r+1)\xi L_r(\xi) - r L_{r-1}(\xi).
\]

---

### 3.2 节　矩的计算（Computation of the moments）

#### 背景

计算式 (3) 右边向量 \(\mathbf{b}\)（即各基函数在 \(\Omega_c\) 上的积分）是矩拟合方法的核心挑战。对于由 B-Rep（STL）描述的切割域，直接进行三维数值积分需要对体域进行空间细分，代价高昂。本节通过散度定理将三维体积分降维为二维曲面积分。

---

### 公式 (5)：散度定理降维——体积分转化为面积分

#### 公式原文

\[
\int_{\Omega_c} f_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Omega_c} \nabla \cdot \pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Gamma_c} \pmb{g}_j(\pmb{x}) \cdot \pmb{n}(\pmb{x})\,\mathrm{d}\Gamma,
\tag{5}
\]

其中 \(\pmb{n}\) 为边界 \(\Gamma_c = \partial\Omega_c\) 上的外法单位向量。

#### 公式含义

式 (5) 通过两个等号完成两步变换：

1. **第一步**：将标量函数 \(f_j\) 的体积分改写为某向量场 \(\pmb{g}_j\) 的散度的体积分——要求构造 \(\pmb{g}_j\) 使得 \(\nabla \cdot \pmb{g}_j = f_j\)；
2. **第二步**：对 \(\pmb{g}_j\) 应用高斯散度定理，将三维体积分转化为二维曲面积分。

#### 严格推导（逐等号）

**数学事实 7（高斯散度定理）：**
设 \(\Omega_c \subset \mathbb{R}^3\) 为有界 Lipschitz 域，\(\pmb{g} \in C^1(\overline{\Omega_c}, \mathbb{R}^3)\)，则

\[
\int_{\Omega_c} \nabla \cdot \pmb{g}\,\mathrm{d}\Omega
= \oint_{\partial\Omega_c} \pmb{g} \cdot \pmb{n}\,\mathrm{d}\Gamma.
\]

**数学事实 8（反散度的存在性）：**
对任意光滑标量函数 \(f : \mathbb{R}^3 \to \mathbb{R}\)，总存在向量场 \(\pmb{g} : \mathbb{R}^3 \to \mathbb{R}^3\) 使得 \(\nabla \cdot \pmb{g} = f\)（此 \(\pmb{g}\) 称为 \(f\) 的**反散度**，类比一元积分中的不定积分 / 原函数）。

---

**第一个等号（\(\int_{\Omega_c} f_j\,\mathrm{d}\Omega = \int_{\Omega_c} \nabla\cdot\pmb{g}_j\,\mathrm{d}\Omega\)）的推导：**

**步骤 1.1** — 选取反散度。

由数学事实 8，对 Legendre 多项式张量积 \(f_j = L_r(x)L_s(y)L_t(z)\)（充分光滑），存在向量场 \(\pmb{g}_j\) 满足 \(\nabla \cdot \pmb{g}_j = f_j\)。论文给出了具体的构造公式，即式 (6)（见下节）。

**步骤 1.2** — 等式成立。

一旦 \(\nabla \cdot \pmb{g}_j = f_j\) 在 \(\Omega_c\) 上成立，则被积函数相等，积分自然相等：

\[
\int_{\Omega_c} f_j\,\mathrm{d}\Omega = \int_{\Omega_c} \nabla\cdot\pmb{g}_j\,\mathrm{d}\Omega. \quad\checkmark
\]

---

**第二个等号（\(\int_{\Omega_c} \nabla\cdot\pmb{g}_j\,\mathrm{d}\Omega = \int_{\Gamma_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma\)）的推导：**

**步骤 2.1** — 验证适用条件。

\(\Omega_c = B_{\mathrm{cut}} \cap \Omega\) 由算法 5 构造为封闭三角面片网格围成的 Lipschitz 域；\(\pmb{g}_j\) 是多项式向量场（由式 (6) 确定），属于 \(C^\infty\)。两个条件均满足数学事实 7 的前提。

**步骤 2.2** — 直接应用高斯散度定理（数学事实 7）：

\[
\int_{\Omega_c} \nabla\cdot\pmb{g}_j\,\mathrm{d}\Omega
= \oint_{\partial\Omega_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma
= \int_{\Gamma_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma. \quad\checkmark
\]

（其中 \(\Gamma_c = \partial\Omega_c = \partial(B_{\mathrm{cut}} \cap \Omega)\) 为切割域的封闭边界。）

#### 降维效果与核心作用

式 (5) 将**三维体积分**转化为**二维曲面积分**，从而：

- 无需对体域 \(\Omega_c\) 进行空间细分（octree、tessellation）；
- 只需在封闭曲面 \(\Gamma_c\) 上数值积分；
- \(\Gamma_c\) 上的被积函数 \(\pmb{g}_j \cdot \pmb{n}\) 为多项式与常数法向量的点积，可被高阶 Gauss 积分精确计算。

**关键前提：\(\Gamma_c\) 必须是封闭的（closed）**，否则散度定理不成立（法向量外法方向不一致，存在"泄漏"）。这也是第 5 节算法的核心任务——为每个切割单元生成封闭的 \(\Gamma_c\)。

---

### 公式 (6)：反散度向量场 \(\pmb{g}_j\) 的构造

#### 公式原文

\[
\pmb{g}_j(\pmb{x})
= \frac{1}{3}
\begin{bmatrix}
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}x \\[8pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}y \\[8pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}z
\end{bmatrix}.
\tag{6}
\]

其中三个分量分别是 \(f_j\) 对 \(x\)、\(y\)、\(z\) 的不定积分（另外两个变量视为参数）。

#### 公式含义

式 (6) 给出了满足 \(\nabla \cdot \pmb{g}_j = f_j\) 的显式向量场构造方法，是式 (5) 第一个等号成立的直接保证。

#### 严格推导（验证 \(\nabla \cdot \pmb{g}_j = f_j\)）

**数学事实 9（散度算子的分量展开）：**

\[
\nabla \cdot \pmb{g}
= \frac{\partial g_x}{\partial x} + \frac{\partial g_y}{\partial y} + \frac{\partial g_z}{\partial z}.
\]

**数学事实 10（微积分基本定理 / 偏导数与积分互逆）：**

若 \(G(x, y, z) = \int f(x, y, z)\,\mathrm{d}x\)（对 \(x\) 不定积分，\(y, z\) 为参数），则

\[
\frac{\partial G}{\partial x} = f(x, y, z).
\]

**推导步骤：**

**步骤 1** — 计算各分量的偏导数。

由式 (6)，记

\[
g_x = \frac{1}{3}\int f_j\,\mathrm{d}x,\quad
g_y = \frac{1}{3}\int f_j\,\mathrm{d}y,\quad
g_z = \frac{1}{3}\int f_j\,\mathrm{d}z.
\]

由数学事实 10：

\[
\frac{\partial g_x}{\partial x}
= \frac{1}{3}\cdot\frac{\partial}{\partial x}\int f_j\,\mathrm{d}x
= \frac{1}{3}\,f_j.
\]

类似地：

\[
\frac{\partial g_y}{\partial y} = \frac{1}{3}\,f_j, \qquad
\frac{\partial g_z}{\partial z} = \frac{1}{3}\,f_j.
\]

**步骤 2** — 计算散度。

由数学事实 9：

\[
\nabla \cdot \pmb{g}_j
= \frac{\partial g_x}{\partial x} + \frac{\partial g_y}{\partial y} + \frac{\partial g_z}{\partial z}
= \frac{1}{3}\,f_j + \frac{1}{3}\,f_j + \frac{1}{3}\,f_j
= f_j. \quad\checkmark
\]

**因子 \(\frac{1}{3}\) 的设计精髓：** 三个方向均等分配（各贡献 \(\frac{1}{3}\)），使 \(\pmb{g}_j\) 具有对称性，数值上避免某一方向主导误差；同时 \(\pmb{g}_j\) 仍是多项式向量场，可被高阶 Gauss 积分精确求值。

#### 对 Legendre 基函数的具体展开

以 \(f_j = L_r(x)L_s(y)L_t(z)\) 为例：

\[
\int f_j\,\mathrm{d}x
= L_s(y)L_t(z) \int L_r(x)\,\mathrm{d}x
= L_s(y)L_t(z) \cdot \frac{L_{r+1}(x) - L_{r-1}(x)}{2r+1}, \quad r \geq 1.
\]

（利用 Legendre 多项式积分的标准递推公式：\(\int L_r\,\mathrm{d}x = \frac{L_{r+1} - L_{r-1}}{2r+1}\)，对 \(r=0\) 有 \(\int L_0\,\mathrm{d}x = x = L_1\)。）

类似地对 \(y, z\) 方向积分，三个分量均为有限阶 Legendre 多项式，可在三角面片上用标准 Gauss 积分精确计算。

---

### 3.3 节　对 B-Rep 的应用

### 公式 (7)：对三角面片逐面求和

#### 公式原文

\[
\int_{\Omega_c} f_j(\pmb{x})\,\mathrm{d}\Omega
= \sum_{f \in \Gamma_c} \int_f \pmb{g}_j(\pmb{x}) \cdot \pmb{n}(\pmb{x})\,\mathrm{d}\Gamma,
\tag{7}
\]

其中 \(f\) 表示一个三角面片。

#### 公式含义

式 (7) 将式 (5) 右边的曲面积分在三角面片网格（STL 格式）上离散化，将封闭曲面 \(\Gamma_c\) 上的连续曲面积分转化为有限个三角面片上的数值积分之和，实现完全基于 STL 的数值计算。

#### 严格推导（从式 5 到式 7 的两步细化）

**数学事实 11（曲面积分的面片分解）：**

封闭曲面 \(\Gamma_c\) 由有限个三角面片 \(\{f^{(k)}\}_{k=1}^{N_f}\) 拼成，相邻面片仅共享边（一维，曲面测度为零）：

\[
\oint_{\Gamma_c}(\cdot)\,\mathrm{d}\Gamma = \sum_{k=1}^{N_f} \int_{f^{(k)}}(\cdot)\,\mathrm{d}\Gamma.
\]

**数学事实 12（平面三角形的常数法向量）：**

对于平面三角面片 \(f\)，其法向量 \(\pmb{n}_f\) 在面片内为常数，由三角形三个顶点 \(\pmb{v}_1, \pmb{v}_2, \pmb{v}_3\) 的叉积确定：

\[
\pmb{n}_f = \frac{(\pmb{v}_2 - \pmb{v}_1) \times (\pmb{v}_3 - \pmb{v}_1)}{\|(\pmb{v}_2 - \pmb{v}_1) \times (\pmb{v}_3 - \pmb{v}_1)\|}.
\]

**推导步骤：**

**步骤 1** — 将式 (5) 右边的曲面积分按面片分解（数学事实 11）：

\[
\int_{\Gamma_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma
= \sum_{f \in \Gamma_c} \int_f \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma.
\]

**步骤 2** — 利用平面三角形的常数法向量（数学事实 12）：

\[
\int_f \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma
= \pmb{n}_f \cdot \int_f \pmb{g}_j\,\mathrm{d}A,
\]

其中 \(\int_f \pmb{g}_j\,\mathrm{d}A\) 为三角形区域上的面积积分，可用**高阶 Gauss-Legendre 积分**精确计算（论文强调可达机器精度，因为 \(\pmb{g}_j\) 是多项式，在足够高阶的 Gauss 积分下精确无误）。

**步骤 3** — 合并得式 (7)：

\[
\int_{\Omega_c} f_j\,\mathrm{d}\Omega
= \sum_{f \in \Gamma_c} \int_f \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma. \quad\checkmark
\]

#### 关键要求与精度保障

- **封闭性要求：** \(\Gamma_c = \partial(B_{\mathrm{cut}} \cap \Omega)\) 必须封闭，法向量一致向外，否则散度定理不成立（式 (5) 第二个等号失效）；
- **单一质量准则：** 论文给出一个基于已知散度定理解析解的质量检验指标：对常数函数 \(f_j = 1\)，式 (7) 应精确给出 \(\Omega_c\) 的体积 \(|\Omega_c|\)；若误差超过阈值，则剔除该单元的面片重构；
- **高阶积分的精度：** 因 \(\pmb{g}_j\) 是多项式，面片上用足够阶次（例如 \(p+1\) 阶）的 Gauss 积分即可达到机器精度。

---

## 第 5 节　B-Rep 的嵌入算法（Embedding of B-Reps）

本节所涉及的算法步骤（Algorithm 1）直接对应上述各公式的实际应用，无新编号公式，但以下关键中间量值得专门说明。

### 5.1 算法核心中间量

**裁剪面片网格（Clipped mesh）：**

\[
\Gamma_{c,c} = B_{\mathrm{cut}} \cap \Gamma
\]

即将 STL 几何边界 \(\Gamma\) 对切割单元的 AABB 做 Sutherland–Hodgman 多边形裁剪后得到的局部 STL 面片集合。这是式 (2) 的直接输入，用于施加边界条件。

**封闭切割面网格（Closed cut surface）：**

\[
\Gamma_c = \partial(B_{\mathrm{cut}} \cap \Omega)
\]

在 \(\Gamma_{c,c}\) 基础上，对 AABB 六个面上被 \(\Omega\) 覆盖的开放多边形区域进行三角化后，将所有面片合并，形成封闭曲面。这是式 (7) 的直接输入，用于计算矩。

**质量检验准则（Quality criterion）：**

对切割域体积进行散度定理验证：

\[
\left|\; \sum_{f \in \Gamma_c} \int_f \pmb{g}_0 \cdot \pmb{n}\,\mathrm{d}\Gamma - V_{\mathrm{ref}} \;\right| \leq \varepsilon,
\]

其中 \(\pmb{g}_0\) 对应 \(f_0 = 1\)（常数基函数），\(V_{\mathrm{ref}}\) 为参考体积，\(\varepsilon\) 为误差阈值。该单一质量准则替代了传统网格质量指标（长宽比、角度等），使算法可容忍高质量问题面片（高长宽比、重复顶点、零面积三角形等）。

### 5.2 单元分类与泛洪填充

**单元分类算法（Flood fill / seed fill）** 将背景网格所有单元分类为 \(B_{\mathrm{in}}\)、\(B_{\mathrm{out}}\)、\(B_{\mathrm{cut}}\)。其数学基础是**点在多面体内的检测（point-in-solid test）**，通过射线投票法（ray casting）或符号距离函数实现。

对于内部单元：

\[
B_{\mathrm{in}} = B_{\mathrm{in}} \cap \Omega \quad \Rightarrow \quad
\int_{B_{\mathrm{in}} \cap \Omega}(\cdot)\,\mathrm{d}\Omega = \int_{B_{\mathrm{in}}}(\cdot)\,\mathrm{d}\Omega,
\]

直接用标准 Gauss 积分即可，无需式 (3)–(7) 的矩拟合流程。

---

## 公式体系全貌总结

以下将论文的 7 个编号公式按其数学关系串联，形成完整的推导链：

**域分解框架（公式 1–2）**

\[
\int_\Omega (\cdot)\,\mathrm{d}\Omega = \sum_{B \in \mathcal{P}} \int_{B \cap \Omega}(\cdot)\,\mathrm{d}\Omega
\quad \xrightarrow{\text{切割单元难以直接积分}} \quad \text{需要特殊数值积分方法}
\]

\[
\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma = \sum_{B \in \mathcal{P}} \sum_{F_\beta \in \Gamma_\beta} \int_{F_\beta}(\cdot)\,\mathrm{d}\Gamma
\quad \text{（边界积分同样分解）}
\]

**矩拟合线性化（公式 3–4）**

\[
A\boldsymbol{\omega} = \mathbf{b}
\quad \xrightarrow{\text{点位预设}} \quad
F = \{ L_r(x)L_s(y)L_t(z) \}
\quad \text{（正交基保证数值稳定）}
\]

**散度定理降维（公式 5–6）**

\[
\int_{\Omega_c} f_j\,\mathrm{d}\Omega
= \int_{\Omega_c} \nabla\cdot\pmb{g}_j\,\mathrm{d}\Omega
= \int_{\Gamma_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma
\quad \text{（三维体积分 → 二维面积分）}
\]

\[
\pmb{g}_j = \frac{1}{3}\begin{bmatrix}\int f_j\,\mathrm{d}x \\ \int f_j\,\mathrm{d}y \\ \int f_j\,\mathrm{d}z\end{bmatrix}
\quad \text{（显式反散度，} \nabla\cdot\pmb{g}_j = f_j \text{）}
\]

**B-Rep 离散化（公式 7）**

\[
\int_{\Omega_c} f_j\,\mathrm{d}\Omega
= \sum_{f \in \Gamma_c} \int_f \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma
\quad \text{（逐三角面片求和，可达机器精度）}
\]

---

## 附表：按论文流程串联各环节

| 环节 | 该环节作用 | 对应公式 | 公式作用 |
|------|-----------|---------|---------|
| **1. 嵌入式方法基础设置** | 将全局弱形式体积分分解为背景网格上的逐单元积分，建立单元级积分框架 | **(1)** | 将 \(\int_\Omega(\cdot)\,\mathrm{d}\Omega\) 拆为 \(\sum_{B \in \mathcal{P}}\int_{B \cap \Omega}(\cdot)\,\mathrm{d}\Omega\)；内部单元用标准 Gauss 积分，切割单元需矩拟合 |
| **2. 边界积分分解** | 将 Dirichlet/Neumann 边界积分同样分解为逐单元框架，与体积分对称处理 | **(2)** | 将 \(\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma\) 拆为逐单元 STL 面片 \(F_\beta\) 的求和；\(\Gamma_{c,c} = B_{\mathrm{cut}} \cap \Gamma\) 直接用于施加边界条件 |
| **3. 矩拟合线性化** | 将切割域数值积分转化为以积分权重为未知量的线性代数问题 | **(3)** | 矩阵方程 \(A\boldsymbol{\omega} = \mathbf{b}\)：\(A_{ji} = f_j(\pmb{x}_i)\) 由预设点位确定；\(\mathbf{b}\)（矩向量）为真实积分值；NNLS 求解保证 \(\omega_i \geq 0\)，点剔除使 \(n_q \leq (p+1)^3\) |
| **4. 基函数空间选取** | 选择条件数最优的正交基函数，确保矩拟合方程组数值稳定 | **(4)** | 定义 \(F = \{L_r(x)L_s(y)L_t(z)\}\)；正交性（\(\int L_r L_s = \frac{2}{2r+1}\delta_{rs}\)）使 \(A^T A\) 接近对角矩阵，改善 NNLS 条件数 |
| **5. 散度定理降维** | 将三维体积分（矩 \(b_j\)）转化为封闭边界 \(\Gamma_c\) 上的二维面积分，消除体域空间细分 | **(5)** | 两步变换：① \(\nabla\cdot\pmb{g}_j = f_j\) 保证第一个等号；② 高斯散度定理（有界 Lipschitz 域成立）给出第二个等号；将三维计算降为二维，是整个框架的核心技巧 |
| **6. 反散度的显式构造** | 为每个基函数 \(f_j\) 给出满足 \(\nabla\cdot\pmb{g}_j = f_j\) 的解析向量场，确保步骤 5 第一个等号成立 | **(6)** | \(\pmb{g}_j = \frac{1}{3}[\int f_j\,\mathrm{d}x,\,\int f_j\,\mathrm{d}y,\,\int f_j\,\mathrm{d}z]^T\)；因子 \(\frac{1}{3}\) 使三方向均等；\(\pmb{g}_j\) 为多项式，高阶 Gauss 积分可精确计算 |
| **7. B-Rep 三角面片离散化** | 将封闭曲面积分在 STL 三角网格上数值离散，实现基于 B-Rep 的矩精确计算 | **(7)** | 将 \(\Gamma_c\) 分解为三角面片 \(\{f\}\)，逐面片计算 \(\int_f \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma\) 后求和；面片上用高阶 Gauss 积分达机器精度；要求 \(\Gamma_c\) 封闭、法向量一致向外 |
| **8. 封闭表面 \(\Gamma_c\) 的生成** | 为每个切割单元自动生成封闭表面 \(\Gamma_c = \partial(B_{\mathrm{cut}} \cap \Omega)\)，这是式 (7) 散度定理成立的必要前提 | **（引用式 2、7）** | 算法：① STL 对 AABB 做 Sutherland–Hodgman 裁剪得 \(\Gamma_{c,c}\)（供式 (2) 用）；② 对 AABB 六面上属于 \(\Omega\) 的开放区域三角化，与 \(\Gamma_{c,c}\) 合并得封闭 \(\Gamma_c\)（供式 (7) 用）；唯一质量准则：\(\Gamma_c\) 上常数基函数的散度定理误差 \(\leq \varepsilon\) |
| **9. 单元分类（泛洪填充）** | 将背景网格所有单元分类为 \(B_{\mathrm{in}}\)、\(B_{\mathrm{out}}\)、\(B_{\mathrm{cut}}\)，确定式 (1) 中活跃集合 \(\mathcal{P}\) | **（引用式 1）** | 从种子单元出发泛洪传播分类；\(B_{\mathrm{in}}\) 直接用标准 Gauss 积分；\(B_{\mathrm{cut}}\) 执行矩拟合全流程；改进为支持任意种子点及并行执行 |

---

*文档完*
