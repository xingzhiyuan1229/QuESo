# 公式解读

本文档逐一解释《文献2.md》中带编号的公式，并给出必要推导与公式之间的内在联系。为满足渲染要求，正文中的数学符号和行内公式均采用 $...$，带编号公式使用 $$...$$。

---

## 2. Embedded boundary method（嵌入边界方法）

### 2.1 Definition（定义）

**公式 (1)**

$$
\int_{\Omega} (\cdot) \, d\Omega = \sum_{\mathcal{B} \in \mathcal{P}} \int_{\mathcal{B} \cap \Omega} (\cdot) \, d\Omega,
\tag{1}
$$

**解释与推导**：
- 该式将全局域 $\Omega$ 上的体积分解为“活动网格” $\mathcal{P}$ 中各单元与实体域交集 $\mathcal{B} \cap \Omega$ 上的局部积分之和。
- 推导来自于集合分解：活动网格 $\mathcal{P}=\mathcal{P}_{\text{bg}}\setminus \mathcal{B}_{\text{out}}$ 对应所有非外部单元的并集，且这些单元在数值积分时互不重叠，因此整体积分可写成逐单元求和。
- 公式 (1) 为嵌入边界法的积分框架，后续公式 (2) 给出与其对应的边界积分形式。

**公式 (2)**

$$
\int_{\Gamma_{\beta}} (\cdot) \, d\Gamma = \sum_{\mathcal{B} \in \mathcal{P}} \sum_{F_{\beta} \in \Gamma_{\beta}} \int_{F_{\beta} \cap \mathcal{B}} (\cdot) \, d\Gamma, \quad \beta \in \{D, N\},
\tag{2}
$$

**解释与推导**：
- 与公式 (1) 类似，这里将边界积分分解为每个活动单元与边界片段 $F_\beta$ 的交集上的积分。
- $\Gamma_D$ 与 $\Gamma_N$ 分别表示 Dirichlet 与 Neumann 边界，$\beta\in\{D,N\}$ 统一表示两类边界积分。
- 该式保证在嵌入网格上可一致地计算弱形式中的边界项，与公式 (1) 共同构成嵌入方法的基本积分结构。

---

## 3. Node-based shape optimization using implicit Vertex-Morphing（基于节点的形状优化）

### 3.0 优化问题表述

**公式 (3)**

$$
\begin{array}{rl}
\min & f(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \\
\mathrm{subject~to} & \boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \mathbf{0}, \quad \mathrm{on}~\Gamma, \\
& \boldsymbol{r}(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = \mathbf{0}, \quad \mathrm{in}~\Omega, \\
& h_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = 0, \quad j = 1\dots m_h, \\
& g_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \leq 0, \quad j = 1\dots m_g,
\end{array}
\tag{3}
$$

**解释与推导**：
- 该式给出外层优化、内层分析的 NAND 结构：目标函数 $f$ 依赖形状 $\boldsymbol{x}_\Gamma$ 与状态变量 $\boldsymbol{u}$。
- 约束包含：几何参数化方程 $\boldsymbol{p}=0$（由滤波或参数化给出）、平衡方程残差 $\boldsymbol{r}=0$（即有限元方程），以及等式/不等式约束 $h_j, g_j$。
- 该优化问题在后续通过 Rosen 投影法公式 (10) 进行迭代求解；公式 (3) 的结构也直接决定了灵敏度需要对 $\boldsymbol{s}_\Gamma$ 与 $\boldsymbol{x}_\Gamma$ 进行传递与过滤（见公式 (11)、(41)、(42)）。

### 3.1 Implicit Vertex-Morphing（隐式顶点变形）

**公式 (4)**

$$
\begin{array}{rl}
& p^j = -(r_\Gamma^H)^2 \nabla_\Gamma \cdot \nabla_\Gamma x_\Gamma^i + x_\Gamma^i - s_\Gamma^i = 0, \quad i \in \{1,2,3\}, \quad \text{on}~\Gamma, \\
& x_\Gamma^i = s_\Gamma^i, \quad \text{along}~\Gamma_D^H \text{ (fixed boundaries)},
\end{array}
\tag{4}
$$

**解释与推导**：
- 该式是 Helmholtz/Sobolev 滤波的表面 PDE 形式，$r_\Gamma^H$ 为滤波半径，$x_\Gamma^i$ 为物理顶点坐标，$s_\Gamma^i$ 为控制场（设计变量）。
- 推导思路：最常见的隐式滤波可视为在边界上最小化能量 $\int_\Gamma \left((x_\Gamma^i-s_\Gamma^i)^2 + (r_\Gamma^H)^2\|\nabla_\Gamma x_\Gamma^i\|^2\right)\,d\Gamma$，对 $x_\Gamma^i$ 取变分即可得到 Helmholtz 方程。
- 第二行给出固定边界（非设计面）上的 Dirichlet 条件，使非设计区域保持不动。
- 公式 (4) 是后续离散化公式 (6)–(9) 的连续起点。

**公式 (5)**

$$
\nabla_\Gamma u = \nabla u - (\boldsymbol{n} \cdot \nabla u) \boldsymbol{n},
\tag{5}
$$

**解释与推导**：
- $\nabla_\Gamma u$ 为切向梯度，其等于三维梯度 $\nabla u$ 减去法向分量 $(\boldsymbol{n}\cdot\nabla u)\boldsymbol{n}$。
- 该式来自向量分解：任意向量可分解为切向与法向两部分，切向部分通过投影矩阵 $\mathbf{I}-\boldsymbol{n}\boldsymbol{n}^T$ 获得。
- 公式 (5) 提供了计算表面 Laplace–Beltrami 算子的基础，是公式 (4) 离散时 $\nabla_\Gamma$ 的实现依据。

**公式 (6)**

$$
\boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \boldsymbol{K}_\Gamma^H \boldsymbol{x}_\Gamma + \boldsymbol{M}_\Gamma^H \boldsymbol{x}_\Gamma - \boldsymbol{M}_\Gamma^H \boldsymbol{s}_\Gamma = \mathbf{0},
\tag{6}
$$

**解释与推导**：
- 对公式 (4) 进行 Galerkin 离散化，得到线性系统形式 $\mathbf{K}^H_\Gamma \boldsymbol{x}_\Gamma + \mathbf{M}^H_\Gamma \boldsymbol{x}_\Gamma = \mathbf{M}^H_\Gamma \boldsymbol{s}_\Gamma$。
- 其中 $\boldsymbol{K}_\Gamma^H$ 对应拉普拉斯项，$\boldsymbol{M}_\Gamma^H$ 对应零阶项 $x_\Gamma^i-s_\Gamma^i$。
- 公式 (6) 说明滤波后的物理几何是控制场 $\boldsymbol{s}_\Gamma$ 的线性映射，后续可通过求解得到公式 (9)。

**公式 (7)**

$$
\boldsymbol{M}_\Gamma^H = \sum \int_{\Gamma^e} \boldsymbol{N}_\Gamma^T \boldsymbol{N}_\Gamma \, d\Gamma,
\tag{7}
$$

**解释与推导**：
- 这是表面质量矩阵的标准定义，来源于弱式中的 $\int_\Gamma (x_\Gamma^i - s_\Gamma^i) v\, d\Gamma$。
- $\boldsymbol{N}_\Gamma$ 为边界单元形函数（由体单元形函数在边界的迹得到）。
- 公式 (7) 与公式 (8) 一起组成离散系统中的两类矩阵项。

**公式 (8)**

$$
\boldsymbol{K}_\Gamma^H = (r_\Gamma^H)^2 \sum \int_{\Gamma^e} (\nabla_\Gamma \boldsymbol{N}_\Gamma)^T \nabla_\Gamma \boldsymbol{N}_\Gamma \, d\Gamma,
\tag{8}
$$

**解释与推导**：
- 该式是表面刚度矩阵，来自 Helmholtz 方程中的二阶导项 $(r_\Gamma^H)^2 \nabla_\Gamma \cdot \nabla_\Gamma x_\Gamma^i$。
- 系数 $(r_\Gamma^H)^2$ 表示滤波半径的平方，直接控制平滑强度。
- 公式 (8) 与公式 (7) 共同构成公式 (6) 的离散系统。

**公式 (9)**

$$
\boldsymbol{x}_\Gamma = \boldsymbol{A}_\Gamma^H \boldsymbol{s}_\Gamma; \quad \boldsymbol{A}_\Gamma^H = (\boldsymbol{K}_\Gamma^H + \boldsymbol{M}_\Gamma^H)^{-1} \boldsymbol{M}_\Gamma^H.
\tag{9}
$$

**解释与推导**：
- 由公式 (6) 得 $(\boldsymbol{K}_\Gamma^H + \boldsymbol{M}_\Gamma^H)\boldsymbol{x}_\Gamma = \boldsymbol{M}_\Gamma^H\boldsymbol{s}_\Gamma$，两边左乘逆矩阵即可得到公式 (9)。
- $\boldsymbol{A}_\Gamma^H$ 是滤波算子，将控制场映射到物理几何场。
- 该映射在后续灵敏度滤波（公式 (11)、(42)）中起关键作用。

### 3.2 Update rule（更新规则）

**公式 (10)**

$$
{}^{k+1}\boldsymbol{s}_\Gamma = {}^{k}\boldsymbol{s}_\Gamma - \alpha \underbrace{[\boldsymbol{I} - \boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}\boldsymbol{P}^T]}_{\text{projection}} \underbrace{\frac{d f}{d \boldsymbol{s}_\Gamma}\bigg|_{\mathcal{C}}}_{\text{projection}} - \underbrace{[\boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}]}_{\text{projection}} \boldsymbol{g}^a,
\tag{10}
$$

**解释与推导**：
- 该式是 Rosen 梯度投影法在控制空间中的迭代更新。
- 第一项为旧设计变量；第二项为投影后的负梯度方向，其中 $\boldsymbol{P}$ 收集所有激活约束梯度，投影矩阵 $\boldsymbol{I}-\boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}\boldsymbol{P}^T$ 将下降方向约束在可行域切空间内。
- 第三项为恢复（纠正）方向，用于处理非线性约束造成的残余违约。
- 公式 (10) 需要 $\frac{d f}{d \boldsymbol{s}_\Gamma}$ 与约束梯度，促成公式 (11) 与后续灵敏度链式传递。

**公式 (11)**

$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \boldsymbol{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c.
\tag{11}
$$

**解释与推导**：
- 由公式 (9) 可得 $\boldsymbol{x}_\Gamma = \boldsymbol{A}_\Gamma^H \boldsymbol{s}_\Gamma$，对响应 $J$ 应用链式法则：$\frac{dJ}{d\boldsymbol{s}_\Gamma} = (\boldsymbol{A}_\Gamma^H)^T \frac{dJ}{d\boldsymbol{x}_\Gamma}$。
- 由于 $\boldsymbol{A}_\Gamma^H$ 为对称正定矩阵（由 Helmholtz 滤波构造），文中直接写为 $\boldsymbol{A}_\Gamma^H$ 作用于梯度。
- 该式把物理空间梯度映射到控制空间梯度，是公式 (10) 中所需的核心输入。

---

## 4. Sensitivity analysis（灵敏度分析）

### 4.0 背景方程

**公式 (12)**

$$
\mathbf{K} \mathbf{u} = \mathbf{F},
\tag{12}
$$

**解释与推导**：
- 这是线弹性或一般静力问题在背景网格 $\mathcal{P}$ 上的离散平衡方程。
- 公式 (12) 为灵敏度推导的起点，通过对其求导可得到状态变量 $\mathbf{u}$ 对几何参数的导数（见公式 (14)）。

### 4.1 半解析灵敏度

**公式 (13)**

$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \frac{d\mathbf{u}}{d\boldsymbol{x}_p}.
\tag{13}
$$

**解释与推导**：
- 这是对响应 $J(\boldsymbol{x}_p,\mathbf{u})$ 的链式法则展开，$\boldsymbol{x}_p$ 为背景网格节点坐标。
- 第一项为显式几何依赖，第二项为通过状态变量 $\mathbf{u}$ 间接依赖。
- 公式 (13) 给出离散（网格相关）灵敏度，为后续加权插值做准备。

**公式 (14)**

$$
\frac{d\mathbf{u}}{d\boldsymbol{x}_p} = \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right).
\tag{14}
$$

**解释与推导**：
- 对公式 (12) 求导：$\frac{d\mathbf{K}}{d\boldsymbol{x}_p}\mathbf{u} + \mathbf{K}\frac{d\mathbf{u}}{d\boldsymbol{x}_p} = \frac{d\mathbf{F}}{d\boldsymbol{x}_p}$。
- 解出 $\frac{d\mathbf{u}}{d\boldsymbol{x}_p}$ 即得公式 (14)。
- 该式直接代入公式 (13) 得到全导数形式的解析灵敏度公式 (15)。

**公式 (15)**

$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right) = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \mathbf{F}^*.
\tag{15}
$$

**解释与推导**：
- 将公式 (14) 代入公式 (13) 得到左侧表达式。
- 定义伪载荷向量 $\mathbf{F}^* = \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p}\mathbf{u}$，从而得到右侧紧凑形式。
- 该式为半解析灵敏度的核心，后续通过有限差分近似 $\mathbf{F}^*$（见公式 (16)）。

**公式 (16)**

$$
\mathbf{F}^* = \frac{\mathbf{F}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{F}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} - \frac{\mathbf{K}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{K}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} \mathbf{u}.
\tag{16}
$$

**解释与推导**：
- 该式用有限差分逼近 $\frac{d\mathbf{F}}{d\boldsymbol{x}_p}$ 与 $\frac{d\mathbf{K}}{d\boldsymbol{x}_p}$，避免显式推导复杂的几何导数。
- 通过在节点 $\boldsymbol{x}_p$ 的小扰动 $\Delta\boldsymbol{x}_p$ 下重新组装 $\mathbf{K}$ 和 $\mathbf{F}$，即可得到 $\mathbf{F}^*$。
- 公式 (16) 与 (15) 联合给出可实现的半解析灵敏度算法。

**公式 (17)**

$$
\frac{dU}{d\boldsymbol{x}_p}\bigg|_d = \mathbf{u}^T \mathbf{F}^*.
\tag{17}
$$

**解释与推导**：
- 当响应为应变能 $U=\frac{1}{2}\mathbf{u}^T\mathbf{K}\mathbf{u}$ 时，$\frac{\partial U}{\partial \boldsymbol{x}_p}=0$（对坐标无显式依赖）。
- 同时有 $\frac{\partial U}{\partial \mathbf{u}}=\mathbf{K}\mathbf{u}$，代入公式 (15) 并利用 $\mathbf{K}^{-1}$ 得到 $\mathbf{u}^T\mathbf{F}^*$。
- 公式 (17) 使得应变能灵敏度可通过一次扰动分析得到，是后续形状更新的关键输入之一。

### 4.2 灵敏度加权与插值

#### 4.2.1 一维示例

**公式 (18)**

$$
\overline{V} = A (\overline{x}_\Gamma - \overline{x}_{p1}),
\tag{18}
$$

**解释与推导**：
- 边界拟合网格下的一维杆体体积为截面积 $A$ 乘以长度差。
- 该式为后续比较嵌入网格灵敏度提供参考基准。

**公式 (19)**

$$
\frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = A.
\tag{19}
$$

**解释与推导**：
- 对公式 (18) 对 $\overline{x}_\Gamma$ 求导即可得到常量 $A$。
- 说明边界拟合网格下的灵敏度包含截面积信息。

**公式 (20)**

$$
\frac{1}{A} \frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = 1.
\tag{20}
$$

**解释与推导**：
- 为获得网格无关的单位梯度，对公式 (19) 作归一化。
- 该式作为嵌入情形下灵敏度加权的参考解。

**公式 (21)**

$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (x_\Gamma - x_{p,2}).
\tag{21}
$$

**解释与推导**：
- 嵌入网格下，一个单元被切分为两段体积 $V^1$ 与 $V^2$。
- 总体积 $V=V^1+V^2$，但边界位置 $x_\Gamma$ 不再与节点重合。

**公式 (22)**

$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (N_1(\xi_\Gamma) x_{p,2} + N_2(\xi_\Gamma) x_{p,3} - x_{p,2}).
\tag{22}
$$

**解释与推导**：
- 使用形函数表示 $x_\Gamma = N_1(\xi_\Gamma)x_{p,2}+N_2(\xi_\Gamma)x_{p,3}$。
- 将此代入公式 (21) 得到嵌入情况下的体积表达式。

**公式 (23)**

$$
\frac{dV}{dx_{p,2}}\bigg|_d = \frac{dV^1}{dx_{p,2}}\bigg|_d + \frac{dV^2}{dx_{p,2}}\bigg|_d = A N_1(\xi_\Gamma),
\tag{23}
$$

**解释与推导**：
- 对公式 (22) 中 $V^1$ 与 $V^2$ 分别求导并相加即可。
- 结果依赖 $\xi_\Gamma$，说明嵌入情况下灵敏度会受切割位置影响。

**公式 (24)**

$$
\frac{dV}{dx_{p,3}}\bigg|_d = \frac{dV^1}{dx_{p,3}}\bigg|_d + \frac{dV^2}{dx_{p,3}}\bigg|_d = A N_2(\xi_\Gamma).
\tag{24}
$$

**解释与推导**：
- 与公式 (23) 类似，对 $x_{p,3}$ 求导得到形函数权重 $N_2(\xi_\Gamma)$。
- 仍然体现了网格与切割位置的依赖性，需要加权消除。

**公式 (25)**

$$
\mathcal{N}_{x_{p,2}} = \int N_1 \, d\Gamma = A N_1(\xi_\Gamma),
\tag{25}
$$

**解释与推导**：
- 将形函数 $N_1$ 在边界截面 $\Gamma$ 上积分得到权重因子。
- 该权重衡量节点扰动对边界的影响程度。

**公式 (26)**

$$
\mathcal{N}_{x_{p,3}} = \int N_2 \, d\Gamma = A N_2(\xi_\Gamma).
\tag{26}
$$

**解释与推导**：
- 与公式 (25) 同理，得到节点 $x_{p,3}$ 的权重因子。
- 公式 (25)–(26) 为灵敏度加权提供必要尺度。

**公式 (27)**

$$
\frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d = 1,
\tag{27}
$$

**解释与推导**：
- 将公式 (23) 用权重 $\mathcal{N}_{x_{p,2}}$ 归一化，即可消除对 $\xi_\Gamma$ 的依赖。
- 该操作使节点灵敏度达到单位梯度，与参考解 (20) 一致。

**公式 (28)**

$$
\frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1.
\tag{28}
$$

**解释与推导**：
- 公式 (24) 通过权重 $\mathcal{N}_{x_{p,3}}$ 归一化后得到单位梯度。
- 该式与公式 (27) 一起保证所有参与插值的节点具有一致的“标准化”灵敏度。

**公式 (29)**

$$
\frac{dV}{dx_\Gamma}\bigg|_c = N_1(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d + N_2(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1,
\tag{29}
$$

**解释与推导**：
- 以形函数在边界位置处插值加权后的节点灵敏度，得到边界处连续梯度。
- 由于公式 (27)–(28) 已归一化，插值结果恒为 1，达到与公式 (20) 相同的网格无关结果。
- 该式为后续二维与三维推广（公式 (35)、(36)）提供范式。

#### 4.2.2 二维示例

**公式 (30)**

$$
\frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = t (\Delta \overline{A}^1 + \Delta \overline{A}^2) = t a,
\tag{30}
$$

**解释与推导**：
- 对边界拟合网格施加单位位移，面积变化为 $\Delta\overline{A}^1+\Delta\overline{A}^2$。
- 体积变化为厚度 $t$ 乘以面积变化，得到公式 (30)。
- 该式表明灵敏度包含网格尺度信息，需要归一化处理。

**公式 (31)**

$$
\frac{1}{t a} \frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = 1.
\tag{31}
$$

**解释与推导**：
- 用局部尺度 $t a$ 归一化得到单位梯度。
- 作为二维嵌入情况下的参考结果。

**公式 (32)**

$$
\frac{dV}{dx_p^*}\bigg|_d = t (\Delta A^1 + \Delta A^2) = t \left( N_{\boldsymbol{x}_p^*}^1(\hat{\xi}, \hat{\eta}) \frac{a}{2} + N_{\boldsymbol{x}_p^*}^2(\hat{\xi}, \hat{\eta}) \frac{a}{2} \right).
\tag{32}
$$

**解释与推导**：
- 嵌入情况下的面积变化由边界与单元交线决定，边界上的最大位移由形函数值决定。
- 该式将面积变化分配到两个切割三角形，显示灵敏度与形函数相关。

**公式 (33)**

$$
\mathcal{N}_{\boldsymbol{x}_p^*} = \int_{\Gamma} N_{\boldsymbol{x}_p^*}^1 \, d\Gamma + \int_{\Gamma} N_{\boldsymbol{x}_p^*}^2 \, d\Gamma.
\tag{33}
$$

**解释与推导**：
- 权重定义为边界上所有与该节点相关形函数的积分。
- 该权重衡量该节点对边界几何的“影响面积”。

**公式 (34)**

$$
\frac{1}{\mathcal{N}_{\boldsymbol{x}_p^*}} \frac{dV}{dx_p^*}\bigg|_d = 1.
\tag{34}
$$

**解释与推导**：
- 将公式 (32) 用公式 (33) 的权重归一化，获得单位梯度。
- 这与公式 (31) 的参考单位梯度一致，说明加权可以消除网格依赖。

**公式 (35)**

$$
\frac{dV}{dx_\Gamma^*}\bigg|_c = \sum_{i=1}^{n=4} N_{\boldsymbol{x}_{pi}}(\hat{\xi}, \hat{\eta}) \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dV}{d\boldsymbol{x}_{pi}}\bigg|_d \right) = 1,
\tag{35}
$$

**解释与推导**：
- 以四节点形函数插值加权后的节点灵敏度，得到边界点的连续梯度。
- 与一维结果 (29) 对应，是二维情况下的统一插值公式。
- 该式与后续三维推广公式 (36) 在形式上保持一致。

#### 4.2.3 三维推广与一致性

**公式 (36)**

$$
\frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c = \sum_{i \in \mathcal{I}} N_{\boldsymbol{x}_{pi}} \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dJ}{d\boldsymbol{x}_{pi}}\bigg|_d \right),
\tag{36}
$$

**解释与推导**：
- 该式将一维、二维的归一化插值策略推广到三维，$\mathcal{I}$ 为在 $\boldsymbol{x}_\Gamma$ 处有支撑的背景节点集合。
- 先对离散灵敏度进行尺度校正，再由形函数插值到边界位置，得到连续梯度场。
- 公式 (36) 是本文核心之一，与后续权重定义 (37) 构成完整流程。

**公式 (37)**

$$
\mathcal{N}_{\boldsymbol{x}_{pi}} = \sum_{j \in \mathcal{J}} \int_{\Gamma} N_{\boldsymbol{x}_{pi}} \, d\Gamma,
\tag{37}
$$

**解释与推导**：
- $\mathcal{N}_{\boldsymbol{x}_{pi}}$ 统计与节点 $\boldsymbol{x}_{pi}$ 相关的所有形函数在边界上的积分。
- 该式是三维情况下的权重定义，对应一维的公式 (25) 与二维的公式 (33)。
- 与公式 (36) 联立可消除切割位置的网格依赖，实现连续、可比较的灵敏度。

**公式 (38)**

$$
 e_{V} = \frac{\| \mathbf{V}_{\text{grad}} - \mathbf{N} \|_{L2}}{\| \mathbf{N} \|_{L2}},
\tag{38}
$$

**解释与推导**：
- 该式定义了灵敏度场与参考法向场之间的相对 $L_2$ 误差。
- $\mathbf{V}_{\text{grad}}$ 是边界上计算得到的体积梯度向量，$\mathbf{N}$ 是理论上均匀的单位法向向量。
- 公式 (38) 用于验证公式 (36)–(37) 的一致性与收敛性。

---

## 5. Geometrical constraints（几何约束）

### 5.1 Active constraint detection（活跃约束检测）

**公式 (39)**

$$
\begin{aligned}
 d &< 2 \Delta x_{\max} + t_{\min} \quad \text{(for wall thickness constraints)}, \\
 d &< 2 \Delta x_{\max} \quad \text{(for design space constraints)},
\end{aligned}
\tag{39}
$$

**解释与推导**：
- 该式定义约束激活条件：当沿法向的距离 $d$ 小于阈值时，对应顶点被标记为活跃约束。
- 对壁厚约束需额外满足最小厚度 $t_{\min}$，而设计空间约束只需考虑安全缓冲 $2\Delta x_{\max}$。
- 该式为构建阻尼矩阵（公式 (40)）提供约束集合。

### 5.2 Constraint enforcement（约束施加）

**公式 (40)**

$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c^{\text{proj}} = \mathbf{D}_\Gamma \frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c, \quad \text{with} \quad \mathbf{D}_\Gamma = [\mathbf{I} - \mathbf{N}(\mathbf{N}^T \mathbf{N})^{-1} \mathbf{N}^T].
\tag{40}
$$

**解释与推导**：
- $\mathbf{D}_\Gamma$ 是投影矩阵，将控制梯度投影到约束法向的正交补空间。
- 推导来自约束优化中的正交投影：对不允许的方向（由 $\mathbf{N}$ 给出）去分量。
- 该式与公式 (10) 的投影思想一致，但用于局部几何约束的梯度抑制。

**公式 (41)**

$$
\boldsymbol{x}_\Gamma = \mathbf{A}_\Gamma^H \mathbf{D}_\Gamma \boldsymbol{s}_\Gamma,
\tag{41}
$$

**解释与推导**：
- 将约束阻尼 $\mathbf{D}_\Gamma$ 引入公式 (9) 的参数化关系，即在滤波前先对控制场做约束投影。
- 该式确保几何更新不会沿不允许方向发生，从源头避免不可行形状。

**公式 (42)**

$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \mathbf{D}_\Gamma \mathbf{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c.
\tag{42}
$$

**解释与推导**：
- 在公式 (11) 的链式法则基础上，加入约束投影 $\mathbf{D}_\Gamma$。
- 该式与公式 (41) 一致，保证控制梯度与参数化都遵循约束方向的限制。

---

## 7. Numerical experiments（数值实验）

### 7.1 Volume maximization of a cube（体积最大化）

**公式 (43)**

$$
{}^{k+1}\boldsymbol{x}_\Gamma = {}^{k}\boldsymbol{x}_\Gamma + \alpha \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c,
\tag{43}
$$

**解释与推导**：
- 当不使用滤波（或直接在物理空间更新）时，形状更新可由连续梯度场直接驱动。
- 该式是标准梯度下降更新，与公式 (10) 的控制空间更新相对应，但作用于物理坐标。
- 使用公式 (36) 获得的连续梯度 $\frac{dJ}{d\boldsymbol{x}_\Gamma}|_c$ 可保证更新平滑且网格无关。

**公式 (44)**

$$
 e_R = \frac{\max\{R\} - \min\{R\}}{\mathrm{avg}\{R\}}, \quad \text{with} \quad \mathrm{avg}\{R\} = \frac{\sum_{R \in \mathcal{R}} R}{|\mathcal{R}|},
\tag{44}
$$

**解释与推导**：
- $R$ 表示边界顶点到几何中心的距离，$\mathcal{R}$ 为全部顶点集合。
- 相对误差 $e_R$ 度量形状接近球体的程度（半径是否一致）。
- 该指标用于评估公式 (43) 更新后形状的趋圆过程。

### 7.2 Strain energy minimization of a hook（挂钩的应变能最小化）

**公式 (45)**

$$
\begin{array}{rl}
\min & U, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& m - m_{\text{init}} = 0,
\end{array}
\tag{45}
$$

**解释与推导**：
- 该式为公式 (3) 在特定算例下的实例化：目标为应变能 $U$，同时保持质量等式约束。
- $\mathbf{p}=0$ 与 $\mathbf{r}=0$ 分别对应参数化约束与平衡方程，与公式 (3) 一致。
- 该问题的灵敏度通过公式 (17)、(36) 与 (42) 传递到控制空间，再由公式 (10) 或 (43) 更新形状。

### 7.3 Mass minimization of a jet engine bracket（支架质量最小化）

**公式 (46)**

$$
\begin{array}{rl}
\min & m, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& U_j - U_j^{\text{init}} = 0, \quad j \in \{1,2,3,4\},
\end{array}
\tag{46}
$$

**解释与推导**：
- 与公式 (45) 类似，此处目标为质量 $m$，同时保持多个载荷工况的应变能约束。
- 该式体现了公式 (3) 的可扩展性：通过加入多约束 $U_j$ 保证结构性能。
- 该优化过程依赖前文公式 (10)–(42) 提供的形状参数化、灵敏度计算与约束投影体系。

---

## 公式之间的内在联系总结

- 公式 (1)–(2) 定义了嵌入边界方法在体积分与边界积分上的离散框架，为后续有限元分析（公式 (12)）奠定基础。
- 公式 (3) 统一描述了优化问题的目标与约束，而公式 (4)–(9) 通过隐式滤波将控制变量 $\boldsymbol{s}_\Gamma$ 映射到几何变量 $\boldsymbol{x}_\Gamma$。
- 公式 (10) 给出控制空间的更新策略；公式 (11) 则把物理空间的梯度转换为控制空间梯度，与公式 (9) 直接耦合。
- 公式 (12)–(17) 描述背景网格上的半解析灵敏度计算；公式 (18)–(38) 通过加权与插值把离散灵敏度转化为连续、网格无关的边界梯度。
- 公式 (39)–(42) 将几何约束融入参数化与梯度计算，保证优化过程始终保持可行。
- 公式 (43)–(46) 展示了以上理论在数值实验中的具体应用，构成完整的优化工作流闭环。
