# 公式与底层代码对照解读

**论文标题：** Robust numerical integration of embedded solids described in boundary representation

**作者：** Manuel Messmer, Stefan Kollmannsberger, Roland Wüchner, Kai-Uwe Bletzinger

**代码库：** https://github.com/xingzhiyuan1229/QuESo

---

## 第 2 节：嵌入式方法（Embedded Methods）

### 公式 (1)：域分解积分

**底层实现代码：**

文件：[`queso/quadrature/single_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/single_element.hpp#L56-L102)

```cpp
// queso/quadrature/single_element.hpp，第 56–102 行
// 对非裁切元素（B_in）进行标准 Gauss 张量积积分规则组装
static void AssembleIPs(IntegrationPointVectorType& rIntegrationPoints,
                        const PointType& rLowerBoundParam,
                        const PointType& rUpperBoundParam,
                        const Vector3i& rOrder,
                        IntegrationMethodType Method = IntegrationMethod::gauss) {
    const auto& r_ip_list_u = IntegrationPointFactory1D::GetGauss(rOrder[0], Method);
    const auto& r_ip_list_v = IntegrationPointFactory1D::GetGauss(rOrder[1], Method);
    const auto& r_ip_list_w = IntegrationPointFactory1D::GetGauss(rOrder[2], Method);

    const SizeType n_point_u = r_ip_list_u.size();
    const SizeType n_point_v = r_ip_list_v.size();
    const SizeType n_point_w = r_ip_list_w.size();

    const SizeType n_point = (n_point_u)*(n_point_v)*(n_point_w);
    rIntegrationPoints.clear();
    rIntegrationPoints.reserve(n_point);

    const double length_u = std::abs(rUpperBoundParam[0] - rLowerBoundParam[0]);
    const double length_v = std::abs(rUpperBoundParam[1] - rLowerBoundParam[1]);
    const double length_w = std::abs(rUpperBoundParam[2] - rLowerBoundParam[2]);

    for (SizeType u = 0; u < n_point_u; ++u) {
        for (SizeType v = 0; v < n_point_v; ++v) {
            for( SizeType w = 0; w < n_point_w; ++w) {
                rIntegrationPoints.push_back( IntegrationPointType(
                    rLowerBoundParam[0] + length_u * (r_ip_list_u)[u][0],
                    rLowerBoundParam[1] + length_v * (r_ip_list_v)[v][0],
                    rLowerBoundParam[2] + length_w * (r_ip_list_w)[w][0],
                    (r_ip_list_u)[u][1] * length_u *
                    (r_ip_list_v)[v][1] * length_v *
                    (r_ip_list_w)[w][1] * length_w ) );
            }
        }
    }
}
```

**对应公式 (1)：**

$$
\int_{\Omega}(\cdot)\,\mathrm{d}\Omega
= \sum_{B\in \mathcal{P}} \int_{B\cap\Omega}(\cdot)\,\mathrm{d}\Omega
\quad (1)
$$

**解读：** 上述代码实现了对背景网格 $\mathcal{P}$ 中每个非裁切（内部）元素 $B_{\mathrm{in}}$ 上的积分贡献的组装。对于这些元素，$B_{\mathrm{in}} \cap \Omega = B_{\mathrm{in}}$，因此可直接应用标准三维张量积 Gauss 求积分规则。外层三重 `for` 循环分别在参数方向 $u, v, w$ 上遍历 1D Gauss 积分点，将三个方向的权重相乘得到最终权重，从而实现 $\int_{B_{\mathrm{in}}} (\cdot)\,\mathrm{d}\Omega$ 的数值积分。整个背景网格上的求和 $\sum_{B \in \mathcal{P}}$ 由上层调用函数（嵌入式模型）循环遍历所有活跃元素来完成。

---

### 公式 (2)：边界积分分解

**底层实现代码：**

文件：[`queso/quadrature/trimmed_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/trimmed_element.hpp#L66-L131)

```cpp
// queso/quadrature/trimmed_element.hpp，第 66–131 行
// 为裁切元素（B_cut）组装积分点
// 内部调用获取边界积分点（Gamma_c = Gamma ∩ B_cut），这正是 Eq.(2) 所需的量
static double AssembleIPs(ElementType& rElement,
                          const Vector3i& rIntegrationOrder,
                          double Residual,
                          IndexType EchoLevel=0) {
    // 获取裁切域的边界积分点
    // p_trimmed_domain 包含 Gamma_c = B_cut ∩ Gamma（用于 Eq.(2)）
    // 以及 partial(B_cut ∩ Omega)（用于 Eq.(1)）
    const auto p_trimmed_domain = rElement.pGetTrimmedDomain();
    const auto p_boundary_ips =
        p_trimmed_domain->template pGetBoundaryIps<
            typename TElementType::BoundaryIntegrationPointType>();

    // 用边界积分点计算 Moment Fitting 方程右端项（常数项）
    VectorType constant_terms{};
    ComputeConstantTerms(constant_terms, p_boundary_ips,
                         rElement, rIntegrationOrder);

    // 构建 Octree，用于在裁切域内分布初始积分点
    const auto bounding_box =
        p_trimmed_domain->GetBoundingBoxOfTrimmedDomain();
    BoundingBoxType bounding_box_uvw =
        MakeBox( rElement.PointFromGlobalToParam(bounding_box.first),
                 rElement.PointFromGlobalToParam(bounding_box.second));
    Octree<TrimmedDomain> octree(p_trimmed_domain,
                                 bounding_box, bounding_box_uvw);

    // 执行点消除迭代，最终在 rElement 中存储优化后的积分点
    double residual = MAXD;
    SizeType iteration = 0UL;
    SizeType point_distribution_factor = 1;
    IntegrationPointVectorType integration_points{};

    const IndexType max_iteration =
        (Math::Max(rIntegrationOrder) == 2) ? 4UL : 3UL;
    while( residual > Residual && iteration < max_iteration){
        const SizeType min_num_points =
            (rIntegrationOrder[0]+1)*(rIntegrationOrder[1]+1)*
            (rIntegrationOrder[2]+1)*(point_distribution_factor);
        DistributeIntegrationPoints(integration_points, octree,
                                    min_num_points, rIntegrationOrder);
        if( integration_points.size() == 0 ){
            rElement.GetIntegrationPoints().clear();
            return 1;
        }
        auto& old_integration_points = rElement.GetIntegrationPoints();
        integration_points.insert(integration_points.end(),
            old_integration_points.begin(), old_integration_points.end());
        old_integration_points.clear();

        residual = PointElimination(constant_terms, integration_points,
                                    rElement, rIntegrationOrder, Residual);
        if( residual > 1e-2 ) {
            auto& reduced_points = rElement.GetIntegrationPoints();
            reduced_points.clear();
        }
        point_distribution_factor *= 2;
        iteration++;
    }
    return residual;
}
```

**对应公式 (2)：**

$$
\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma
= \sum_{B\in \mathcal{P}}\sum_{F_\beta\in\Gamma_\beta}
  \int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma,
\quad \beta\in\{D,N\}
\quad (2)
$$

**解读：** 公式 (2) 描述了边界（Dirichlet 或 Neumann）积分在背景网格各元素上的分解。代码通过 `p_boundary_ips = p_trimmed_domain->pGetBoundaryIps(...)` 获取裁切面 $\Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma$ 上的边界积分点 $F_\beta \cap B_{\mathrm{cut}}$，这些积分点正好对应公式中的 $\sum_{F_\beta\in\Gamma_\beta}\int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma$ 部分。外层对所有元素的求和 $\sum_{B \in \mathcal{P}}$ 由嵌入式模型主循环完成。

---

## 第 3.1 节：矩拟合（Moment Fitting）

### 公式 (3)：矩拟合方程组

**底层实现代码：**

文件：[`queso/quadrature/trimmed_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/trimmed_element.hpp#L293-L343)

```cpp
// queso/quadrature/trimmed_element.hpp，第 293–343 行
// 组装矩拟合矩阵并求解加权最小二乘问题
static double MomentFitting(const VectorType& rConstantTerms,
                             IntegrationPointVectorType& rIntegrationPoint,
                             const ElementType& rElement,
                             const Vector3i& rIntegrationOrder) {

    PointType a = rElement.GetBoundsUVW().first;
    PointType b = rElement.GetBoundsUVW().second;
    double jacobian = rElement.DetJ();

    const IndexType ffactor = 1;
    const IndexType order_u = rIntegrationOrder[0];
    const IndexType order_v = rIntegrationOrder[1];
    const IndexType order_w = rIntegrationOrder[2];

    // 函数个数 = (p_u+1)(p_v+1)(p_w+1)，对应 m 行
    const IndexType number_of_functions =
        (order_u*ffactor + 1) * (order_v*ffactor+1) * (order_w*ffactor + 1);
    // 积分点个数，对应 n_q 列
    const IndexType number_reduced_points = rIntegrationPoint.size();

    const double l2_norm_ct = std::sqrt(
        std::inner_product(rConstantTerms.begin(), rConstantTerms.end(),
                           rConstantTerms.begin(), 0.0));

    // 组装矩拟合矩阵：fitting_matrix[column * number_of_functions + row]
    // = f_j(x_i)，即矩阵第 j 行第 i 列 = f_j(x_i)
    // 对应 Eq.(3) 左侧矩阵 [f_1(x_1) ... f_1(x_{n_q}); ...; f_m(x_1) ... f_m(x_{n_q})]
    NNLS::MatrixType fitting_matrix(
        number_of_functions * number_reduced_points, 0.0);
    const auto points_it_begin = rIntegrationPoint.begin();
    for( IndexType column_index = 0;
         column_index < number_reduced_points; ++column_index ) {
        auto point_it = points_it_begin +
            static_cast<std::ptrdiff_t>(column_index);
        IndexType row_index = 0;
        for( IndexType i_x = 0; i_x <= order_u*ffactor; ++i_x){
            for( IndexType i_y = 0; i_y <= order_v*ffactor; ++i_y ){
                for( IndexType i_z = 0; i_z <= order_w*ffactor; ++i_z){
                    // f_j(x_i) = L_r(x) * L_s(y) * L_t(z)
                    const double value =
                        Polynomial::f_x(point_it->X(), i_x, a[0], b[0])
                      * Polynomial::f_x(point_it->Y(), i_y, a[1], b[1])
                      * Polynomial::f_x(point_it->Z(), i_z, a[2], b[2]);
                    // 矩阵按列存储（column first）
                    fitting_matrix[column_index*number_of_functions
                                   + row_index] = value;
                    row_index++;
                }
            }
        }
    }

    // 用 NNLS 求解非负最小二乘：min ||fitting_matrix * weights - constant_terms||
    // 对应 Eq.(3)：[f_j(x_i)] * [omega_i] = [∫f_j dΩ]
    VectorType weights(number_reduced_points);
    VectorType tmp_constant_terms(rConstantTerms);
    const double rel_residual =
        NNLS::solve(fitting_matrix, tmp_constant_terms, weights)
        / l2_norm_ct;

    // 将计算得到的权重写回积分点，除以 Jacobian 以抵消外部求解器中的相乘
    for( IndexType i = 0; i < number_reduced_points; ++i){
        double new_weight = weights[i]/(jacobian);
        rIntegrationPoint[i].SetWeight(new_weight);
    }

    return rel_residual;
}
```

**对应公式 (3)：**

$$
\begin{bmatrix}
f_1(\pmb{x}_1) & \cdots & f_1(\pmb{x}_{n_q}) \\
\vdots          & \ddots & \vdots              \\
f_m(\pmb{x}_1) & \cdots & f_m(\pmb{x}_{n_q})
\end{bmatrix}
\begin{bmatrix}
\omega_1 \\ \vdots \\ \omega_{n_q}
\end{bmatrix}
=
\begin{bmatrix}
\displaystyle\int_{\Omega_{\mathrm{c}}}f_1(\pmb{x})\,\mathrm{d}\Omega \\
\vdots \\
\displaystyle\int_{\Omega_{\mathrm{c}}}f_m(\pmb{x})\,\mathrm{d}\Omega
\end{bmatrix}
\quad (3)
$$

**解读：** `MomentFitting` 函数直接实现了公式 (3) 的矩阵形式：
- 左侧矩阵 $[f_j(\pmb{x}_i)]$ 由三重 `for` 循环组装为 `fitting_matrix`，每列对应一个积分点 $\pmb{x}_i$，每行对应一个基函数 $f_j$（Legendre 多项式张量积）。矩阵以列优先方式按序列化存储。
- 右侧向量 $[\int_{\Omega_c} f_j\,\mathrm{d}\Omega]$ 即为 `rConstantTerms`（常数项），由 `ComputeConstantTerms` 预先计算。
- 求解向量 $[\omega_i]$ 通过调用 `NNLS::solve` 求解非负最小二乘问题得到，保证所有权重均为正。
- 求得的权重除以单元的 Jacobian 行列式（`DetJ()`）后写入积分点，以与外部有限元求解器的约定保持一致。

---

### 公式 (4)：矩拟合基函数空间

**底层实现代码：**

文件：[`queso/utilities/polynomial_utilities.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/utilities/polynomial_utilities.hpp#L39-L86)

```cpp
// queso/utilities/polynomial_utilities.hpp，第 39–86 行
// 返回定义在区间 (a,b) 上的第 order 阶 Legendre 多项式 L_r(x) 的值
inline double f_x(double x, IndexType order, double a, double b) noexcept {
    switch(order) {
        case 0:
            // L_0(x) = 1
            return 1;
        case 1: {
            // L_1(ξ) = ξ，其中 ξ = (2x - a - b)/(b - a) ∈ [-1,1]
            double tmp_x = (2*x - a - b)/(b-a);
            return tmp_x;
        }
        case 2: {
            // L_2(ξ) = (3ξ^2 - 1)/2
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/2.0*(3.0*power(tmp_x,2)-1.0);
        }
        case 3: {
            // L_3(ξ) = (5ξ^3 - 3ξ)/2
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/2.0*(5.0*power(tmp_x,3) - 3.0*tmp_x);
        }
        case 4: {
            // L_4(ξ) = (35ξ^4 - 30ξ^2 + 3)/8
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/8.0*(35.0*power(tmp_x,4)-30.0*power(tmp_x,2)+3.0);
        }
        case 5: {
            // L_5(ξ) = (63ξ^5 - 70ξ^3 + 15ξ)/8
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/8.0*(63.0*power(tmp_x,5)-70.0*power(tmp_x,3)+15.0*tmp_x);
        }
        case 6: {
            // L_6(ξ) = (231ξ^6 - 315ξ^4 + 105ξ^2 - 5)/16
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/16.0*(231.0*power(tmp_x,6)-315.0*power(tmp_x,4)
                             +105.0*power(tmp_x,2)-5.0);
        }
        case 7: {
            // L_7(ξ) = (429ξ^7 - 693ξ^5 + 315ξ^3 - 35ξ)/16
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/16.0*(429.0*power(tmp_x,7)-693.0*power(tmp_x,5)
                             +315.0*power(tmp_x,3)-35.0*tmp_x);
        }
        case 8: {
            // L_8(ξ) = (6435ξ^8 - 12012ξ^6 + 6930ξ^4 - 1260ξ^2 + 35)/128
            double tmp_x = (2*x - a - b)/(b-a);
            return 1.0/128.0*(6435.0*power(tmp_x,8) - 12012.0*power(tmp_x,6)
                              +6930.0*power(tmp_x,4)-1260.0*power(tmp_x,2)+35.0);
        }
        default:
            return 0.0;
    };
}
```

**对应公式 (4)：**

$$
F = \bigl\{\, f_{j}(\pmb{x}) = L_{r}(x)\,L_{s}(y)\,L_{t}(z) \;;\;
r, s, t = 0,1,2,\ldots,p \,\bigr\}
\quad (4)
$$

**解读：** `Polynomial::f_x(x, order, a, b)` 实现了公式 (4) 中的每个一维 Legendre 多项式因子 $L_r(x)$（定义在区间 $[a,b]$ 上）。实现中首先将物理坐标 $x \in [a,b]$ 映射为标准坐标 $\xi = (2x - a - b)/(b-a) \in [-1,1]$，然后代入标准 Legendre 多项式的显式表达式（最高支持 8 阶）。三维基函数 $f_j(\pmb{x}) = L_r(x)\,L_s(y)\,L_t(z)$ 由 `MomentFitting` 函数中的三重 `for` 循环通过乘积形式 `Polynomial::f_x(...) * Polynomial::f_x(...) * Polynomial::f_x(...)` 组装实现，下标 $(r,s,t)$ 分别对应 `i_x`, `i_y`, `i_z`。

---

## 第 3.2 节：矩的计算（Computation of the Moments）

### 公式 (5)：散度定理变换

**底层实现代码：**

文件：[`queso/quadrature/trimmed_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/trimmed_element.hpp#L209-L284)

```cpp
// queso/quadrature/trimmed_element.hpp，第 209–284 行
// 利用散度定理，将体积分（矩）转化为边界曲面积分
static void ComputeConstantTerms(VectorType& rConstantTerms,
                                  const BoundaryIPsVectorPtrType& pBoundaryIPs,
                                  const ElementType& rElement,
                                  const Vector3i& rIntegrationOrder) {
    // 常数项在物理坐标系中求值
    const auto bounds_xyz = rElement.GetBoundsXYZ();
    const PointType& a = bounds_xyz.first;
    const PointType& b = bounds_xyz.second;

    const IndexType ffactor = 1;
    const IndexType order_u = rIntegrationOrder[0];
    const IndexType order_v = rIntegrationOrder[1];
    const IndexType order_w = rIntegrationOrder[2];

    const IndexType number_of_functions =
        (order_u*ffactor + 1) * (order_v*ffactor+1) * (order_w*ffactor + 1);

    rConstantTerms.resize(number_of_functions, false);
    std::fill(rConstantTerms.begin(), rConstantTerms.end(), 0.0);

    // 预分配各方向的 f_x 和 f_x_int 缓存
    std::vector<double> f_x_x(order_u*ffactor+1);
    std::vector<double> f_x_int_x(order_u*ffactor+1);
    std::vector<double> f_x_y(order_v*ffactor+1);
    std::vector<double> f_x_int_y(order_v*ffactor+1);
    std::vector<double> f_x_z(order_w*ffactor+1);
    std::vector<double> f_x_int_z(order_w*ffactor+1);

    // 遍历所有边界积分点（三角面片上的积分点）
    IndexType row_index = 0;
    const auto begin_points_it_ptr = pBoundaryIPs->begin();
    for( IndexType i = 0; i < pBoundaryIPs->size(); ++i ){
        auto point_it = (begin_points_it_ptr + static_cast<std::ptrdiff_t>(i));
        const auto& normal = point_it->Normal();  // 外法向量 n(x)
        PointType point = point_it->data();

        // 预计算各方向的 f_x 和 f_x_int（抗导数）
        for( IndexType i_x = 0; i_x <= order_u*ffactor; ++i_x){
            f_x_x[i_x]     = Polynomial::f_x(point[0], i_x, a[0], b[0]);
            f_x_int_x[i_x] = Polynomial::f_x_int(point[0], i_x, a[0], b[0]);
        }
        for( IndexType i_y = 0; i_y <= order_v*ffactor; ++i_y){
            f_x_y[i_y]     = Polynomial::f_x(point[1], i_y, a[1], b[1]);
            f_x_int_y[i_y] = Polynomial::f_x_int(point[1], i_y, a[1], b[1]);
        }
        for( IndexType i_z = 0; i_z <= order_w*ffactor; ++i_z){
            f_x_z[i_z]     = Polynomial::f_x(point[2], i_z, a[2], b[2]);
            f_x_int_z[i_z] = Polynomial::f_x_int(point[2], i_z, a[2], b[2]);
        }

        // 组装右端项（常数项/矩）
        row_index = 0;
        // 因子 1/3 对应 Eq.(6) 中 g_j 的定义
        const double weight = 1.0/3.0 * point_it->Weight();
        for( IndexType i_x = 0; i_x <= order_u*ffactor; ++i_x){
            for( IndexType i_y = 0; i_y <= order_v*ffactor; ++i_y ){
                for( IndexType i_z = 0; i_z <= order_w*ffactor; ++i_z){
                    // 计算 g_j(x) 的三个分量：
                    // value[0] = ∫f_j dx * f_y * f_z  （x 方向抗导数）
                    // value[1] = f_x * ∫f_j dy * f_z  （y 方向抗导数）
                    // value[2] = f_x * f_y * ∫f_j dz  （z 方向抗导数）
                    PointType value;
                    value[0] = f_x_int_x[i_x] * f_x_y[i_y]   * f_x_z[i_z];
                    value[1] = f_x_x[i_x]     * f_x_int_y[i_y] * f_x_z[i_z];
                    value[2] = f_x_x[i_x]     * f_x_y[i_y]   * f_x_int_z[i_z];

                    // 点积 g_j(x) · n(x)，实现散度定理中的曲面积分被积项
                    double integrand = normal[0]*value[0]
                                     + normal[1]*value[1]
                                     + normal[2]*value[2];
                    rConstantTerms[row_index] += integrand * weight;
                    row_index++;
                }
            }
        }
    }
}
```

**对应公式 (5)：**

$$
\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Omega_{\mathrm{c}}} \nabla\cdot\pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
\quad (5)
$$

**解读：** `ComputeConstantTerms`（边界积分点版本）实现了公式 (5) 中的散度定理变换：将体积分 $\int_{\Omega_c} f_j\,\mathrm{d}\Omega$ 转化为 $\Gamma_c$ 上的曲面积分 $\int_{\Gamma_c} \pmb{g}_j \cdot \pmb{n}\,\mathrm{d}\Gamma$。核心计算为：
- `value[0], value[1], value[2]` 分别对应矢量场 $\pmb{g}_j(\pmb{x})$ 的三个分量（见公式 (6)）。
- `integrand = normal[0]*value[0] + normal[1]*value[1] + normal[2]*value[2]` 计算点积 $\pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})$。
- 外层循环遍历所有边界积分点（即 $\Gamma_c$ 上各三角面片的积分点），对应曲面积分的求和。
- `weight = 1/3 * point_it->Weight()` 中的因子 $1/3$ 正是公式 (6) 中 $\pmb{g}_j$ 定义的 $1/3$ 前缀因子。

---

### 公式 (6)：抗导数 $\pmb{g}_j$

**底层实现代码：**

文件：[`queso/utilities/polynomial_utilities.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/utilities/polynomial_utilities.hpp#L90-L131)

```cpp
// queso/utilities/polynomial_utilities.hpp，第 90–131 行
// 返回定义在区间 (a,b) 上的第 order 阶 Legendre 多项式的不定积分
inline double f_x_int(double x, IndexType order, double a, double b) noexcept {
    switch(order) {
        case 0: {
            // ∫L_0 dx = x
            return x;
        }
        case 1: {
            // ∫L_1 dx = -(a+b-2x)^2 / [4(a-b)]
            return -power((a + b - 2.0*x),2)/(4.0*(a - b));
        }
        case 2: {
            // ∫L_2 dx = -x/2 - (a+b-2x)^3 / [4(a-b)^2]
            return -x/2.0 - power((a + b - 2.0*x),3)/(4.0*power((a - b),2));
        }
        case 3: {
            // ∫L_3 dx = 3(a+b-2x)^2/[8(a-b)] - 5(a+b-2x)^4/[16(a-b)^3]
            return (3.0*power((a + b - 2.0*x),2))/(8.0*(a - b))
                 - (5*power((a + b - 2.0*x),4))/(16*power((a - b),3));
        }
        case 4: {
            // ∫L_4 dx = 3x/8 + 5(a+b-2x)^3/[8(a-b)^2]
            //           - 7(a+b-2x)^5/[16(a-b)^4]
            return (3.0*x)/8.0
                 + (5.0*power((a + b - 2.0*x),3))/(8*power((a - b),2))
                 - (7.0*power((a + b - 2*x),5))/(16*power((a - b),4));
        }
        case 5: {
            // ∫L_5 dx = 35(a+b-2x)^4/[32(a-b)^3]
            //           - 15(a+b-2x)^2/[32(a-b)]
            //           - 21(a+b-2x)^6/[32(a-b)^5]
            return (35*power((a + b - 2*x),4))/(32*power((a - b),3))
                 - (15*power((a + b - 2*x),2))/(32*(a - b))
                 - (21*power((a + b - 2*x),6))/(32*power((a - b),5));
        }
        case 6: {
            return (63*power((a + b - 2*x),5))/(32*power((a - b),4))
                 - (35*power((a + b - 2*x),3))/(32*power((a - b),2))
                 - (5*x)/16
                 - (33*power((a + b - 2*x),7))/(32*power((a - b),6));
        }
        case 7: {
            return (35.0*power((a + b - 2*x),2))/(64*(a - b))
                 - (315*power((a + b - 2*x),4))/(128.0*power((a - b),3))
                 + (231.0*power((a + b - 2*x),6))/(64.0*power((a - b),5))
                 - (429.0*power((a + b - 2*x),8))/(256.0*power((a - b),7));
        }
        case 8: {
            return (35.0*x)/128.0
                 + (105.0*power((a + b - 2*x),3))/(64.0*power((a - b),2))
                 - (693.0*power((a + b - 2*x),5))/(128.0*power((a - b),4))
                 + (429.0*power((a + b - 2*x),7))/(64.0*power((a - b),6))
                 - (715.0*power((a + b - 2*x),9))/(256.0*power((a - b),8));
        }
        default:
            return 0.0;
    };
}
```

**公式 (6) 中 $\pmb{g}_j$ 与代码中三分量的对应关系**（摘自 `ComputeConstantTerms`，第 273–275 行）：

```cpp
// queso/quadrature/trimmed_element.hpp，第 272–275 行
// g_j(x) 的三个分量由 f_x_int（抗导数）和 f_x（函数值）组合而成
value[0] = f_x_int_x[i_x] * f_x_y[i_y]    * f_x_z[i_z];  // g_j 的 x 分量
value[1] = f_x_x[i_x]     * f_x_int_y[i_y] * f_x_z[i_z];  // g_j 的 y 分量
value[2] = f_x_x[i_x]     * f_x_y[i_y]    * f_x_int_z[i_z]; // g_j 的 z 分量
// 因子 1/3 在 weight = 1.0/3.0 * point_it->Weight() 中体现
```

**对应公式 (6)：**

$$
\pmb{g}_j(\pmb{x}) = \frac{1}{3}
\begin{bmatrix}
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}x \\[6pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}y \\[6pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}z
\end{bmatrix}
\quad (6)
$$

**解读：** `Polynomial::f_x_int(x, order, a, b)` 计算 $\int L_r(x)\,\mathrm{d}x$，即 Legendre 多项式在区间 $[a,b]$ 上的不定积分。公式 (6) 中的三维矢量场 $\pmb{g}_j$ 通过以下方式在代码中实现：
- `value[0] = f_x_int_x[i_x] * f_x_y[i_y] * f_x_z[i_z]` 对应 $g_j^{(1)} = \int L_r(x)\,\mathrm{d}x \cdot L_s(y) \cdot L_t(z)$，
- `value[1] = f_x_x[i_x] * f_x_int_y[i_y] * f_x_z[i_z]` 对应 $g_j^{(2)} = L_r(x)\cdot\int L_s(y)\,\mathrm{d}y\cdot L_t(z)$，
- `value[2] = f_x_x[i_x] * f_x_y[i_y] * f_x_int_z[i_z]` 对应 $g_j^{(3)} = L_r(x)\cdot L_s(y)\cdot\int L_t(z)\,\mathrm{d}z$。

公式中的 $1/3$ 因子通过 `weight = 1.0/3.0 * point_it->Weight()` 在权重中全局引入。注意，对于张量积基函数 $f_j(\pmb{x}) = L_r(x)L_s(y)L_t(z)$，其各方向抗导数分别只涉及对应方向的不定积分，其余方向保持函数值不变，这与公式 (6) 完全一致（因为 $\nabla\cdot\pmb{g}_j = f_j$ 成立）。

---

## 第 3.3 节：对边界表示（B-Rep）的应用

### 公式 (7)：三角面片上的边界曲面积分之和

**底层实现代码（1）— 三角面片裁切与网格生成：**

文件：[`queso/embedding/clipper.cpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/embedding/clipper.cpp#L25-L86)

```cpp
// queso/embedding/clipper.cpp，第 25–86 行
// 将三角面片 (rV1, rV2, rV3) 裁切到包围盒 [rLowerBound, rUpperBound] 内
// 生成裁切后的多边形（对应 Eq.(7) 中的面片 f ∈ Gamma_c 的几何信息）
Unique<PolygonType> Clipper::ClipTriangle(
        const PointType& rV1, const PointType& rV2, const PointType& rV3,
        const PointType& rNormal,
        const PointType& rLowerBound, const PointType& rUpperBound) {

    Unique<PolygonType> p_current_poly = MakeUnique<PolygonType>(rNormal);
    Unique<PolygonType> p_prev_poly    = MakeUnique<PolygonType>(rNormal);

    // 如果三角形所有顶点均在某平面上，返回 nullptr
    for( IndexType plane_index = 0; plane_index < 6; ++plane_index){
        const double plane_position = (plane_index%2UL)
            ? rUpperBound[plane_index/2UL] : rLowerBound[plane_index/2UL];
        Plane plane(plane_index, plane_position);
        auto loc_v1 = ClassifyPointToPlane(rV1, plane, 10.0*ZEROTOL);
        auto loc_v2 = ClassifyPointToPlane(rV2, plane, 10.0*ZEROTOL);
        auto loc_v3 = ClassifyPointToPlane(rV3, plane, 10.0*ZEROTOL);
        IndexType count_on_plane =
            static_cast<IndexType>(loc_v1 == ON_PLANE) +
            static_cast<IndexType>(loc_v2 == ON_PLANE) +
            static_cast<IndexType>(loc_v3 == ON_PLANE);
        if( count_on_plane == 3 ){
            return nullptr;
        }
    }

    // 计算三角形的包围盒
    const PointType x_values{rV1[0], rV2[0], rV3[0]};
    const PointType y_values{rV1[1], rV2[1], rV3[1]};
    const PointType z_values{rV1[2], rV2[2], rV3[2]};
    auto x_min_max = std::minmax_element(x_values.begin(), x_values.end());
    auto y_min_max = std::minmax_element(y_values.begin(), y_values.end());
    auto z_min_max = std::minmax_element(z_values.begin(), z_values.end());
    PointType min_tri{*x_min_max.first,  *y_min_max.first,  *z_min_max.first};
    PointType max_tri{*x_min_max.second, *y_min_max.second, *z_min_max.second};

    // 将三角形顶点加入多边形
    p_current_poly->AddVertex(rV1);
    p_current_poly->AddVertex(rV2);
    p_current_poly->AddVertex(rV3);

    // 依次对 6 个包围盒平面进行 Sutherland-Hodgman 裁切
    for(IndexType dimension = 0; dimension < 3; ++dimension) {
        if(max_tri[dimension] > rLowerBound[dimension]) {
            std::swap(p_prev_poly, p_current_poly);
            Plane plane(2*dimension+0, rLowerBound[dimension]);
            ClipPolygonByPlane(p_prev_poly.get(), p_current_poly.get(), plane);
        }
        if(min_tri[dimension] < rUpperBound[dimension]) {
            std::swap(p_prev_poly, p_current_poly);
            Plane plane(2*dimension+1, rUpperBound[dimension]);
            ClipPolygonByPlane(p_prev_poly.get(), p_current_poly.get(), plane);
        }
    }
    return p_current_poly;
}
```

**底层实现代码（2）— 裁切网格的整体组装：**

文件：[`queso/embedding/brep_operator.cpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/embedding/brep_operator.cpp#L221-L243)

```cpp
// queso/embedding/brep_operator.cpp，第 221–243 行
// 将 STL 网格中所有与 [rLowerBound, rUpperBound] 相交的三角面片
// 裁切后组装为一个新的三角网格（Gamma_{c,c} = B_cut ∩ Gamma）
Unique<TriangleMeshInterface> BRepOperator::pClipTriangleMesh(
        const PointType& rLowerBound, const PointType& rUpperBound) const {

    const double snap_tolerance =
        0.1*RelativeSnapTolerance(rUpperBound, rLowerBound);
    // 第一步：通过 AABB 树快速找出可能与包围盒相交的三角面片 ID
    auto p_intersected_triangle_ids =
        mGeometryQuery.GetIntersectedTriangleIds(
            rLowerBound, rUpperBound, snap_tolerance);
    auto p_triangle_mesh = MakeUnique<TriangleMesh>();
    p_triangle_mesh->Reserve(2 * p_intersected_triangle_ids->size());
    p_triangle_mesh->ReserveEdgesOnPlane(p_intersected_triangle_ids->size());

    // 第二步：对每个候选三角面片执行精确裁切，保留裁切后的多边形
    // 对应 Eq.(7) 中的 "对每个 f ∈ Gamma_c 求积分" 的几何准备
    for( auto triangle_id : (*p_intersected_triangle_ids) ){
        const auto& P1     = mTriangleMesh.P1(triangle_id);
        const auto& P2     = mTriangleMesh.P2(triangle_id);
        const auto& P3     = mTriangleMesh.P3(triangle_id);
        const auto& normal = mTriangleMesh.Normal(triangle_id);
        auto p_polygon =
            Clipper::ClipTriangle(P1, P2, P3, normal,
                                  rLowerBound, rUpperBound);
        if( p_polygon ){
            // 将裁切后的多边形三角剖分，加入裁切网格
            p_polygon->AddToTriangleMesh(*p_triangle_mesh.get());
        }
    }
    if( p_triangle_mesh->NumOfTriangles() > 0)
        p_triangle_mesh->Check();
    return p_triangle_mesh;
}
```

**底层实现代码（3）— 曲面积分的逐面片求和：**

文件：[`queso/quadrature/trimmed_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/trimmed_element.hpp#L239-L283)

```cpp
// queso/quadrature/trimmed_element.hpp，第 239–283 行（摘自 ComputeConstantTerms 边界版本）
// 遍历 Gamma_c 上所有边界积分点，对每个三角面片 f 累加积分贡献
// 实现 Eq.(7)：∫_Ω_c f_j dΩ = Σ_{f ∈ Gamma_c} ∫_f g_j(x)·n(x) dΓ

const auto begin_points_it_ptr = pBoundaryIPs->begin();
for( IndexType i = 0; i < pBoundaryIPs->size(); ++i ){
    auto point_it = (begin_points_it_ptr + static_cast<std::ptrdiff_t>(i));
    const auto& normal = point_it->Normal();  // 面片法向量 n(x)
    PointType point = point_it->data();

    // ... （预计算 f_x 和 f_x_int，见公式 (6) 的代码解读）

    row_index = 0;
    const double weight = 1.0/3.0 * point_it->Weight();  // 1/3 * w_quad
    for( IndexType i_x = 0; i_x <= order_u*ffactor; ++i_x){
        for( IndexType i_y = 0; i_y <= order_v*ffactor; ++i_y ){
            for( IndexType i_z = 0; i_z <= order_w*ffactor; ++i_z){
                PointType value;
                // g_j(x) 的三个分量（对应 Eq.(6)）
                value[0] = f_x_int_x[i_x] * f_x_y[i_y]     * f_x_z[i_z];
                value[1] = f_x_x[i_x]     * f_x_int_y[i_y]  * f_x_z[i_z];
                value[2] = f_x_x[i_x]     * f_x_y[i_y]     * f_x_int_z[i_z];

                // g_j(x) · n(x)，即散度定理曲面积分的被积项
                double integrand = normal[0]*value[0]
                                 + normal[1]*value[1]
                                 + normal[2]*value[2];
                // 在所有面片上累加，实现 Σ_{f ∈ Gamma_c} ∫_f (...) dΓ
                rConstantTerms[row_index] += integrand * weight;
                row_index++;
            }
        }
    }
}
```

**对应公式 (7)：**

$$
\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega
= \sum_{f\in\Gamma_{\mathrm{c}}} \int_{f} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
\quad (7)
$$

**解读：** 公式 (7) 是公式 (5) 的离散化实现。其对应关系如下：

1. **几何准备**（生成 $\Gamma_c$ 上的三角面片集合 $\{f\}$）：
   - `BRepOperator::pClipTriangleMesh` 通过 AABB 树查询与包围盒 $B_{\mathrm{cut}}$ 相交的 STL 三角面片，再调用 `Clipper::ClipTriangle` 对每个面片进行 Sutherland-Hodgman 多面体裁切，生成关于 $\Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}}\cap\Gamma$ 的封闭三角网格 $\Gamma_c = \partial(B_{\mathrm{cut}}\cap\Omega)$。

2. **求积分（公式 (7) 的 $\sum_f\int_f$）**：
   - `ComputeConstantTerms`（边界积分点版本）中，外层 `for (IndexType i = 0; i < pBoundaryIPs->size(); ++i)` 循环遍历 $\Gamma_c$ 上所有三角面片的所有数值积分点，通过累加 `rConstantTerms[row_index] += integrand * weight` 实现求和 $\sum_{f\in\Gamma_c}\int_f\pmb{g}_j\cdot\pmb{n}\,\mathrm{d}\Gamma$。

3. **被积项 $\pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})$**：
   - `value[0..2]` 对应 $\pmb{g}_j(\pmb{x})$ 的三个分量（分别调用 `Polynomial::f_x_int` 和 `Polynomial::f_x`，含 $1/3$ 因子）。
   - `integrand = normal[0]*value[0] + ... + normal[2]*value[2]` 实现点积 $\pmb{g}_j\cdot\pmb{n}$。

---

## 附：点消除算法（Point Elimination）

公式 (3) 需要借助点消除（Point Elimination）算法从过定初始点集中筛选出最优点集，以保证 $n_q \le (p+1)^d$。

**底层实现代码：**

文件：[`queso/quadrature/trimmed_element.hpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/quadrature/trimmed_element.hpp#L352-L457)

```cpp
// queso/quadrature/trimmed_element.hpp，第 352–457 行
// 迭代点消除算法：通过反复求解矩拟合方程并移除权重最小的点，
// 最终得到 n_q ≤ (p+1)^3 个具有正权重的积分点
static double PointElimination(const VectorType& rConstantTerms,
                                IntegrationPointVectorType& rIntegrationPoint,
                                ElementType& rElement,
                                const Vector3i& rIntegrationOrder,
                                double Residual) {
    const SizeType ffactor = 1;
    const SizeType order_u = rIntegrationOrder[0];
    const SizeType order_v = rIntegrationOrder[1];
    const SizeType order_w = rIntegrationOrder[2];
    // 基函数总数 m = (p_u+1)(p_v+1)(p_w+1)
    const IndexType number_of_functions =
        (order_u*ffactor + 1) * (order_v*ffactor+1) * (order_w*ffactor + 1);
    // 最小保留点数 = p_u * p_v * p_w
    const IndexType min_number_of_points = order_u * order_v * order_w;

    const double targeted_residual = Residual;
    double global_residual = MIND;
    double prev_residual = 0.0;
    const SizeType maximum_iteration = 1000UL;
    SizeType number_iterations = 0UL;
    bool point_is_eliminated = false;
    IntegrationPointVectorType prev_solution{};

    // 迭代直到残差大于目标残差或达到最大迭代次数
    while( point_is_eliminated ||
           (global_residual < targeted_residual &&
            number_iterations < maximum_iteration) ) {
        point_is_eliminated = false;
        // 每次迭代求解矩拟合方程（Eq.(3)）
        global_residual = MomentFitting(rConstantTerms, rIntegrationPoint,
                                         rElement, rIntegrationOrder);
        if( number_iterations == 0UL ){
            // 第一次迭代：按权重排序，仅保留前 number_of_functions 个点
            std::sort(rIntegrationPoint.begin(), rIntegrationPoint.end(),
                [](const IntegrationPointType& a, const IntegrationPointType& b)
                { return a.Weight() > b.Weight(); });
            rIntegrationPoint.erase(
                rIntegrationPoint.begin() +
                static_cast<std::ptrdiff_t>(number_of_functions),
                rIntegrationPoint.end());
            // 移除权重为零的点
            rIntegrationPoint.erase(
                std::remove_if(rIntegrationPoint.begin(),
                               rIntegrationPoint.end(),
                    [](const IntegrationPointType& p) {
                        return p.Weight() < ZEROTOL; }),
                rIntegrationPoint.end());
            if( rIntegrationPoint.size() == 0 ) break;
            point_is_eliminated = true;
        } else if( global_residual < targeted_residual &&
                   number_iterations < maximum_iteration ) {
            // 保存当前解
            prev_solution.clear();
            prev_solution.insert(prev_solution.begin(),
                rIntegrationPoint.begin(), rIntegrationPoint.end());
            prev_residual = global_residual;

            // 找最小/最大权重
            auto min_value_it = rIntegrationPoint.begin();
            double min_value = MAXD;
            double max_value = LOWESTD;
            const auto begin_it = rIntegrationPoint.begin();
            for(IndexType k = 0; k < rIntegrationPoint.size(); k++){
                auto it = begin_it + static_cast<std::ptrdiff_t>(k);
                if( it->Weight() < min_value ) {
                    min_value_it = it;
                    min_value = it->Weight();
                }
                if( it->Weight() > max_value ) max_value = it->Weight();
            }
            // 移除权重 < 1e-8 * max_value 的点（但至少保留 min_number_of_points）
            SizeType counter = 0;
            for(IndexType k = 0; k < rIntegrationPoint.size(); k++){
                auto it = begin_it + static_cast<std::ptrdiff_t>(k);
                if( it->Weight() < 1e-8*max_value &&
                    rIntegrationPoint.size() > min_number_of_points ){
                    rIntegrationPoint.erase(it);
                    point_is_eliminated = true;
                    counter++;
                }
            }
            if( counter == 0 &&
                rIntegrationPoint.size() > min_number_of_points ) {
                rIntegrationPoint.erase(min_value_it);
                point_is_eliminated = true;
            }
            if( rIntegrationPoint.size() <= min_number_of_points )
                number_iterations = maximum_iteration + 10;
        }
        number_iterations++;
    }
    // 将最优解写入 rElement
    auto& reduced_points = rElement.GetIntegrationPoints();
    if( (global_residual >= targeted_residual && prev_solution.size() > 0) ||
        number_iterations > maximum_iteration ) {
        reduced_points.insert(reduced_points.begin(),
            prev_solution.begin(), prev_solution.end());
        reduced_points.erase(
            std::remove_if(reduced_points.begin(), reduced_points.end(),
                [](const IntegrationPointType& p) {
                    return p.Weight() < ZEROTOL; }),
            reduced_points.end());
        return prev_residual;
    } else {
        reduced_points.insert(reduced_points.begin(),
            rIntegrationPoint.begin(), rIntegrationPoint.end());
        reduced_points.erase(
            std::remove_if(reduced_points.begin(), reduced_points.end(),
                [](const IntegrationPointType& p) {
                    return p.Weight() < ZEROTOL; }),
            reduced_points.end());
        return global_residual;
    }
}
```

**说明：** 点消除算法（Point Elimination）配合公式 (3) 的求解（`MomentFitting`），实现了文中提到的 $n_q \le (p+1)^d$ 结果（$p$ 为多项式阶次，$d = 3$ 为维度）。算法通过迭代移除权重最小的积分点（每次剔除权重 $< 10^{-8}\,\omega_{\max}$ 的点，若无满足条件的点则移除最小权重点），同时调用 `NNLS::solve` 重新求解矩拟合方程，直至残差 $\|\mathbf{A}\boldsymbol{\omega} - \mathbf{b}\|_{L^2}/\|\mathbf{b}\|_{L^2}$ 超过目标阈值。在整个过程中，NNLS 求解器保证所有权重 $\omega_i \ge 0$，且积分点位于材料域内。

---

## 附：洪水填充元素分类（Flood Fill）

文中描述的背景元素分类（$B_{\mathrm{in}}$、$B_{\mathrm{out}}$、$B_{\mathrm{cut}}$）对应公式 (1) 中活跃集 $\mathcal{P} = \mathbf{P}_{\mathrm{bg}}\setminus\mathbf{P}_{\mathrm{out}}$ 的计算。

**底层实现代码：**

文件：[`queso/embedding/flood_fill.cpp`](https://github.com/xingzhiyuan1229/QuESo/blob/copilot/extract-formulas-and-code/queso/embedding/flood_fill.cpp#L39-L118)

```cpp
// queso/embedding/flood_fill.cpp，第 39–118 行
// 基于洪水填充的并行化元素分类，将背景网格元素标记为
// inside（内部）、outside（外部）或 trimmed（裁切）
Unique<StatusVectorType> FloodFill::ClassifyElements(
        GroupSetVectorType& rGroupsOutput) const {
    const IndexType total_num_elements =
        mNumberOfElements[0]*mNumberOfElements[1]*mNumberOfElements[2];

    // 初始化所有元素为 outside
    StatusVectorType states(total_num_elements);
    std::fill(states.begin(), states.end(), IntersectionState::outside);

    // 选择划分方向（沿最长维度划分以利于并行）
    IndexType max_num_elements_per_dir = std::max<IndexType>(
        std::max<IndexType>(mNumberOfElements[0], mNumberOfElements[1]),
        mNumberOfElements[2]);
    IndexType partition_index = 0;
    std::array<IndexType, 2> directions;
    if( mNumberOfElements[0] == max_num_elements_per_dir ){
        partition_index = 0; directions[0] = 0; directions[1] = 1;
    } else if (mNumberOfElements[1] == max_num_elements_per_dir) {
        partition_index = 1; directions[0] = 2; directions[1] = 3;
    } else {
        partition_index = 2; directions[0] = 4; directions[1] = 5;
    }

    // 计算并行分区
    std::vector<PartitionBoxType> partitions;
    IndexType num_threads = 1;
    #pragma omp parallel
    { num_threads = static_cast<IndexType>(omp_get_num_threads()); }

    const IndexType partition_size = std::max<IndexType>(
        static_cast<IndexType>(std::ceil(
            static_cast<double>(mNumberOfElements[partition_index]) /
            static_cast<double>(num_threads))), 1);

    Vector3i partition_lower_bound{0, 0, 0};
    Vector3i partition_upper_bound{mNumberOfElements[0]-1,
                                   mNumberOfElements[1]-1,
                                   mNumberOfElements[2]-1};
    for( IndexType i = 0; i < num_threads; ++i ){
        if( partition_size*i < mNumberOfElements[partition_index] ){
            partition_lower_bound[partition_index] = partition_size*i;
            partition_upper_bound[partition_index] =
                std::min<IndexType>(partition_size*(i+1),
                    mNumberOfElements[partition_index])-1;
            partitions.push_back(
                std::make_pair(partition_lower_bound, partition_upper_bound));
        }
    }

    // 并行洪水填充（每个分区独立执行）
    GroupSetVectorType groups;
    PartitionedFill(groups, partitions, states);

    // 合并各分区的组
    MergeGroups(groups, rGroupsOutput, partition_index, partitions);

    // 根据 inside_count 标记元素状态
    for( auto& r_group : rGroupsOutput ){
        int inside_count = std::get<2>(r_group);
        auto state = (inside_count > 0) ?
            IntersectionState::inside : IntersectionState::outside;
        for( auto group_it = std::get<1>(r_group).begin();
             group_it != std::get<1>(r_group).end(); ++group_it ){
            states[*group_it] = state;
        }
    }

    return MakeUnique<StatusVectorType>(states);
}
```

**对应论文活跃集定义：**

$$
\mathcal{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}}
$$

**解读：** `FloodFill::ClassifyElements` 实现了论文中基于洪水填充的鲁棒元素分类算法，将背景网格中所有元素分为三类：
- `IntersectionState::trimmed`（裁切元素 $B_{\mathrm{cut}}$）：与 STL 曲面相交的元素；
- `IntersectionState::inside`（内部元素 $B_{\mathrm{in}}$）：完全位于 $\Omega$ 内部的元素；
- `IntersectionState::outside`（外部元素 $B_{\mathrm{out}}$）：完全位于 $\Omega$ 外部的元素。

分类结果用于确定 $\mathcal{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}}$（活跃积分集），从而决定哪些元素需要参与公式 (1) 的积分计算。为支持并行执行，算法沿最长维度将背景网格划分为若干条状分区（`PartitionedFill`），各分区独立执行洪水填充，最后通过 `MergeGroups` 合并跨分区的连通组，并用 `inside_count`（正负计数）判断每个连通组的内外属性。

---

*文档生成日期：2026-04-04*
