# 论文总结报告

**论文标题：** Robust numerical integration of embedded solids described in boundary representation

**作者：** Manuel Messmer, Stefan Kollmannsberger, Roland Wüchner, Kai-Uwe Bletzinger

**发表期刊：** Computer Methods in Applied Mechanics and Engineering（CMAME）

**开源代码：** https://github.com/manuelmessmer/QuESo

---

## 一、作者研究的问题

### 1.1 背景与核心挑战

嵌入式有限元方法（Embedded FEM）及浸没边界方法（Immersed Boundary Method）是计算力学中处理复杂几何体的重要工具。其核心思想是将物理域 [ \Omega \subset \mathbb{R}^3 ] 嵌入一个简单的规则背景网格（background mesh）中求解 PDE，而不需要生成贴合边界的有限元网格，从根本上绕开了传统网格生成的瓶颈。

这类方法面临的**主要技术难题**是：由于背景网格的边界与几何体的边界不重合，网格中会不可避免地出现被几何边界穿越的**裁切单元（cut elements）**。在裁切单元 [ B_{\mathrm{cut}} ] 上，积分域 [ B_{\mathrm{cut}} \cap \Omega ] 的形状任意复杂，标准 Gauss 求积分规则无法直接使用，必须为每个裁切单元专门构造高效、精确的积分规则。

本文所针对的几何输入格式是工业界最广泛使用的 **边界表示（Boundary Representation, B-Rep）模型**，特别是 **STL（STereoLithography）三角网格**——它只描述几何体的边界表面，不包含体积信息，可以从任何 CAD 软件直接导出。

### 1.2 已有方法的不足

现有方法主要分为两类：

1. **空间细分类（八叉树等）**：通过递归细分裁切单元来逼近复杂积分域，几何近似阶次低，积分点数量庞大，效率低下。
2. **矩拟合（Moment Fitting）类**：通过求解矩拟合方程，以少量优化积分点实现高精度积分。矩拟合方法本身已非常高效，但其**关键瓶颈**在于右端项（矩）的计算——传统方法需要对三维域进行空间细化或调用 CGAL 等通用几何库做布尔操作，这些操作：
   - 对每个裁切单元重复执行，耗时极大；
   - 通常要求输入几何为**水密（watertight）网格**，而工程实践中的 STL 模型常含有缺陷（gaps, duplicates, wrong orientations 等）；
   - 通用几何库不专门针对"一个几何体与百万量级背景单元求交"的场景优化。

### 1.3 本文要解决的具体问题

> **核心问题**：如何针对 STL 格式的 B-Rep 几何体，高效、鲁棒地计算矩拟合方程右端项（矩），同时对有缺陷的 STL 网格也能给出可靠结果？

具体地，本文需要解决以下三个子问题：

1. 如何快速为每个裁切单元 [ B_{\mathrm{cut}} ] 构造封闭的曲面参数化 [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ]，以便应用散度定理将体积矩转化为曲面积分？
2. 如何对含有大量缺陷的 STL 网格，仍然鲁棒地完成背景网格单元分类（内部/外部/裁切）？
3. 如何在并行计算环境下高效地执行上述流程？

---

## 二、作者所做的工作及创新点

### 2.1 创新点一：面向矩拟合的专用裁切与交叉算法

**核心思路：** 不追求裁切域的完整体积表示（如多面体），而是只计算散度定理所需的**最低限度信息**——每个裁切单元的封闭曲面网格 [ \Gamma_{\mathrm{c}} ]。

**具体做法：**
- 将输入 STL 中与单元 [ B ] 相交的三角面片用 Sutherland-Hodgeman 类算法裁切到单元包围盒（AABB）内，得到开放曲面 [ \Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma ]；
- 在单元的每个轴对齐平面上，将 [ \Gamma_{\mathrm{c,c}} ] 与边界的交线段用多边形三角化填充开放面片，得到封闭曲面 [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ]。

**创新性在于：** 放弃了传统几何算法对网格质量的严苛要求。由于 [ \Gamma_{\mathrm{c}} ] 仅用于积分，高长宽比、悬挂节点、重复顶点、零面积三角形等质量问题完全不影响积分精度。唯一的质量准则是通过散度定理对常数函数（即体积）进行验证：

$$\text{Vol}(\Omega_\mathrm{c}) = \int_{\Gamma_\mathrm{c}} \frac{1}{3}(x \cdot n_x + y \cdot n_y + z \cdot n_z)\,\mathrm{d}\Gamma$$

若该值与理论值偏差超过阈值，则放弃该单元或标记为有问题。这个单一质量准则极大简化了算法设计，显著提升了速度。

### 2.2 创新点二：支持有缺陷几何的鲁棒泛洪填充单元分类方案

**核心思路：** 在泛洪填充（Flood Fill）算法基础上，引入**任意种子元素出发**和**局部光线追踪投票**机制，同时支持含空腔（cavity）的固体几何。

**具体做法：**
- 不依赖几何体的水密性来判断内外；
- 用射线方向与裁切面片法向量的点积进行局部内外判断（多数票决）；
- 将背景网格划分为若干条带（stripe partitions），每个条带独立进行泛洪填充，最后合并——这使算法能够在多线程/OpenMP 环境下并行执行。

### 2.3 创新点三：高阶曲面积分——近机器精度计算矩

**核心思路：** 在封闭曲面 [ \Gamma_{\mathrm{c}} ] 的每个三角面片上，用高阶二维 Gauss 积分规则计算散度定理右端的曲面积分。由于 Legendre 多项式的反导数（antiderivative）解析已知，矢量场 [ \pmb{g}_j ] 的计算无需任何额外数值积分，从而整个矩的计算可以达到**接近机器精度**。

---

## 三、创新点在论文中的体现

### 3.1 矩拟合精度问题的直接回应（Section 3）

论文明确指出，无论矩拟合方程（公式 3）采用何种求解策略，最终积分规则的精度始终受到矩（右端项 [ b_j = \int_{\Omega_\mathrm{c}} f_j\,\mathrm{d}\Omega ]）计算质量的制约。因此作者的工作聚焦于矩的精确计算，而不是提出新的矩拟合求解器。

公式 (5) 引入散度定理作为转换桥梁，公式 (6) 给出解析已知的抗导数矢量场，公式 (7) 将连续曲面积分离散为三角面片上的求和，形成完整的计算链。

### 3.2 创新算法的伪代码描述（Section 5）

**Algorithm 1**（论文第 5 节）系统描述了整个嵌入流程：
1. 基于泛洪填充对背景网格所有单元分类；
2. 对每个裁切单元：调用裁切算法获得 [ \Gamma_{\mathrm{c,c}} ]，再调用三角化算法封闭边界得到 [ \Gamma_{\mathrm{c}} ]；
3. 对 [ \Gamma_{\mathrm{c}} ] 上的三角面片计算矩（右端项），组装矩拟合矩阵，用 NNLS + 点消除求解积分权重。

论文还通过与传统八叉树方法的对比（Section 6）、对 Thingi10K 数据库 4948 个 STL 的鲁棒性测试，以及直接对有缺陷几何进行 FE 分析等实验，定量验证了上述创新点的效果。

### 3.3 仅用单一质量准则的设计哲学

论文强调，经典网格质量准则（高长宽比、小角度等）在本方法中完全不适用。作者通过理论分析说明：只要封闭曲面 [ \Gamma_{\mathrm{c}} ] 满足散度定理的体积验证准则，积分即可达到高精度，而这个准则的计算成本极低。

---

## 四、创新点在代码中的实现

### 4.1 整体代码架构

```
QuESo（C++ 框架）
├── embedded_model.cpp/h      ← 主入口：ComputeVolume(), ComputeCondition()
├── embedding/
│   ├── brep_operator.cpp/h   ← B-Rep 操作核心：分类、裁切、获取裁切域
│   ├── clipper.cpp/h         ← 三角面片裁切（Sutherland-Hodgeman AABB变种）
│   ├── trimmed_domain.cpp/h  ← 裁切域：存储封闭曲面 Γ_c
│   ├── trimmed_domain_on_plane.cpp/h ← 平面三角化（封闭开放面片）
│   ├── flood_fill.cpp/h      ← 泛洪填充单元分类
│   ├── octree.cpp/h          ← 八叉树（用于初始点分布）
│   └── aabb_tree.cpp/h       ← AABB 树（快速几何查询）
├── quadrature/
│   ├── trimmed_element.hpp   ← 裁切单元矩拟合求积（核心数学）
│   ├── single_element.hpp    ← 非裁切单元标准 Gauss 规则
│   └── multiple_elements.hpp ← GGQ 广义 Gauss 规则（多单元联合）
├── solvers/
│   └── nnls.h                ← NNLS 求解器（保证权重非负）
└── utilities/
    └── polynomial_utilities.hpp ← Legendre 多项式及其反导数（解析实现）
```

### 4.2 主流程：`EmbeddedModel::ComputeVolume()`（`embedded_model.cpp:28`）

这是整个框架的顶层入口，代码直接对应论文 Algorithm 1：

```cpp
// Step 1: 构造 BRepOperator（建立 AABB 树加速几何查询）
BRepOperator brep_operator(rTriangleMesh);

// Step 2: 用泛洪填充对所有元素分类（内/外/裁切）→ 创新点2
Unique<BRepOperator::StatusVectorType> p_classifications
    = brep_operator.pGetElementClassifications(r_settings);

// Step 3: 对每个活跃单元（并行 OpenMP）
#pragma omp parallel for
for( int for_index = 0; ...) {
    if( status == IntersectionState::trimmed ) {
        // Step 3a: 计算裁切域 Γ_c（裁切 + 封闭曲面）→ 创新点1
        auto p_trimmed_domain = brep_operator.pGetTrimmedDomain(...);
        // Step 3b: 矩拟合求积（计算矩 + NNLS 点消除）→ 创新点3
        QuadratureTrimmedElement<ElementType>::AssembleIPs(
            *p_new_element, polynomial_order, moment_fitting_residual);
    } else { // inside
        // 非裁切单元：标准 Gauss 规则
        QuadratureSingleElement<ElementType>::AssembleIPs(...);
    }
}
```

### 4.3 裁切与封闭曲面构造：`Clipper::ClipTriangle()` + `TrimmedDomain`

**`Clipper::ClipTriangle()`**（`clipper.cpp`）实现了三角面片的 AABB 裁切，这是 Sutherland-Hodgeman 算法针对轴对齐平面的专门优化：

```cpp
// 依次用 AABB 的 6 个面（-x, +x, -y, +y, -z, +z）裁切多边形
static Unique<PolygonType> ClipTriangle(
    const PointType& rV1, const PointType& rV2, const PointType& rV3,
    const PointType& rNormal,
    const PointType& rLowerBound, const PointType& rUpperBound);
```

每个面用 `ClipPolygonByPlane()` 函数按轴对齐平面裁切，仅保留平面正侧的部分，最终输出为最多 9 个顶点的多边形（原三角形与 6 个 AABB 面的最大交集顶点数）。

**封闭曲面生成**：`TrimmedDomainOnPlane` 负责在 AABB 的每个轴对齐面上，将 [ \Gamma_{\mathrm{c,c}} ] 的边界线段收集并三角化为封闭面片，最终将 [ \Gamma_{\mathrm{c,c}} ]（来自 STL 的裁切面片）与这些平面面片合并，得到封闭的 [ \Gamma_{\mathrm{c}} ]。

### 4.4 泛洪填充分类：`FloodFill::ClassifyElements()`（`flood_fill.cpp`）

```cpp
// 1. 将背景网格按 n_threads 切分为条带（stripe partitions），
//    沿最大元素数量方向进行分区
// 2. 每个分区独立运行 SinglePartitionFill()，标记内/外/裁切
// 3. MergeGroups() 合并相邻分区的结果
//    —— 通过局部光线追踪（GetIsInsideCount）解决边界处的歧义
Unique<StatusVectorType> ClassifyElements() const;
```

**局部光线追踪投票**（对应论文的鲁棒分类方案）：在分区边界相邻的两个单元中，从内侧单元向裁切单元的三角面片发射若干射线，利用三角面片法向量的点积判断内外，多数票决给出最终分类。这就是论文中**"任意种子元素出发"**的具体实现。

---

## 五、论文大致流程文字叙述

### 阶段 0：输入准备

用户提供两个输入：
1. **STL 几何文件**（[ \Gamma = \partial\Omega ]，有向三角网格，可包含缺陷）；
2. **背景网格参数**（包围盒范围、各方向元素数量、多项式阶次、精度要求等）。

QuESo 内部构建 AABB 树以加速 STL 的几何查询。

### 阶段 1：单元分类（FloodFill）

对背景网格 [ \mathbf{P}_{\mathrm{bg}} ] 中的每个六面体单元 [ B ]，判断其类型：
- **外部单元** [ B_{\mathrm{out}} ]：完全在 [ \Omega ] 之外，直接跳过；
- **内部单元** [ B_{\mathrm{in}} ]：完全在 [ \Omega ] 之内，用标准 Gauss 规则处理；
- **裁切单元** [ B_{\mathrm{cut}} ]：被 [ \Gamma ] 穿越，需要专门处理。

分类通过泛洪填充算法实现：首先标记所有与 [ \Gamma ] 相交的单元为裁切，然后从边界种子出发进行洪水填充，用局部光线追踪投票确定各连通组的内外属性。该过程支持 OpenMP 并行，按条带分区后独立填充并合并。

### 阶段 2：裁切域几何构造（BRepOperator + Clipper + TrimmedDomainOnPlane）

对每个裁切单元 [ B_{\mathrm{cut}} ]，执行以下几何操作：

**步骤 2a：裁切 STL**

将 STL 中与当前单元 AABB 有交叉的所有三角面片，用 `Clipper::ClipTriangle()` 裁切到 AABB 内部，得到开放的裁切网格：

$$\Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma$$

这个开放网格直接用于公式 (2) 中的边界积分（施加 Dirichlet/Neumann 边界条件）。

**步骤 2b：三角化开放面片（封闭曲面）**

在 AABB 的每个轴对齐面上，收集 [ \Gamma_{\mathrm{c,c}} ] 的边界边，对 AABB 面内的封闭区域进行约束三角化，生成填补面片。将所有这些面片与 [ \Gamma_{\mathrm{c,c}} ] 合并，得到封闭曲面：

$$\Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) = \Gamma_{\mathrm{c,c}} \cup \Gamma_{\mathrm{c,plane}}$$

**步骤 2c：质量验证**

用散度定理验算体积：

$$\mathrm{Vol}_{\mathrm{computed}} = \int_{\Gamma_{\mathrm{c}}} \frac{1}{3}(x\,n_x + y\,n_y + z\,n_z)\,\mathrm{d}\Gamma$$

若 [ |\mathrm{Vol}_{\mathrm{computed}} - \mathrm{Vol}_{\mathrm{exact}}| / \mathrm{Vol}_{\mathrm{exact}} > \delta ]，则该单元被标记为有问题，根据用户设置决定是否丢弃。

**步骤 2d：生成曲面积分点**

在 [ \Gamma_{\mathrm{c}} ] 的每个三角面片上，放置高阶二维 Gauss 积分点（边界积分点）。这些点携带坐标、法向量和积分权重，将作为后续矩计算的输入。

### 阶段 3：矩的计算（散度定理 + Legendre 多项式反导数）

对每个裁切单元，计算矩拟合方程右端项（矩）向量 [ \pmb{b} ]，分量为：

$$b_j = \int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega = \sum_{f \in \Gamma_{\mathrm{c}}} \int_f \pmb{g}_j(\pmb{x}) \cdot \pmb{n}(\pmb{x})\,\mathrm{d}\Gamma$$

具体在代码中，对 [ \Gamma_{\mathrm{c}} ] 的每个边界积分点，计算三个方向的贡献（对应公式 (6)）并累加：

```cpp
value[0] = f_x_int_x[i_x] * f_x_y[i_y]     * f_x_z[i_z];     // ∫L_r dx · L_s · L_t
value[1] = f_x_x[i_x]     * f_x_int_y[i_y] * f_x_z[i_z];     // L_r · ∫L_s dy · L_t
value[2] = f_x_x[i_x]     * f_x_y[i_y]     * f_x_int_z[i_z]; // L_r · L_s · ∫L_t dz
double integrand = normal[0]*value[0] + normal[1]*value[1] + normal[2]*value[2];
rConstantTerms[row_index] += integrand * (1.0/3.0 * weight);
```

其中 `Polynomial::f_x()` 计算 Legendre 多项式值，`Polynomial::f_x_int()` 计算其解析反导数值（均在 `polynomial_utilities.hpp` 中硬编码，支持 0~8 阶）。

### 阶段 4：矩拟合求解（点消除 + NNLS）

**组装矩阵 [ \mathbf{A} ]**：在参数空间（UVW）中，对预设的候选积分点 [ \{\pmb{x}_i\} ] 计算 Legendre 多项式张量积：

$$A_{ji} = L_r(x_i)\,L_s(y_i)\,L_t(z_i), \quad f_j = L_r L_s L_t$$

**迭代点消除（PointElimination）**：
1. 初始候选点通过八叉树均匀分布在裁切域内（内部点）；
2. 调用 NNLS 求解 [ \mathbf{A}\pmb{\omega} = \pmb{b} ]，得到权重 [ \omega_i ]；
3. 移除权重最小的点；
4. 重复步骤 2~3，直至达到目标精度 [ \|\mathbf{A}\pmb{\omega} - \pmb{b}\|/\|\pmb{b}\| < \varepsilon ] 或达到最少点数 [ n_q = p_u \cdot p_v \cdot p_w ]；
5. 输出最终积分点集 [ \{(\pmb{x}_i, \omega_i)\} ]，满足 [ n_q \leq (p+1)^3 ] 且 [ \omega_i > 0 ]。

### 阶段 5：输出

所有单元（内部单元 + 裁切单元）的积分点和权重被存储在 `BackgroundGrid` 中，可直接输出为 FE 求解器可读的格式（JSON, VTK, Python 接口等）。FE 求解器只需接收背景网格 + 积分点集，即可进行有限元分析，无需了解 QuESo 的内部实现。

---

## 六、关键公式解读及对应代码

### 公式 (1)：体积分的单元分解

$$\int_{\Omega}(\cdot)\,\mathrm{d}\Omega = \sum_{B\in \mathcal{P}}\int_{B\cap \Omega}(\cdot)\,\mathrm{d}\Omega \qquad (1)$$

**含义：** 将物理域上的积分分解为背景网格活跃集（内部 + 裁切单元）上各单元积分域上积分之和。对内部单元 [ B_{\mathrm{in}} \cap \Omega = B_{\mathrm{in}} ]，标准 Gauss 规则可直接使用；对裁切单元 [ B_{\mathrm{cut}} \cap \Omega ]，需要矩拟合方法。

**代码对应（`embedded_model.cpp:96–151`）：**

```cpp
// 对背景网格所有元素（公式(1)的求和）
#pragma omp parallel for
for( int for_index = 0; for_index < global_number_of_elements; ++for_index) {
    if( status == IntersectionState::inside ) {
        // B_in：标准Gauss规则（公式(1)积分域 = 整个单元）
        QuadratureSingleElement<ElementType>::AssembleIPs(*p_new_element, polynomial_order);
    }
    else if( status == IntersectionState::trimmed ) {
        // B_cut：矩拟合（公式(1)积分域 = B_cut ∩ Ω，形状复杂）
        QuadratureTrimmedElement<ElementType>::AssembleIPs(*p_new_element, ...);
    }
}
```

---

### 公式 (2)：边界积分的单元分解

$$\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma = \sum_{B\in \mathcal{P}}\sum_{F_\beta\in \Gamma_\beta}\int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma, \qquad \beta \in \{D, N\} \qquad (2)$$

**含义：** 边界条件（Dirichlet 或 Neumann）的积分也按单元分解，每项是 STL 面片 [ F_\beta ] 被裁切到单元 [ B ] 内的部分 [ F_\beta \cap B ] 上的积分。这个裁切结果就是 [ \Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma ]，无需封闭。

**代码对应（`embedded_model.cpp:223`，`EmbeddedModel::ComputeCondition()`，`brep_operator.cpp::pClipTriangleMeshUnique()`）：**

```cpp
void EmbeddedModel::ComputeCondition(...) {
    // 对每个单元
    for(auto& el : active_elements) {
        // 裁切 STL → 得到 Γ_{c,c} = B_cut ∩ Γ（开放网格）
        auto p_new_mesh = brep_operator.pClipTriangleMeshUnique(
            bounding_box_xyz.first, bounding_box_xyz.second);
        // 在裁切后的面片上生成积分点，用于公式(2)的边界积分
        // （对应 F_β ∩ B，即裁切到当前单元范围内的 STL 面片）
        ...
    }
}
```

---

### 公式 (3)：矩拟合方程组

$$\underbrace{\begin{bmatrix} f_1(\pmb{x}_1) & \cdots & f_1(\pmb{x}_{n_q}) \\ \vdots & \ddots & \vdots \\ f_m(\pmb{x}_1) & \cdots & f_m(\pmb{x}_{n_q}) \end{bmatrix}}_{\mathbf{A}} \underbrace{\begin{bmatrix} \omega_1 \\ \vdots \\ \omega_{n_q} \end{bmatrix}}_{\pmb{\omega}} = \underbrace{\begin{bmatrix} \int_{\Omega_\mathrm{c}} f_1\,\mathrm{d}\Omega \\ \vdots \\ \int_{\Omega_\mathrm{c}} f_m\,\mathrm{d}\Omega \end{bmatrix}}_{\pmb{b}} \qquad (3)$$

**含义：** 矩拟合方程组的矩阵形式 [ \mathbf{A}\pmb{\omega} = \pmb{b} ]。[ \mathbf{A} ] 是基函数值组成的矩阵（预先固定积分点坐标后，[ \mathbf{A} ] 已知），[ \pmb{b} ] 是矩（右端项，通过公式 (5)~(7) 计算），[ \pmb{\omega} ] 是待求的积分权重向量。

**数据流：** [ \Gamma_\mathrm{c} ] 上的边界积分点 → `ComputeConstantTerms()` 计算 [ \pmb{b} ] → `MomentFitting()` 组装 [ \mathbf{A} ] → `NNLS::solve()` 求解 [ \pmb{\omega} ] → 赋值给积分点的 Weight 属性。

**代码对应（`trimmed_element.hpp:293`，`MomentFitting()`）：**

```cpp
static double MomentFitting(const VectorType& rConstantTerms,
                             IntegrationPointVectorType& rIntegrationPoint,
                             const ElementType& rElement,
                             const Vector3i& rIntegrationOrder) {
    // 组装矩阵 A（行：基函数 f_j，列：积分点 x_i）
    for (point_it : rIntegrationPoint) {
        for (i_x, i_y, i_z) {
            // A[row][col] = L_r(x_i) * L_s(y_i) * L_t(z_i)
            fitting_matrix[row_index][col_index] =
                Polynomial::f_x(x, i_x, a[0], b[0])
              * Polynomial::f_x(y, i_y, a[1], b[1])
              * Polynomial::f_x(z, i_z, a[2], b[2]);
        }
    }
    // 求解 Aω = b（NNLS，保证 ω_i ≥ 0）
    VectorType tmp_constant_terms(rConstantTerms);
    const double rel_residual = NNLS::solve(fitting_matrix, tmp_constant_terms, weights)
                                 / l2_norm_ct;
    // 将权重赋给积分点
    for (i, point_it : enumerate(rIntegrationPoint))
        point_it->SetWeight(weights[i]);
    return rel_residual;
}
```

---

### 公式 (4)：Legendre 多项式张量积基函数

$$F = \bigl\{\, f_j(\pmb{x}) = L_r(x)\,L_s(y)\,L_t(z) \;;\; r, s, t = 0, 1, \ldots, p \,\bigr\} \qquad (4)$$

**含义：** 矩拟合方程中使用的基函数集合。采用三维 Legendre 多项式的张量积，基函数个数为 [ m = (p+1)^3 ]。Legendre 多项式在 [ [-1,1] ] 上正交，对矩阵 [ \mathbf{A} ] 的条件数有利，且解析反导数已知（支持散度定理计算矩）。

**代码对应（`polynomial_utilities.hpp`，`Polynomial::f_x()`）：**

```cpp
// 计算定义在 [a,b] 上的第 order 阶 Legendre 多项式在 x 处的值
// （先将 x 映射到 [-1,1]：ξ = (2x-a-b)/(b-a)）
inline double f_x(double x, IndexType order, double a, double b) noexcept {
    switch(order) {
        case 0: return 1;                                           // L_0(ξ) = 1
        case 1: return (2*x-a-b)/(b-a);                           // L_1(ξ) = ξ
        case 2: return 1.0/2.0*(3.0*ξ²-1.0);                     // L_2(ξ)
        case 3: return 1.0/2.0*(5.0*ξ³ - 3.0*ξ);                // L_3(ξ)
        case 4: return 1.0/8.0*(35.0*ξ⁴-30.0*ξ²+3.0);           // L_4(ξ)
        // ... 直至 L_8
    }
}
```

---

### 公式 (5)：散度定理——体积分化为曲面积分

$$\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega = \int_{\Omega_{\mathrm{c}}} \nabla\cdot\pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega = \int_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma \qquad (5)$$

**含义：** 本文最核心的数学工具。通过 Gauss 散度定理，将三维体积分（矩 [ b_j ]）转化为封闭曲面 [ \Gamma_\mathrm{c} ] 上的二维曲面积分，积分维度降低一维。前提条件是存在满足 [ \nabla\cdot\pmb{g}_j = f_j ] 的矢量场（由公式 (6) 提供），且 [ \Gamma_\mathrm{c} ] 是封闭有向曲面（由阶段 2 的几何算法保证）。

**代码对应（`trimmed_element.hpp:209`，`ComputeConstantTerms()`，散度定理分支）：**

```cpp
// 对 Γ_c 上每个边界积分点（来自封闭曲面三角面片的 Gauss 点）
for (point_it : pBoundaryIPs) {
    const auto& normal = point_it->Normal();   // 外法向量 n
    PointType point = point_it->data();         // 积分点坐标 x

    // ... 预计算 f_x 和 f_x_int ...

    // 实现 g_j(x)·n(x) = 1/3 * [∫f_j dx·n_x + ∫f_j dy·n_y + ∫f_j dz·n_z]
    double integrand = normal[0]*value[0] + normal[1]*value[1] + normal[2]*value[2];
    // weight = 1/3 * (三角面片上的 Gauss 权重 × 面积)
    rConstantTerms[row_index] += integrand * (1.0/3.0 * point_it->Weight());
}
```

---

### 公式 (6)：抗导数矢量场 [ \pmb{g}_j ] 的构造

$$\pmb{g}_j(\pmb{x}) = \frac{1}{3} \begin{bmatrix} \int f_j\,\mathrm{d}x \\ \int f_j\,\mathrm{d}y \\ \int f_j\,\mathrm{d}z \end{bmatrix} \qquad (6)$$

**含义：** 给出公式 (5) 所需矢量场 [ \pmb{g}_j ] 的显式构造。三个分量分别是 [ f_j ] 对 [ x, y, z ] 的不定积分（乘以对称系数 [ 1/3 ]）。由于 [ f_j = L_r(x)L_s(y)L_t(z) ] 是张量积形式，各方向的不定积分解析已知：

$$\int L_r(x)L_s(y)L_t(z)\,\mathrm{d}x = \left(\int L_r(x)\,\mathrm{d}x\right)\cdot L_s(y)\cdot L_t(z)$$

**代码对应（`polynomial_utilities.hpp`，`Polynomial::f_x_int()`；`trimmed_element.hpp:273`）：**

```cpp
// 计算 L_r(x) 在 [a,b] 上的不定积分
inline double f_x_int(double x, IndexType order, double a, double b) noexcept {
    switch(order) {
        case 0: return x;                                         // ∫L_0 dx = x
        case 1: return -power((a+b-2x),2)/(4*(a-b));            // ∫L_1 dx
        case 2: return -x/2.0 - power((a+b-2x),3)/(4*(a-b)²); // ∫L_2 dx
        case 3: ...                                               // ∫L_3 dx
        // ... 直至 L_8
    }
}

// 在 ComputeConstantTerms() 中（对应公式(6)的三分量）：
value[0] = f_x_int_x[i_x] * f_x_y[i_y]     * f_x_z[i_z];  // g_j 的 x 分量
value[1] = f_x_x[i_x]     * f_x_int_y[i_y] * f_x_z[i_z];  // g_j 的 y 分量
value[2] = f_x_x[i_x]     * f_x_y[i_y]     * f_x_int_z[i_z]; // g_j 的 z 分量
double weight = 1.0/3.0 * point_it->Weight(); // 1/3 系数
```

---

### 公式 (7)：B-Rep 上的曲面积分离散

$$\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega = \sum_{f \in \Gamma_{\mathrm{c}}} \int_{f} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma \qquad (7)$$

**含义：** 将公式 (5) 中的封闭曲面积分离散化：把 [ \Gamma_\mathrm{c} ] 的所有三角面片 [ f ] 的积分求和。每个三角面片是平面的，法向量 [ \pmb{n}_f ] 为常量，可以在面片上放置高阶二维 Gauss 点精确计算。整个求和构成了矩 [ b_j ] 的最终计算方式。

**数据流（公式 (7) 在代码中的路径）：**

```
STL 三角面片（输入）
    ↓ BRepOperator::pGetTrimmedDomain()
    ↓ Clipper::ClipTriangle()              → 裁切到 AABB 内
    ↓ TrimmedDomainOnPlane（三角化）        → 封闭开放面片
    ↓ pGetBoundaryIps<BoundaryIPType>()    → 在每个三角面片上生成高阶 Gauss 点
         （点携带：坐标 x, 法向量 n, 权重 w = Gauss权重 × 面积）
    ↓ ComputeConstantTerms()（BoundaryIPs 分支）
         累加 b_j += g_j(x)·n × (1/3 × w)  ← 公式(7)的一个面片一个 Gauss 点的贡献
    ↓ rConstantTerms[]                     → 完整的矩向量 b
```

**代码对应（`trimmed_element.hpp:239–283`）：**

```cpp
// 公式(7)的实现：对 Γ_c 的所有三角面片上的 Gauss 点（边界积分点）求和
for( IndexType i = 0; i < pBoundaryIPs->size(); ++i ){
    auto point_it = pBoundaryIPs->begin() + i;
    const auto& normal = point_it->Normal(); // 面片法向量（常量）
    PointType point = point_it->data();       // Gauss 点坐标

    // 预计算 f_x 和 f_x_int（对应公式(4)(6)）
    for( i_x: 0..order_u )
        f_x_int_x[i_x] = Polynomial::f_x_int(point[0], i_x, a[0], b[0]);
    ...

    // 累加每个基函数 f_j 的矩贡献（对应公式(7)中一个面片一个 Gauss 点）
    for( i_x, i_y, i_z ) {
        value[0] = f_x_int_x * f_x_y * f_x_z;     // g_j 的 x 分量
        value[1] = f_x_x * f_x_int_y * f_x_z;     // g_j 的 y 分量
        value[2] = f_x_x * f_x_y * f_x_int_z;     // g_j 的 z 分量
        integrand = n·value;                          // g_j(x)·n(x)
        rConstantTerms[j] += integrand * (1.0/3.0 * point_it->Weight());
        // Weight = Gauss权重 × 三角面片面积（包含雅可比变换）
    }
}
```

---

## 七、公式与代码的串联：完整数据流

以下展示从 STL 输入到积分点输出的完整数据传递路径，每一步都标注了对应的论文公式：

```
用户输入：STL 文件 + 背景网格参数
        ↓
EmbeddedModel::ComputeVolume()   ← 论文 Algorithm 1
        ↓
FloodFill → 单元分类（内/外/裁切）← 论文 Section 5（创新点2）
        ↓ 对每个裁切单元 B_cut
BRepOperator::pGetTrimmedDomain()
   ├── Clipper::ClipTriangle()   ← STL ∩ AABB → Γ_{c,c}（开放）[用于公式(2)]
   └── TrimmedDomainOnPlane      ← 三角化开放面 → Γ_c（封闭）[公式(5)(7)的前提]
        ↓
TrimmedDomain::pGetBoundaryIps() ← 在 Γ_c 的三角面片上放置高阶 Gauss 点
        ↓
ComputeConstantTerms()           ← 计算矩 b_j（公式5、6、7）
   ├── Polynomial::f_x_int()     ← 解析反导数 ∫L_r dx（公式6，创新点3）
   └── ∑ g_j(x)·n · w           ← 散度定理离散求和（公式7）
        ↓ 得到 b ∈ R^m（m=(p+1)³）
        ↓
Octree → DistributeIntegrationPoints() ← 在 Ω_c 内均匀分布候选积分点
        ↓
MomentFitting()                  ← 组装矩阵 A，A_{ji} = L_r(x_i)L_s(y_i)L_t(z_i)（公式3、4）
   └── NNLS::solve(A, b, ω)      ← 非负最小二乘求解权重 ω
        ↓
PointElimination()               ← 迭代删点，n_q ≤ (p+1)³（公式3求解策略）
        ↓
最终输出：积分点集 {(x_i, ω_i)}，存入 BackgroundGrid
        ↓
FE 求解器使用积分点计算刚度矩阵 K = ∑ B^T D B · ω_i（公式1的数值实现）
```

---

## 八、总结

本文的技术贡献可以用一句话概括：**为嵌入式有限元方法中的矩拟合积分，提供了一套专门针对 STL 几何体、高效且鲁棒的矩计算框架**，其创新在于：

| 创新点 | 方法核心 | 代码实现 | 对应公式 |
|-------|---------|---------|---------|
| 专用裁切算法 | 只求最低限度封闭曲面，放弃传统网格质量要求 | `Clipper` + `TrimmedDomainOnPlane` | 公式 (5)(7) |
| 鲁棒单元分类 | 泛洪填充 + 局部光线追踪投票 + 并行条带分区 | `FloodFill` + `BRepOperator` | 公式 (1)(2) |
| 近机器精度矩计算 | 高阶 Gauss 曲面积分 + 解析 Legendre 反导数 | `Polynomial::f_x_int()` + `ComputeConstantTerms()` | 公式 (4)(5)(6)(7) |
| 高效矩拟合求解 | NNLS + 点消除（组合已有方法，非本文提出） | `MomentFitting()` + `NNLS::solve()` | 公式 (3)(4) |

该框架已在 Thingi10K 数据库的 4948 个 STL（含大量缺陷几何）上验证，所有情况下运行时间与背景网格元素数量呈最多线性关系，积分精度接近机器精度，并已集成到开源 C++ 框架 QuESo 中对外发布。

---

*报告生成日期：2026-04-04*
