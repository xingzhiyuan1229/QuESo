# 论文《Shape optimization of embedded solids using implicit Vertex-Morphing》总结

> 依据：/tmp/workspace/xingzhiyuan1229/QuESo/文献2.md

## 1 引言（研究动机与问题）

论文关注复杂三维实体的形状优化。传统边界拟合网格在大变形时会严重扭曲，需要频繁重网格。作者采用嵌入边界法，用固定背景网格承载有限元求解，使几何在网格中自由移动；同时引入隐式 Vertex-Morphing 和新的灵敏度加权方法，实现网格无关的形状梯度与几何约束控制。

---

## 2 嵌入边界方法

### 2.1 定义

**公式 (1)**  
$$
\int_{\Omega} (\cdot) \, d\Omega = \sum_{\mathcal{B} \in \mathcal{P}} \int_{\mathcal{B} \cap \Omega} (\cdot) \, d\Omega,
\tag{1}
$$
**简述**：将总体域 $\Omega$ 的体积分拆解为活动单元 $\mathcal{P}$ 上与实体相交子域的求和。数学事实：集合分解与积分可加性。

**公式 (2)**  
$$
\int_{\Gamma_{\beta}} (\cdot) \, d\Gamma = \sum_{\mathcal{B} \in \mathcal{P}} \sum_{F_{\beta} \in \Gamma_{\beta}} \int_{F_{\beta} \cap \mathcal{B}} (\cdot) \, d\Gamma, \quad \beta \in \{D, N\},
\tag{2}
$$
**简述**：边界积分按切割单元与边界片段的交集求和。数学事实：边界集合分解与积分可加性。

### 2.2 QuESo 数值积分

作者将 B-Rep（STL）边界输入 QuESo，使用剪切后的三角网格计算切割单元的矩量并解矩量拟合方程，从而得到正权重、域内积分点。该步骤支撑公式 (1)–(2) 在嵌入边界法中的可实施性。

---

## 3 基于节点的形状优化与隐式 Vertex-Morphing

### 3.0 优化问题表述

**公式 (3)**  
$$
\begin{array}{rl}
\min & f(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \\
\mathrm{subject~to} & \boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \mathbf{0}, \quad \mathrm{on}~\Gamma, \\
& \boldsymbol{r}(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = \mathbf{0}, \quad \mathrm{in}~\Omega, \\
& h_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = 0, \quad j = 1\dots m_h, \\
& g_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \leq 0, \quad j = 1\dots m_g,
\end{array}
\tag{3}
$$
**简述**：外层优化、内层分析的 NAND 结构。$f$ 为目标，$\boldsymbol{p}$ 为参数化约束，$\boldsymbol{r}$ 为平衡方程残差。

### 3.1 隐式 Vertex-Morphing（关键）

**公式 (4)**  
$$
\begin{array}{rl}
& p^j = -(r_\Gamma^H)^2 \nabla_\Gamma \cdot \nabla_\Gamma x_\Gamma^i + x_\Gamma^i - s_\Gamma^i = 0, \quad i \in \{1,2,3\}, \quad \text{on}~\Gamma, \\
& x_\Gamma^i = s_\Gamma^i, \quad \text{along}~\Gamma_D^H \text{ (fixed boundaries)},
\end{array}
\tag{4}
$$
**严格推导**：  
1. **建立滤波能量泛函**：$E=\int_\Gamma \left((x_\Gamma^i-s_\Gamma^i)^2+(r_\Gamma^H)^2\|\nabla_\Gamma x_\Gamma^i\|^2\right)d\Gamma$。数学事实：Tikhonov/Sobolev 正则化。  
2. **取变分**：$\delta E=\int_\Gamma 2(x_\Gamma^i-s_\Gamma^i)\delta x_\Gamma^i+2(r_\Gamma^H)^2\nabla_\Gamma x_\Gamma^i\cdot\nabla_\Gamma \delta x_\Gamma^i\,d\Gamma$。数学事实：变分法与链式法则。  
3. **表面分部积分**：$\int_\Gamma \nabla_\Gamma x\cdot\nabla_\Gamma \delta x\,d\Gamma=-\int_\Gamma (\nabla_\Gamma\cdot\nabla_\Gamma x)\delta x\,d\Gamma+\int_{\partial\Gamma}(\cdot)$。数学事实：表面 Green 恒等式/分部积分。  
4. **施加 Dirichlet 边界条件**：在 $\Gamma_D^H$ 上 $\delta x_\Gamma^i=0$，边界项消失。  
5. **得到 Euler–Lagrange 方程**：$-(r_\Gamma^H)^2\nabla_\Gamma\cdot\nabla_\Gamma x_\Gamma^i+x_\Gamma^i-s_\Gamma^i=0$。数学事实：Euler–Lagrange 方程。  
6. **固定边界**：$x_\Gamma^i=s_\Gamma^i$ 为 Dirichlet 条件。

**公式 (5)**  
$$
\nabla_\Gamma u = \nabla u - (\boldsymbol{n} \cdot \nabla u) \boldsymbol{n},
\tag{5}
$$
**简述**：切向梯度为总体梯度减去法向投影。数学事实：向量在切平面与法向方向上的正交分解。

**公式 (6)**  
$$
\boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \boldsymbol{K}_\Gamma^H \boldsymbol{x}_\Gamma + \boldsymbol{M}_\Gamma^H \boldsymbol{x}_\Gamma - \boldsymbol{M}_\Gamma^H \boldsymbol{s}_\Gamma = \mathbf{0},
\tag{6}
$$
**严格推导（简版）**：  
1. 以 $\boldsymbol{N}_\Gamma$ 为试函数做 Galerkin 离散。数学事实：弱式等价与有限元插值。  
2. 拉普拉斯项形成刚度矩阵 $\boldsymbol{K}_\Gamma^H$，零阶项形成质量矩阵 $\boldsymbol{M}_\Gamma^H$。  
3. 得到线性系统形式，如上式。

**公式 (7)**  
$$
\boldsymbol{M}_\Gamma^H = \sum \int_{\Gamma^e} \boldsymbol{N}_\Gamma^T \boldsymbol{N}_\Gamma \, d\Gamma,
\tag{7}
$$
**简述**：表面质量矩阵定义。

**公式 (8)**  
$$
\boldsymbol{K}_\Gamma^H = (r_\Gamma^H)^2 \sum \int_{\Gamma^e} (\nabla_\Gamma \boldsymbol{N}_\Gamma)^T \nabla_\Gamma \boldsymbol{N}_\Gamma \, d\Gamma,
\tag{8}
$$
**简述**：表面刚度矩阵定义，系数 $(r_\Gamma^H)^2$ 控制平滑强度。

**公式 (9)**  
$$
\boldsymbol{x}_\Gamma = \boldsymbol{A}_\Gamma^H \boldsymbol{s}_\Gamma; \quad \boldsymbol{A}_\Gamma^H = (\boldsymbol{K}_\Gamma^H + \boldsymbol{M}_\Gamma^H)^{-1} \boldsymbol{M}_\Gamma^H.
\tag{9}
$$
**严格推导**：  
1. 由公式 (6) 得 $(\boldsymbol{K}_\Gamma^H+\boldsymbol{M}_\Gamma^H)\boldsymbol{x}_\Gamma=\boldsymbol{M}_\Gamma^H\boldsymbol{s}_\Gamma$。数学事实：线性方程组等价变形。  
2. 左乘逆矩阵得到 $\boldsymbol{x}_\Gamma=(\boldsymbol{K}_\Gamma^H+\boldsymbol{M}_\Gamma^H)^{-1}\boldsymbol{M}_\Gamma^H\boldsymbol{s}_\Gamma$。数学事实：可逆矩阵乘法。

### 3.2 更新规则

**公式 (10)**  
$$
{}^{k+1}\boldsymbol{s}_\Gamma = {}^{k}\boldsymbol{s}_\Gamma - \alpha [\boldsymbol{I} - \boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}\boldsymbol{P}^T] \frac{d f}{d \boldsymbol{s}_\Gamma}\bigg|_{\mathcal{C}} - [\boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}] \boldsymbol{g}^a,
\tag{10}
$$
**严格推导（简版）**：  
1. 设 $\boldsymbol{P}$ 为激活约束梯度矩阵。数学事实：约束的线性化。  
2. 正交投影到约束切空间：$\mathbf{P}_{\perp}=\boldsymbol{I}-\boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}\boldsymbol{P}^T$。数学事实：最小二乘投影/正交投影公式。  
3. 在可行域切空间内沿负梯度下降，并添加恢复项处理非线性约束。

**公式 (11)**  
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \boldsymbol{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c.
\tag{11}
$$
**严格推导**：  
1. 由公式 (9) 得 $\boldsymbol{x}_\Gamma=\boldsymbol{A}_\Gamma^H\boldsymbol{s}_\Gamma$。  
2. 对 $J(\boldsymbol{x}_\Gamma)$ 用链式法则：$\frac{dJ}{d\boldsymbol{s}_\Gamma}=\left(\frac{d\boldsymbol{x}_\Gamma}{d\boldsymbol{s}_\Gamma}\right)^T\frac{dJ}{d\boldsymbol{x}_\Gamma}=(\boldsymbol{A}_\Gamma^H)^T\frac{dJ}{d\boldsymbol{x}_\Gamma}$。  
3. 文中采用 $\boldsymbol{A}_\Gamma^H$ 表达，等价于对称或近似对称的实现假设。数学事实：链式法则与矩阵微分。

---

## 4 灵敏度分析（关键贡献）

### 4.0 背景方程

**公式 (12)**  
$$
\mathbf{K} \mathbf{u} = \mathbf{F},
\tag{12}
$$
**简述**：背景网格上的离散平衡方程。

### 4.1 半解析灵敏度（关键）

**公式 (13)**  
$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \frac{d\mathbf{u}}{d\boldsymbol{x}_p}.
\tag{13}
$$
**严格推导**：  
1. $J=J(\boldsymbol{x}_p,\mathbf{u})$，对 $\boldsymbol{x}_p$ 求全导数。数学事实：多变量链式法则。  
2. 将显式依赖与隐式依赖分离得到 (13)。

**公式 (14)**  
$$
\frac{d\mathbf{u}}{d\boldsymbol{x}_p} = \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right).
\tag{14}
$$
**严格推导**：  
1. 对公式 (12) 求导：$\frac{d\mathbf{K}}{d\boldsymbol{x}_p}\mathbf{u}+\mathbf{K}\frac{d\mathbf{u}}{d\boldsymbol{x}_p}=\frac{d\mathbf{F}}{d\boldsymbol{x}_p}$。数学事实：矩阵乘积求导。  
2. 移项得 $\mathbf{K}\frac{d\mathbf{u}}{d\boldsymbol{x}_p}=\frac{d\mathbf{F}}{d\boldsymbol{x}_p}-\frac{d\mathbf{K}}{d\boldsymbol{x}_p}\mathbf{u}$。  
3. 左乘 $\mathbf{K}^{-1}$ 得 (14)。数学事实：线性系统求解与可逆性。

**公式 (15)**  
$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right) = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \mathbf{F}^*.
\tag{15}
$$
**简述**：将 (14) 代入 (13) 得到；定义伪载荷 $\mathbf{F}^*$。

**公式 (16)**  
$$
\mathbf{F}^* = \frac{\mathbf{F}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{F}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} - \frac{\mathbf{K}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{K}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} \mathbf{u}.
\tag{16}
$$
**简述**：用有限差分近似 $\frac{d\mathbf{F}}{d\boldsymbol{x}_p}$ 与 $\frac{d\mathbf{K}}{d\boldsymbol{x}_p}$。数学事实：一阶有限差分。

**公式 (17)**  
$$
\frac{dU}{d\boldsymbol{x}_p}\bigg|_d = \mathbf{u}^T \mathbf{F}^*.
\tag{17}
$$
**严格推导**：  
1. $U=\frac{1}{2}\mathbf{u}^T\mathbf{K}\mathbf{u}$，若 $\mathbf{K}$ 对称则 $\frac{\partial U}{\partial \mathbf{u}}=\mathbf{K}\mathbf{u}$。数学事实：二次型求导。  
2. 在静力平衡下 $\mathbf{K}\mathbf{u}=\mathbf{F}$，代入 (15) 并取 $\frac{\partial U}{\partial \boldsymbol{x}_p}=0$ 得 $dU/d\boldsymbol{x}_p=\mathbf{u}^T\mathbf{F}^*$。

### 4.2 灵敏度加权与插值

#### 4.2.1 一维示例

**公式 (18)**  
$$
\overline{V} = A (\overline{x}_\Gamma - \overline{x}_{p1}),
\tag{18}
$$
**简述**：边界拟合网格中体积表达式。

**公式 (19)**  
$$
\frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = A.
\tag{19}
$$
**简述**：体积对边界点的离散灵敏度。

**公式 (20)**  
$$
\frac{1}{A} \frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = 1.
\tag{20}
$$
**简述**：对面积归一化得到网格无关的单位梯度。

**公式 (21)**  
$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (x_\Gamma - x_{p,2}).
\tag{21}
$$
**简述**：嵌入网格下两个单元的体积分解。

**公式 (22)**  
$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (N_1(\xi_\Gamma) x_{p,2} + N_2(\xi_\Gamma) x_{p,3} - x_{p,2}).
\tag{22}
$$
**简述**：用形函数将 $x_\Gamma$ 写成背景节点插值。

**公式 (23)**  
$$
\frac{dV}{dx_{p,2}}\bigg|_d = A N_1(\xi_\Gamma).
\tag{23}
$$
**简述**：离散灵敏度依赖切割位置。

**公式 (24)**  
$$
\frac{dV}{dx_{p,3}}\bigg|_d = A N_2(\xi_\Gamma).
\tag{24}
$$
**简述**：同上。

**公式 (25)**  
$$
\mathcal{N}_{x_{p,2}} = \int N_1 \, d\Gamma = A N_1(\xi_\Gamma).
\tag{25}
$$
**简述**：权重因子定义。

**公式 (26)**  
$$
\mathcal{N}_{x_{p,3}} = \int N_2 \, d\Gamma = A N_2(\xi_\Gamma).
\tag{26}
$$
**简述**：权重因子定义。

**公式 (27)**  
$$
\frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d = 1.
\tag{27}
$$
**简述**：加权后得到网格无关梯度。

**公式 (28)**  
$$
\frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1.
\tag{28}
$$
**简述**：同上。

**公式 (29)**  
$$
\frac{dV}{dx_\Gamma}\bigg|_c = N_1(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d + N_2(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1.
\tag{29}
$$
**简述**：用加权后的节点梯度插值得到边界处的连续梯度。

#### 4.2.2 二维示例

**公式 (30)**  
$$
\frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = t (\Delta \overline{A}^1 + \Delta \overline{A}^2) = t a,
\tag{30}
$$
**简述**：边界拟合网格中面积变化导致的体积梯度。

**公式 (31)**  
$$
\frac{1}{t a} \frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = 1.
\tag{31}
$$
**简述**：归一化得到单位梯度。

**公式 (32)**  
$$
\frac{dV}{dx_p^*}\bigg|_d = t (\Delta A^1 + \Delta A^2) = t \left( N_{\boldsymbol{x}_p^*}^1(\hat{\xi}, \hat{\eta}) \frac{a}{2} + N_{\boldsymbol{x}_p^*}^2(\hat{\xi}, \hat{\eta}) \frac{a}{2} \right).
\tag{32}
$$
**简述**：嵌入网格中的离散梯度依赖切割位置。

**公式 (33)**  
$$
\mathcal{N}_{\boldsymbol{x}_p^*} = \int_{\Gamma} N_{\boldsymbol{x}_p^*}^1 \, d\Gamma + \int_{\Gamma} N_{\boldsymbol{x}_p^*}^2 \, d\Gamma.
\tag{33}
$$
**简述**：二维情形权重因子。

**公式 (34)**  
$$
\frac{1}{\mathcal{N}_{\boldsymbol{x}_p^*}} \frac{dV}{dx_p^*}\bigg|_d = 1.
\tag{34}
$$
**简述**：加权后得到网格无关结果。

**公式 (35)**  
$$
\frac{dV}{dx_\Gamma^*}\bigg|_c = \sum_{i=1}^{n=4} N_{\boldsymbol{x}_{pi}}(\hat{\xi}, \hat{\eta}) \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dV}{d\boldsymbol{x}_{pi}}\bigg|_d \right) = 1.
\tag{35}
$$
**简述**：二维情形的加权插值公式。

#### 4.2.3 三维推广（关键）

**公式 (36)**  
$$
\frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c = \sum_{i \in \mathcal{I}} N_{\boldsymbol{x}_{pi}} \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dJ}{d\boldsymbol{x}_{pi}}\bigg|_d \right),
\tag{36}
$$
**严格推导（简版）**：  
1. 以背景网格节点梯度为离散“样本”，用形函数 $N_{\boldsymbol{x}_{pi}}$ 插值到边界。数学事实：有限元插值与分片多项式基函数。  
2. 为消除网格尺寸影响，引入 $\mathcal{N}_{\boldsymbol{x}_{pi}}$ 作为尺度因子。数学事实：尺度归一化与一致性要求。  
3. 得到连续梯度 $dJ/d\boldsymbol{x}_\Gamma|_c$。

**公式 (37)**  
$$
\mathcal{N}_{\boldsymbol{x}_{pi}} = \sum_{j \in \mathcal{J}} \int_{\Gamma} N_{\boldsymbol{x}_{pi}} \, d\Gamma,
\tag{37}
$$
**严格推导**：  
1. 将一维、二维示例中的“形函数积分”推广到三维边界 $\Gamma$。  
2. 对所有与节点 $i$ 相关的形函数求边界积分，形成尺度权重。数学事实：数值积分与形函数分片性。

**公式 (38)**  
$$
e_{V} = \frac{\| \mathbf{V}_{\text{grad}} - \mathbf{N} \|_{L2}}{\| \mathbf{N} \|_{L2}},
\tag{38}
$$
**简述**：以法向量场为参考的 $L2$ 相对误差。

---

## 5 几何约束

### 5.1 约束激活检测

**公式 (39)**  
$$
d < 2 \Delta x_{\max} + t_{\min} \quad \text{(for wall thickness constraints)}, \qquad
d < 2 \Delta x_{\max} \quad \text{(for design space constraints)},
\tag{39}
$$
**简述**：通过射线相交检测与缓冲距离判断约束是否激活。

### 5.2 约束执行（关键）

**公式 (40)**  
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c^{\text{proj}} = \mathbf{D}_\Gamma \frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c, \quad \text{with} \quad \mathbf{D}_\Gamma = [\mathbf{I} - \mathbf{N}(\mathbf{N}^T \mathbf{N})^{-1} \mathbf{N}^T].
\tag{40}
$$
**严格推导**：  
1. 设 $\mathbf{N}$ 的列向量为不可行方向（约束法向）。  
2. 目标是将梯度投影到 $\mathbf{N}$ 的正交补空间，使用正交投影公式 $\mathbf{P}_\perp=\mathbf{I}-\mathbf{N}(\mathbf{N}^T\mathbf{N})^{-1}\mathbf{N}^T$。数学事实：正交投影与最小二乘解。  
3. 得到阻尼矩阵 $\mathbf{D}_\Gamma$ 并修正梯度方向。

**公式 (41)**  
$$
\boldsymbol{x}_\Gamma = \mathbf{A}_\Gamma^H \mathbf{D}_\Gamma \boldsymbol{s}_\Gamma,
\tag{41}
$$
**简述**：将阻尼矩阵直接嵌入参数化映射。

**公式 (42)**  
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \mathbf{D}_\Gamma \mathbf{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c.
\tag{42}
$$
**简述**：阻尼后的灵敏度传递公式。

### 5.3 设计面与非设计面过渡

无新公式，使用滤波传播单位向量场并设阈值 $\delta$ 控制过渡带。

---

## 6 优化流程（整体串联）

作者将“嵌入边界积分 → 参数化与更新 → 灵敏度加权 → 约束阻尼 → 形状更新”串成闭环，确保在固定背景网格上完成形状优化。

---

## 7 数值实验

### 7.1 体积最大化立方体

**公式 (43)**  
$$
{}^{k+1}\boldsymbol{x}_\Gamma = {}^{k}\boldsymbol{x}_\Gamma + \alpha \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c,
\tag{43}
$$
**简述**：在物理空间直接用连续梯度更新边界。

**公式 (44)**  
$$
e_R = \frac{\max\{R\} - \min\{R\}}{\mathrm{avg}\{R\}}, \quad \text{with} \quad \mathrm{avg}\{R\} = \frac{\sum_{R \in \mathcal{R}} R}{|\mathcal{R}|},
\tag{44}
$$
**简述**：立方体向球收敛的半径一致性指标。

### 7.2 挂钩结构应变能最小化

**公式 (45)**  
$$
\begin{array}{rl}
\min & U, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& m - m_{\text{init}} = 0,
\end{array}
\tag{45}
$$
**简述**：保持质量不变的应变能最小化问题。

### 7.3 喷气发动机支架质量最小化

**公式 (46)**  
$$
\begin{array}{rl}
\min & m, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& U_j - U_j^{\text{init}} = 0, \quad j \in \{1,2,3,4\},
\end{array}
\tag{46}
$$
**简述**：多载荷约束下的质量最小化问题。

---

## 8 结论（主要收获）

作者构建了“嵌入边界 + 隐式 Vertex-Morphing + 灵敏度加权 + 约束阻尼”的完整形状优化流程，在固定背景网格上实现复杂实体的稳定优化，避免了网格畸变问题。

---

## 2）流程表（按论文顺序）

| 环节 | 该环节作用 | 该环节对应公式几 | 公式作用 |
|---|---|---|---|
| 嵌入边界积分分解 | 将体积分/边界积分分配到切割单元 | (1)-(2) | 定义嵌入边界法的积分框架 |
| QuESo 生成积分点 | 对切割单元构建稳定积分点 | 无编号公式 | 支撑 (1)-(2) 的数值积分 |
| 优化问题建模 | 统一目标、参数化、方程与约束 | (3) | 给出 NAND 形式优化模型 |
| 隐式 Vertex-Morphing | 控制特征尺度、平滑几何 | (4)-(9) | 建立滤波 PDE 及离散算子 |
| 约束投影更新 | 在约束可行域内更新设计 | (10) | 形成梯度投影迭代 |
| 灵敏度传递 | 从物理空间到控制空间 | (11) | 梯度链式传递 |
| 背景网格求解 | 解有限元平衡方程 | (12) | 定义状态变量 |
| 半解析灵敏度 | 计算背景节点离散梯度 | (13)-(17) | 建立伪载荷与灵敏度公式 |
| 加权插值 | 消除切割位置影响 | (18)-(37) | 构造网格无关梯度 |
| 误差评估 | 检验灵敏度一致性 | (38), (44) | 定义误差度量 |
| 几何约束检测 | 识别违反厚度/设计域的点 | (39) | 激活约束条件 |
| 约束阻尼 | 抑制不可行方向 | (40)-(42) | 构造阻尼矩阵并更新梯度 |
| 直接形状更新 | 用连续梯度更新边界 | (43) | 物理空间更新规则 |
| 数值算例 | 展示优化效果与约束 | (45)-(46) | 实验问题定义 |

---

## 3）创新点（结合关键公式与代码）

### 创新点 1：将嵌入边界法与隐式 Vertex-Morphing 深度融合

- **创新点是什么**：在固定背景网格上进行节点级形状优化，同时用隐式滤波控制特征尺寸与非设计面。  
- **作者怎么做到的**：以公式 (1)–(2) 定义嵌入边界积分框架，结合公式 (4)–(9) 的 Helmholtz/Sobolev 滤波构造参数化映射。  
- **相关核心公式解读**：  
  - 公式 (1)–(2) 说明几何与背景网格解耦。  
  - 公式 (4)–(9) 给出滤波 PDE 与离散算子 $\boldsymbol{A}_\Gamma^H$，保证形状更新平滑并可控。  
- **怎么通过代码实现，解读代码**：  
  - 仓库中积分框架由 QuESo 实现：`queso/embedded_model.cpp` 中通过积分点累积体积（对应公式 (1)），`kratos_interface/weak_bcs.py` 通过边界积分点施加载荷（对应公式 (2)）。  
  - 参数化滤波与优化更新在仓库中标注为“未实现”，需要在上层优化器里实现 $\boldsymbol{K}_\Gamma^H,\boldsymbol{M}_\Gamma^H$ 组装与求解（公式 (6)–(9)）。  
  - 代码落地方式：在几何边界三角网格上组装表面有限元矩阵；调用线性求解器得到 $\boldsymbol{x}_\Gamma$ 并与优化器接口对接。

### 创新点 2：提出嵌入边界的网格无关灵敏度加权与插值

- **创新点是什么**：在嵌入边界下恢复连续、网格无关的形状梯度，避免切割位置影响。  
- **作者怎么做到的**：先在背景网格求离散灵敏度 (13)–(17)，再用加权因子 (36)–(37) 进行插值恢复。  
- **相关核心公式解读**：  
  - 公式 (13)–(17) 给出半解析灵敏度与伪载荷。  
  - 公式 (36)–(37) 用边界上的形函数积分构造权重，实现尺度归一化。  
- **怎么通过代码实现，解读代码**：  
  - 仓库提供背景网格与积分点：`kratos_interface/model_part_utilities.py` 在 Kratos 中创建带积分点的单元（对应公式 (12)）。  
  - 实现建议：在 FE 求解后，对每个背景节点计算 $dJ/d\boldsymbol{x}_p$；利用 QuESo 提供的切割后的边界网格与积分点，对 (37) 进行数值积分；最后按 (36) 在边界顶点插值。  
  - 这一步需要新增“灵敏度模块”，目前仓库尚未覆盖。

### 创新点 3：几何约束通过阻尼矩阵直接融入参数化与灵敏度

- **创新点是什么**：将最小壁厚与设计空间约束转化为方向阻尼，而非大量显式约束。  
- **作者怎么做到的**：用公式 (39) 检测激活约束，公式 (40) 构造阻尼矩阵，并在公式 (41)–(42) 中嵌入参数化与梯度传递。  
- **相关核心公式解读**：  
  - 公式 (40) 是正交投影矩阵，保证梯度不朝向不可行方向。  
  - 公式 (41)–(42) 将阻尼直接写入几何与灵敏度映射。  
- **怎么通过代码实现，解读代码**：  
  - 约束检测需要射线与三角网格相交。QuESo 中的几何裁剪与相交能力可复用（见 `queso` 的裁剪/几何处理模块）。  
  - 实现步骤：为每个顶点计算法向，射线检测得到 $\Gamma_c$ 距离并判断 (39)，组装 $\mathbf{N}$ 与 $\mathbf{D}_\Gamma$，再在梯度更新处应用 (40)–(42)。  
  - 仓库目前未实现约束阻尼模块，需要在优化层新增矩阵与邻域传播逻辑。

---

## 4）摘要逐句翻译与解读

1. **One of the biggest challenges in optimizing the shape of complex solids is the requirement to maintain a reasonable mesh quality not only at the boundary but also for the bulk discretization of the interior.**  
   中文翻译：优化复杂实体形状时，最大的挑战之一是在边界和内部体网格都保持合理的网格质量。  
   摘要解读：指出传统方法面临的核心痛点，即形状更新导致网格畸变。  

2. **Thus, additional regularization and, in many cases, re-meshing of the structure during the iterative process is unavoidable with a Lagrangian description.**  
   中文翻译：因此在拉格朗日描述下，迭代过程中必须进行额外正则化，且往往需要重新网格化。  
   摘要解读：说明现有方法的代价和局限。  

3. **By tracking the shape update using an Eulerian representation, embedded boundary methods are a promising technique for eliminating mesh distortion problems.**  
   中文翻译：若采用欧拉描述跟踪形状更新，嵌入边界法有望消除网格畸变问题。  
   摘要解读：提出解决思路，即固定背景网格。  

4. **This work consistently combines the unique features of implicit Vertex-Morphing and embedded boundary methods, facilitating the node-based shape optimization of solids with industrial complexity.**  
   中文翻译：本文将隐式 Vertex-Morphing 与嵌入边界法结合，实现工业复杂实体的节点级形状优化。  
   摘要解读：给出本文核心组合方法。  

5. **One of the crucial elements for solving the primal problem on a fixed background grid is an efficient and robust quadrature scheme.**  
   中文翻译：在固定背景网格上求解主问题的关键之一是高效稳健的数值积分方案。  
   摘要解读：强调 QuESo 的必要性。  

6. **To this end, we incorporate the open-source C++ framework QuESo (https://github.com/manuelmessmer/QuESo) developed for the numerical integration of arbitrarily complex embedded solids defined by oriented boundary meshes, e.g., in STereoLithography (STL) format.**  
   中文翻译：为此引入 QuESo 框架，用于对 STL 等边界网格定义的复杂嵌入实体进行数值积分。  
   摘要解读：说明技术实现路径。  

7. **Meanwhile, applying the Helmholtz/Sobolev-based (implicit) filter to the vertices of the embedded boundary mesh not only exploits the extensive design space of node-based optimization but also ensures robust control over the feature size.**  
   中文翻译：同时对嵌入边界顶点施加 Helmholtz/Sobolev 隐式滤波，既利用节点级设计空间，又能稳健控制特征尺寸。  
   摘要解读：解释隐式 Vertex-Morphing 的作用。  

8. **To realize the above methodology, this work introduces a novel sensitivity analysis that yields mesh-independent shape gradients with respect to the immersed boundary.**  
   中文翻译：为实现上述方法，本文提出新的灵敏度分析，得到与网格无关的嵌入边界形状梯度。  
   摘要解读：点明第二个核心贡献。  

9. **Through a specific sensitivity weighting, we recover a continuous gradient field from discrete values calculated at the nodes of the background grid.**  
   中文翻译：通过特定灵敏度加权，将背景网格节点的离散梯度恢复为连续梯度场。  
   摘要解读：对应公式 (36)–(37) 的核心思想。  

10. **In addition, the presented workflow ensures robust enforcement of challenging geometrical constraints, including minimum wall thicknesses and design space limitations.**  
   中文翻译：此外，流程还能稳健地执行最小壁厚与设计空间等几何约束。  
   摘要解读：对应公式 (39)–(42) 的约束阻尼。  

11. **We critically assess our approach with benchmarks and structures of industrial relevance.**  
   中文翻译：我们用基准算例与工业结构验证该方法。  
   摘要解读：说明实验验证范围。  

12. **In all examples, trivariate B-Spline bases span the background grid, providing highly accurate finite element solutions and shape sensitivities at every iteration.**  
   中文翻译：所有算例都用三变量 B-Spline 构造背景网格，保证高精度的有限元解与灵敏度。  
   摘要解读：说明离散精度与网格选择。  

13. **Moreover, the elimination of mesh distortion problems enables a successful termination at the local optimum, even for large shape modifications.**  
   中文翻译：此外，消除网格畸变后，即便大幅形状变化也能在局部最优处收敛。  
   摘要解读：总结方法优势与收益。  

**整篇文章干了什么，得到了什么收益**：  
- 文章构建了完整的嵌入边界形状优化流程，并引入隐式滤波与灵敏度加权。  
- 得到了网格无关的连续梯度、稳定的几何约束处理，以及对复杂实体的可行优化。  
- 工业算例证明：在固定背景网格下仍可获得高精度与收敛性。
