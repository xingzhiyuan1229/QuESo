# 论文公式逐一解析与推导

**论文标题：** Robust numerical integration of embedded solids described in boundary representation

**作者：** Manuel Messmer, Stefan Kollmannsberger, Roland Wüchner, Kai-Uwe Bletzinger

---

## 概述：公式体系与内在联系

本论文共提出 **7 个核心编号公式**，它们构成一条完整的数学推导链：

$$
\underbrace{(1),(2)}_{\text{有限元积分分解}} \xrightarrow{\text{裁切元素困难}} \underbrace{(3),(4)}_{\text{矩拟合方程}} \xrightarrow{\text{矩的计算}} \underbrace{(5),(6)}_{\text{散度定理变换}} \xrightarrow{\text{B-Rep 离散}} \underbrace{(7)}_{\text{三角面片曲面积分}}
$$

整体逻辑为：**嵌入式有限元方法需要在裁切单元上进行数值积分** → **矩拟合方程可高效构造积分规则，但需预先计算"矩"（右端项）** → **散度定理将三维体积分降为二维曲面积分** → **对 B-Rep（STL 网格）离散，曲面积分化为各三角面片积分之和**，从而避免了对三维裁切域的空间细化，实现高效鲁棒的数值积分。

---

## 第 2 节：嵌入式方法（Embedded Methods）

### 基本设定与符号

设 [ \Omega \subset \mathbb{R}^3 ] 为需要求解偏微分方程（PDE）的物理域，其边界为 [ \Gamma = \partial\Omega ]。边界被分为两个不相交的部分：

- [ \Gamma_D ]：Dirichlet 边界（施加本质边界条件），
- [ \Gamma_N ]：Neumann 边界（施加自然边界条件），

满足：

$$\Gamma_D \cup \Gamma_N = \Gamma, \qquad \Gamma_D \cap \Gamma_N = \emptyset$$

嵌入式方法的核心思想是引入一个简单的**背景分区（background partition）** [ \mathbf{P}_{\mathrm{bg}} ]，由规则的六面体单元/网格 [ B \in \mathbf{P}_{\mathrm{bg}} ] 构成，将物理域 [ \Omega ] 嵌入其中。由于背景网格不贴合几何边界，不可避免地出现三类单元：

| 符号 | 类型 | 含义 |
|------|------|------|
| [ B_{\mathrm{in}} ] | 内部单元 | 完全在 [ \Omega ] 内部，[ B_{\mathrm{in}} \subset \Omega ] |
| [ B_{\mathrm{out}} ] | 外部单元 | 完全在 [ \Omega ] 外部，[ B_{\mathrm{out}} \cap \Omega = \emptyset ] |
| [ B_{\mathrm{cut}} ] | 裁切单元 | 与 [ \Omega ] 的边界 [ \Gamma ] 相交 |

**活跃集**（参与计算的单元集合）定义为：

$$\mathbf{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}}$$

即去除所有外部单元后剩余的集合，包含 [ B_{\mathrm{in}} ] 和 [ B_{\mathrm{cut}} ] 两类单元。

---

### 公式 (1)：体积分的分解

$$
\int_{\Omega}(\cdot)\,\mathrm{d}\Omega = \sum_{B\in \mathcal{P}}\int_{B\cap \Omega}(\cdot)\,\mathrm{d}\Omega
\qquad (1)
$$

#### 物理含义

公式 (1) 将整个物理域 [ \Omega ] 上的积分分解为背景网格活跃集 [ \mathcal{P} ] 中所有单元对应积分域 [ B \cap \Omega ] 上积分之和。这是有限元方法中**单元级分解（element-wise decomposition）**的标准形式。

#### 推导过程

由集合论基础，若 [ \{B_i\}_{B_i \in \mathcal{P}} ] 构成对 [ \Omega ] 的（近似）覆盖，且外部单元 [ B_{\mathrm{out}} ] 满足 [ B_{\mathrm{out}} \cap \Omega = \emptyset ]，则：

$$
\int_{\Omega}(\cdot)\,\mathrm{d}\Omega
= \int_{\bigcup_{B\in\mathcal{P}}(B\cap\Omega)}(\cdot)\,\mathrm{d}\Omega
$$

由于不同单元的内部互不重叠（背景网格无重叠），且对外部单元有 [ B_{\mathrm{out}} \cap \Omega = \emptyset ]：

$$
= \sum_{B\in\mathcal{P}} \int_{B\cap\Omega}(\cdot)\,\mathrm{d}\Omega
= \sum_{B_{\mathrm{in}}\in\mathcal{P}}\int_{B_{\mathrm{in}}}(\cdot)\,\mathrm{d}\Omega
+ \sum_{B_{\mathrm{cut}}\in\mathcal{P}}\int_{B_{\mathrm{cut}}\cap\Omega}(\cdot)\,\mathrm{d}\Omega
$$

**对内部单元的积分**（[ B_{\mathrm{in}} \cap \Omega = B_{\mathrm{in}} ]，积分域就是整个单元本身）可直接用标准 Gauss 求积分规则处理；**对裁切单元的积分**（[ B_{\mathrm{cut}} \cap \Omega ]，积分域为单元内部与几何域的交集）则需要更为复杂的处理策略，这正是本文后续工作的动因。

#### 关键观察

对于裁切单元，积分域 [ B_{\mathrm{cut}} \cap \Omega ] 的边界为：

$$
\partial(B_{\mathrm{cut}} \cap \Omega) = (B_{\mathrm{cut}} \cap \Gamma) \cup (B_{\mathrm{cut,faces}} \cap \Omega)
$$

即由几何边界与单元内部的交 [ B_{\mathrm{cut}} \cap \Gamma ]（来自 STL 曲面的裁切部分），以及属于单元面且在 [ \Omega ] 内部的矩形面片（来自单元自身的面，但被 [ \Omega ] 截断）共同构成。正是这个封闭曲面 [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ] 的计算构成了第 5 节算法的核心任务。

---

### 公式 (2)：边界积分的分解

$$
\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma = \sum_{B\in \mathcal{P}}\sum_{F_\beta\in \Gamma_\beta}\int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma,
\qquad \beta \in \{D, N\}
\qquad (2)
$$

#### 物理含义

公式 (2) 对应有限元弱形式中的**边界积分项**（Dirichlet 或 Neumann 边界条件的贡献）的单元级分解。其中 [ F_D ] 和 [ F_N ] 分别是定义在 [ \Gamma_D ] 和 [ \Gamma_N ] 上的边界面片（triangular faces，来自 STL 网格）。

#### 推导过程

由于几何边界 [ \Gamma_\beta ] 由 STL 面片（三角形）组成，且这些面片的边界与背景网格单元边界一般不重合，因此积分必须按单元分解。对 [ \Gamma_\beta ] 的每个面片 [ F_\beta ]，其与单元 [ B ] 的相交部分 [ F_\beta \cap B ] 的积分贡献为：

$$
\int_{\Gamma_\beta}(\cdot)\,\mathrm{d}\Gamma
= \sum_{F_\beta \in \Gamma_\beta} \int_{F_\beta}(\cdot)\,\mathrm{d}\Gamma
= \sum_{F_\beta \in \Gamma_\beta} \sum_{B\in\mathcal{P}} \int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma
= \sum_{B\in\mathcal{P}} \sum_{F_\beta \in \Gamma_\beta} \int_{F_\beta \cap B}(\cdot)\,\mathrm{d}\Gamma
$$

（交换求和顺序合法，因为每个面片 [ F_\beta ] 只与有限个单元 [ B ] 相交。）

#### 与公式 (1) 的内在联系

公式 (1) 和公式 (2) 共同构成嵌入式有限元弱形式的**完整积分分解**：
- 公式 (1) 处理体积分项（如刚度矩阵的计算），
- 公式 (2) 处理边界积分项（如 Neumann 力和 Dirichlet 弱强制）。

两者都需要对裁切单元进行专门处理：
- 公式 (1) 中的 [ B_{\mathrm{cut}} \cap \Omega ] 需要**封闭的体积积分域** → 由算法第 5 节通过三角化开放面片（open faces）获得 [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ]，然后利用公式 (5)~(7) 计算矩。
- 公式 (2) 中的 [ F_\beta \cap B ] 仅需将 STL 面片裁切到单元包围盒内 → 直接由裁切算法（Clipper）输出 [ \Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma ]（无需封闭）。

---

## 第 3 节：高效求积规则的构造

### 3.1 矩拟合（Moment Fitting）

#### 背景与动机

对于裁切单元，公式 (1) 中的 [ \int_{B_{\mathrm{cut}}\cap\Omega}(\cdot)\,\mathrm{d}\Omega ] 无法用标准 Gauss 规则直接计算（积分域形状任意复杂）。**矩拟合方法**的思路是：给定积分域 [ \Omega_{\mathrm{c}} = B_{\mathrm{cut}} \cap \Omega ]，找到一组优化的积分点 [ \{(\pmb{x}_i, \omega_i)\}_{i=1}^{n_q} ]，使其对一类基函数 [ \{f_j\} ] 精确积分。

---

### 公式 (3)：矩拟合方程组（线性系统）

$$
\underbrace{\begin{bmatrix}
f_1(\pmb{x}_1) & \cdots & f_1(\pmb{x}_{n_q}) \\
\vdots & \ddots & \vdots \\
f_m(\pmb{x}_1) & \cdots & f_m(\pmb{x}_{n_q})
\end{bmatrix}}_{\mathbf{A} \in \mathbb{R}^{m \times n_q}}
\underbrace{\begin{bmatrix}
\omega_1 \\ \vdots \\ \omega_{n_q}
\end{bmatrix}}_{\pmb{\omega} \in \mathbb{R}^{n_q}}
=
\underbrace{\begin{bmatrix}
\displaystyle\int_{\Omega_{\mathrm{c}}} f_1(\pmb{x})\,\mathrm{d}\Omega \\
\vdots \\
\displaystyle\int_{\Omega_{\mathrm{c}}} f_m(\pmb{x})\,\mathrm{d}\Omega
\end{bmatrix}}_{\pmb{b} \in \mathbb{R}^m}
\qquad (3)
$$

#### 物理含义与各符号解释

| 符号 | 维度 | 含义 |
|------|------|------|
| [ \pmb{x}_i \in \mathbb{R}^3 ] | — | 第 [ i ] 个积分点的空间坐标，[ i = 1, \ldots, n_q ] |
| [ \omega_i \in \mathbb{R} ] | — | 第 [ i ] 个积分点的权重（待求未知量） |
| [ f_j(\pmb{x}) ] | — | 第 [ j ] 个基函数，[ j = 1, \ldots, m ] |
| [ n_q ] | — | 积分点总数 |
| [ m ] | — | 基函数总数（方程数） |
| [ \mathbf{A}_{ji} = f_j(\pmb{x}_i) ] | [ m \times n_q ] | 矩拟合矩阵 |
| [ b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega ] | — | 第 [ j ] 个矩（右端项），即基函数在 [ \Omega_{\mathrm{c}} ] 上的精确积分值 |

#### 推导过程

矩拟合方程的核心要求是：积分规则 [ \sum_{i=1}^{n_q} \omega_i f_j(\pmb{x}_i) ] 对每个基函数 [ f_j ] 精确成立，即：

$$
\sum_{i=1}^{n_q} \omega_i f_j(\pmb{x}_i) = \int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega, \qquad j = 1, \ldots, m
$$

写成矩阵形式即为公式 (3)：[ \mathbf{A}\pmb{\omega} = \pmb{b} ]。

**线性化处理：** 若积分点位置 [ \pmb{x}_i ] 已知（预先指定），则矩阵 [ \mathbf{A} ] 完全确定，公式 (3) 成为关于权重 [ \pmb{\omega} ] 的**线性方程组**。

$$
\mathbf{A}\pmb{\omega} = \pmb{b} \quad \Longrightarrow \quad \text{求解 } \pmb{\omega}
$$

**若点位置未知**（非线性情形），则 [ \mathbf{A} ] 依赖于 [ \pmb{x}_i ]，方程组对 [ (\pmb{x}_i, \omega_i) ] 是**非线性**的，求解非常困难。论文采用预先固定 [ \pmb{x}_i ] 的线性化策略。

**点消除（Point Elimination）：** 为控制积分点数目 [ n_q \leq (p+1)^d ]（[ p ] 为多项式阶次，[ d=3 ] 为空间维度），先取一个过定初始点集，求解超定线性最小二乘问题，然后迭代地移除权重最小的点，直至达到目标精度。

**非负最小二乘（NNLS）：** 求解器保证所有权重 [ \omega_i \geq 0 ]（正权重），避免数值不稳定。

#### 右端项（矩）的重要性

右端向量 [ \pmb{b} ] 中的每个分量 [ b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega ] 称为**矩（moment）**或**常数项（constant term）**。无论采用何种求解策略（NNLS、点消除等），[ \pmb{b} ] 的计算精度直接决定最终积分规则的质量。**这正是本文技术贡献的核心：高精度、高效地计算这些矩**，通过公式 (5)~(7) 的散度定理变换实现。

---

### 公式 (4)：Legendre 多项式张量积基函数空间

$$
F = \bigl\{\, f_j(\pmb{x}) = L_r(x)\,L_s(y)\,L_t(z) \;;\; r, s, t = 0, 1, 2, \ldots, p \,\bigr\}
\qquad (4)
$$

其中 [ L_r ] 为第 [ r ] 阶 Legendre 多项式。

#### 物理含义

公式 (4) 定义了矩拟合方程 (3) 中使用的基函数集合 [ F ]。它采用三个坐标方向上 Legendre 多项式的**张量积（tensor product）**，将一维多项式空间推广至三维。

#### 各 Legendre 多项式的显式表达

设坐标 [ x ] 定义在单元区间 [ [a, b] ] 上，令 [ \xi = \frac{2x - a - b}{b - a} \in [-1, 1] ]，则：

$$
L_0(\xi) = 1
$$
$$
L_1(\xi) = \xi
$$
$$
L_2(\xi) = \frac{1}{2}(3\xi^2 - 1)
$$
$$
L_3(\xi) = \frac{1}{2}(5\xi^3 - 3\xi)
$$
$$
L_4(\xi) = \frac{1}{8}(35\xi^4 - 30\xi^2 + 3)
$$
$$
L_r(\xi) = \frac{1}{2^r}\sum_{k=0}^{\lfloor r/2 \rfloor}(-1)^k\binom{r}{k}\binom{2r-2k}{r}\xi^{r-2k} \quad \text{（一般递推公式）}
$$

Legendre 多项式满足**正交性**：

$$
\int_{-1}^{1} L_r(\xi)\,L_s(\xi)\,\mathrm{d}\xi = \frac{2}{2r+1}\,\delta_{rs}
$$

其中 [ \delta_{rs} ] 为 Kronecker delta。

#### 基函数总数

当多项式阶次为 [ p ] 时，每个方向有 [ (p+1) ] 个 Legendre 多项式（[ r = 0, 1, \ldots, p ]），三维张量积共有：

$$
m = (p+1)^3 \quad \text{个基函数}
$$

#### 为何选用 Legendre 多项式？

1. **正交性** → 矩拟合矩阵 [ \mathbf{A} ] 条件数优于单项式基，数值求解更稳定。
2. **已知抗导数** → 便于通过散度定理（公式 (5)~(6)）解析计算矩 [ b_j ]，不需要额外数值积分。
3. **张量积结构** → 便于高效组装矩阵 [ \mathbf{A} ]，且三维情形可以分解为三个一维问题的乘积。

#### 与公式 (3) 的联系

基函数集 [ F ]（公式 (4)）定义了矩拟合矩阵 [ \mathbf{A} ]（公式 (3)）的每一行：矩阵第 [ j ] 行第 [ i ] 列为 [ A_{ji} = L_r(x_i)L_s(y_i)L_t(z_i) ]，其中 [ (r,s,t) ] 是多指标 [ j ] 对应的三维索引。

---

### 3.2 矩的计算（Computation of the Moments）

本节是论文技术贡献的核心：如何高效、精确地计算公式 (3) 右端项 [ b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega ]。

---

### 公式 (5)：散度定理——体积分化为曲面积分

$$
\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Omega_{\mathrm{c}}} \nabla\cdot\pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
\qquad (5)
$$

其中 [ \pmb{n}(\pmb{x}) ] 为边界 [ \Gamma_{\mathrm{c}} = \partial\Omega_{\mathrm{c}} ] 上的**外法向单位向量**。

#### 物理含义

公式 (5) 将三维**体积分**（计算矩 [ b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega ]）转化为二维**曲面积分**（沿 [ \Omega_{\mathrm{c}} ] 的封闭边界 [ \Gamma_{\mathrm{c}} ] 积分），从而将维度降低一维。这是本文方法论的核心技巧。

#### 完整推导

**第一步：引入矢量场 [ \pmb{g}_j ]**

要应用散度定理，需要找到一个矢量场 [ \pmb{g}_j(\pmb{x}) ]，使其散度等于 [ f_j ]：

$$
\nabla\cdot\pmb{g}_j(\pmb{x}) = f_j(\pmb{x})
$$

**第二步：Gauss 散度定理**

设 [ \Omega_{\mathrm{c}} ] 为有界区域，其边界 [ \Gamma_{\mathrm{c}} = \partial\Omega_{\mathrm{c}} ] 为分段光滑封闭曲面，[ \pmb{n} ] 为外法向单位法向量。由 Gauss 散度定理：

$$
\int_{\Omega_{\mathrm{c}}} \nabla\cdot\pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega = \oint_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
$$

**第三步：组合**

由于 [ \nabla\cdot\pmb{g}_j = f_j ]，将上两步结合：

$$
\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Omega_{\mathrm{c}}} \nabla\cdot\pmb{g}_j(\pmb{x})\,\mathrm{d}\Omega
= \int_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
$$

这就是公式 (5) 中的等式链。

#### 关键前提条件

1. **[ \Gamma_{\mathrm{c}} ] 必须是封闭曲面**：散度定理要求积分域 [ \Omega_{\mathrm{c}} ] 的边界 [ \Gamma_{\mathrm{c}} = \partial\Omega_{\mathrm{c}} ] 为完整封闭的有向曲面。这正是本文第 5 节算法必须将裁切后的开放面片（STL 裁切部分 + 单元边界面片）**三角化并合并**为封闭网格的原因。
2. **[ \pmb{g}_j ] 必须存在**：对于给定的 [ f_j ]，需要构造满足 [ \nabla\cdot\pmb{g}_j = f_j ] 的矢量场，这由公式 (6) 给出。

#### 维度降低的意义

- **不应用散度定理**时：需要对三维区域 [ B_{\mathrm{cut}} \cap \Omega ] 进行空间细分（如八叉树），点数随细分层数指数增长，且实现复杂。
- **应用散度定理后**：只需在二维曲面 [ \Gamma_{\mathrm{c}} ] 上积分，可在三角面片上使用高阶 Gauss 规则，精度可达机器精度，且几何操作简单（三角面片裁切）。

---

### 公式 (6)：抗导数矢量场 [ \pmb{g}_j ] 的构造

$$
\pmb{g}_j(\pmb{x}) = \frac{1}{3}
\begin{bmatrix}
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}x \\[6pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}y \\[6pt]
\displaystyle\int f_j(\pmb{x})\,\mathrm{d}z
\end{bmatrix}
\qquad (6)
$$

#### 物理含义

公式 (6) 给出了公式 (5) 中所需矢量场 [ \pmb{g}_j ] 的**显式构造方法**。它由 [ f_j ] 对三个坐标方向的**不定积分**（反导数）组成，并统一乘以系数 [ \frac{1}{3} ]。

#### 推导：为什么这个构造满足 [ \nabla\cdot\pmb{g}_j = f_j ]？

设 [ \pmb{g}_j = \frac{1}{3}(g_j^{(1)}, g_j^{(2)}, g_j^{(3)})^T ]，其中：

$$
g_j^{(1)} = \int f_j\,\mathrm{d}x, \quad g_j^{(2)} = \int f_j\,\mathrm{d}y, \quad g_j^{(3)} = \int f_j\,\mathrm{d}z
$$

计算散度：

$$
\nabla\cdot\pmb{g}_j
= \frac{1}{3}\left(\frac{\partial g_j^{(1)}}{\partial x} + \frac{\partial g_j^{(2)}}{\partial y} + \frac{\partial g_j^{(3)}}{\partial z}\right)
= \frac{1}{3}\left(f_j + f_j + f_j\right)
= f_j
$$

注意：
- [ \frac{\partial}{\partial x}\int f_j\,\mathrm{d}x = f_j ]（不定积分对相应变量求导还原为原函数），
- [ \frac{\partial}{\partial y}\int f_j\,\mathrm{d}y = f_j ]（同理），
- [ \frac{\partial}{\partial z}\int f_j\,\mathrm{d}z = f_j ]（同理）。

因此 [ \nabla\cdot\pmb{g}_j = f_j ]，公式 (6) 给出的构造满足公式 (5) 的要求。[ \frac{1}{3} ] 系数确保了对称性——三个方向的贡献各占 [ \frac{1}{3} ]。

#### 对张量积 Legendre 多项式的显式展开

公式 (4) 中的基函数 [ f_j(\pmb{x}) = L_r(x)L_s(y)L_t(z) ] 是三个一维函数的乘积。代入公式 (6)：

$$
g_j^{(1)} = \int L_r(x)L_s(y)L_t(z)\,\mathrm{d}x = \left(\int L_r(x)\,\mathrm{d}x\right) L_s(y) L_t(z)
$$

$$
g_j^{(2)} = \int L_r(x)L_s(y)L_t(z)\,\mathrm{d}y = L_r(x)\left(\int L_s(y)\,\mathrm{d}y\right) L_t(z)
$$

$$
g_j^{(3)} = \int L_r(x)L_s(y)L_t(z)\,\mathrm{d}z = L_r(x) L_s(y)\left(\int L_t(z)\,\mathrm{d}z\right)
$$

因此，[ \pmb{g}_j ] 的计算只需要 Legendre 多项式的**一维不定积分**（解析已知），完全无需数值积分。这保证了矩计算的高精度（可达机器精度）。

以 [ L_0 \sim L_3 ] 的不定积分为例（定义在区间 [ [a,b] ]，令 [ \xi = \frac{2x-a-b}{b-a} ]）：

$$
\int L_0(x)\,\mathrm{d}x = x
$$

$$
\int L_1(x)\,\mathrm{d}x = -\frac{(a+b-2x)^2}{4(a-b)}
$$

$$
\int L_2(x)\,\mathrm{d}x = -\frac{x}{2} - \frac{(a+b-2x)^3}{4(a-b)^2}
$$

$$
\int L_3(x)\,\mathrm{d}x = \frac{3(a+b-2x)^2}{8(a-b)} - \frac{5(a+b-2x)^4}{16(a-b)^3}
$$

#### 与公式 (5) 的联系

公式 (6) 给出的 [ \pmb{g}_j ] 代入公式 (5)，得到体积分 [ \int_{\Omega_c} f_j\,\mathrm{d}\Omega ] 的计算公式：

$$
\int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega = \int_{\Gamma_{\mathrm{c}}} \frac{1}{3}
\left[
\left(\int L_r\,\mathrm{d}x\right) L_s L_t\,n_x
+ L_r\left(\int L_s\,\mathrm{d}y\right) L_t\,n_y
+ L_r L_s\left(\int L_t\,\mathrm{d}z\right) n_z
\right]
\mathrm{d}\Gamma
$$

这里 [ (n_x, n_y, n_z) ] 是 [ \Gamma_{\mathrm{c}} ] 上的外法向量分量。

---

### 3.3 对 B-Rep 的应用（Application to B-Reps）

---

### 公式 (7)：B-Rep（STL 网格）上的曲面积分离散化

$$
\int_{\Omega_{\mathrm{c}}} f_j(\pmb{x})\,\mathrm{d}\Omega
= \sum_{f \in \Gamma_{\mathrm{c}}} \int_{f} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
\qquad (7)
$$

其中 [ f ] 表示一个三角面片（triangular face）。

#### 物理含义

公式 (7) 是公式 (5) 的**离散化实现**：将连续的封闭曲面 [ \Gamma_{\mathrm{c}} ] 表示为有限个三角面片 [ \{f\}_{f \in \Gamma_{\mathrm{c}}} ] 的集合，从而将曲面积分化为有限个三角形上积分之和。这使得计算机可以直接处理 STL 格式的几何数据（STL 即由有向三角网格表示的几何体边界）。

#### 推导过程

**第一步：曲面的三角网格离散**

假设 [ \Gamma_{\mathrm{c}} ] 是一个封闭的有向三角网格，由 [ N_f ] 个三角面片 [ f_1, f_2, \ldots, f_{N_f} ] 组成，满足：

$$
\Gamma_{\mathrm{c}} = \bigcup_{k=1}^{N_f} f_k, \quad f_k \cap f_l = \partial f_k \cap \partial f_l \quad (k \neq l)
$$

（面片内部互不重叠，仅在边上相交。）

**第二步：积分的可加性**

由积分的可加性（面片面积和不重叠），曲面积分可以分解为：

$$
\int_{\Gamma_{\mathrm{c}}} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
= \sum_{f \in \Gamma_{\mathrm{c}}} \int_{f} \pmb{g}_j(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
$$

**第三步：组合**

将公式 (5) 与上式结合，直接得到公式 (7)。

**第四步：单个三角面片上的积分**

对每个三角面片 [ f ]，其上的法向量 [ \pmb{n}(x) ] 在整个面片上为**常数**（三角形是平面片，法向量处处相同）。设 [ f ] 的三个顶点为 [ \pmb{P}_1, \pmb{P}_2, \pmb{P}_3 ]，则：

$$
\pmb{n}_f = \frac{(\pmb{P}_2 - \pmb{P}_1) \times (\pmb{P}_3 - \pmb{P}_1)}{\|(\pmb{P}_2 - \pmb{P}_1) \times (\pmb{P}_3 - \pmb{P}_1)\|} \quad \text{（外法向单位向量）}
$$

面积 [ A_f = \frac{1}{2}\|(\pmb{P}_2 - \pmb{P}_1) \times (\pmb{P}_3 - \pmb{P}_1)\| ]

每个三角面片上的积分用 **2D Gauss 求积规则**（如 Gauss 三角形积分）数值计算：

$$
\int_{f} \pmb{g}_j(\pmb{x})\cdot\pmb{n}_f\,\mathrm{d}\Gamma \approx \sum_{q} w_q \,\pmb{g}_j(\pmb{x}_q)\cdot\pmb{n}_f
$$

其中 [ \{\pmb{x}_q, w_q\} ] 为三角面片 [ f ] 上的 Gauss 积分点和权重。使用**高阶** Gauss 规则可使单个面片上的积分达到机器精度（因为 [ \pmb{g}_j ] 是光滑的多项式），这是本文方法精度高的关键原因。

#### 封闭曲面条件的必要性

公式 (7) 的有效性依赖公式 (5) 中散度定理的正确应用，而散度定理**要求 [ \Gamma_{\mathrm{c}} ] 是封闭的有向曲面**（每条边恰好属于两个面片，且面片方向一致）。

对于裁切单元 [ B_{\mathrm{cut}} ]：

- [ B_{\mathrm{cut}} \cap \Gamma ]（来自 STL 的三角面片裁切到单元包围盒内的部分）：这些面片来自原始 STL，方向已知，但裁切后形成**开放网格**（有边界边）。
- 单元内部面片（单元 AABB 的面，在 [ \Omega ] 内部的部分）：需要**三角化开放面片（open faces triangulation）**来封闭网格。

第 5 节的核心算法正是完成这一几何任务：将 [ B_{\mathrm{cut}} \cap \Gamma ] 与三角化的开放面片合并，得到**封闭的** [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ]，从而使公式 (7) 可以正确应用。

#### 与质量准则的关系

由于公式 (7) 基于散度定理（体积守恒），文中给出了一个**质量验证准则**：对常数函数 [ f_j = 1 ]，有：

$$
\int_{\Omega_{\mathrm{c}}} 1\,\mathrm{d}\Omega = \text{Vol}(\Omega_{\mathrm{c}}) = \int_{\Gamma_{\mathrm{c}}} \pmb{g}_1(\pmb{x})\cdot\pmb{n}(\pmb{x})\,\mathrm{d}\Gamma
$$

其中对 [ f_j = 1 ]，由公式 (6)：[ \pmb{g}_1 = \frac{1}{3}(x, y, z)^T ]，从而 [ \nabla\cdot\pmb{g}_1 = \frac{1}{3}(1+1+1) = 1 ]，散度定理给出：

$$
\text{Vol}(\Omega_{\mathrm{c}}) = \int_{\Gamma_{\mathrm{c}}} \frac{1}{3}(x\,n_x + y\,n_y + z\,n_z)\,\mathrm{d}\Gamma
$$

这是一个可以直接验证的精确结果（用 Gauss 散度定理精确计算体积）。用计算值与真实值之差评估封闭网格 [ \Gamma_{\mathrm{c}} ] 的质量，是本文唯一的网格质量准则（[ \varepsilon = |\text{Vol}_{\mathrm{computed}} - \text{Vol}_{\mathrm{exact}}|/\text{Vol}_{\mathrm{exact}} < \delta ]）。

---

## 第 3 节总结：公式 (3)~(7) 的完整推导链

以下展示从矩拟合方程到最终三角面片积分的完整推导链：

**目标：** 计算矩拟合方程 (3) 的右端项（矩）[ b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega ]。

**步骤 1（公式 (4)）：** 选择基函数为 Legendre 多项式张量积：
$$f_j(\pmb{x}) = L_r(x)L_s(y)L_t(z)$$

**步骤 2（公式 (6)）：** 构造满足 [ \nabla\cdot\pmb{g}_j = f_j ] 的矢量场：
$$\pmb{g}_j(\pmb{x}) = \frac{1}{3}\left[\left(\int L_r\mathrm{d}x\right)L_sL_t,\; L_r\left(\int L_s\mathrm{d}y\right)L_t,\; L_rL_s\left(\int L_t\mathrm{d}z\right)\right]^T$$

**步骤 3（公式 (5)）：** 应用 Gauss 散度定理将体积分化为曲面积分：
$$b_j = \int_{\Omega_{\mathrm{c}}} f_j\,\mathrm{d}\Omega = \int_{\Gamma_{\mathrm{c}}} \pmb{g}_j\cdot\pmb{n}\,\mathrm{d}\Gamma$$

**步骤 4（公式 (7)）：** 对 STL 三角网格离散 [ \Gamma_{\mathrm{c}} ]，将曲面积分化为各面片积分之和：
$$b_j = \sum_{f \in \Gamma_{\mathrm{c}}} \int_f \pmb{g}_j\cdot\pmb{n}_f\,\mathrm{d}\Gamma$$

**步骤 5：** 对每个三角面片 [ f ] 用高阶 Gauss 规则数值计算：
$$\int_f \pmb{g}_j\cdot\pmb{n}_f\,\mathrm{d}\Gamma \approx \sum_q w_q \,\pmb{g}_j(\pmb{x}_q)\cdot\pmb{n}_f$$

**步骤 6（公式 (3)）：** 将计算得到的 [ \pmb{b} ] 代入矩拟合方程，求解权重 [ \pmb{\omega} ]（通过 NNLS + 点消除）：
$$\mathbf{A}\pmb{\omega} = \pmb{b}$$

---

## 附录：内联公式与关键不等式的说明

### [ n_q \leq (p+1)^3 ]（最优积分点数上界）

论文中提到，对于三维裁切元素，矩拟合 + 点消除 + NNLS 的组合可以构造满足以下条件的积分规则：

$$
n_q \leq (p+1)^3
$$

其中 [ p ] 为多项式阶次，[ d = 3 ] 为维度。

**解释：** 精确积分 [ p ] 阶多项式至少需要 [ \lceil (p+1)/2 \rceil ] 个 Gauss 点（一维），三维情形下标准 Gauss 规则需要 [ \lceil (p+1)/2 \rceil^3 ] 个点，而矩拟合方法可以用**最多** [ (p+1)^3 ] 个点达到相同精度。这一结果对应矩拟合矩阵 [ \mathbf{A} ] 的基函数个数（公式 (4) 中 [ m = (p+1)^3 ]），是点消除算法的理论上界保证。

对于一般 [ d ] 维情形：[ n_q \leq (p+1)^d ]，文中这是针对 [ d=3 ] 的三维结果。

### 活跃集定义 [ \mathbf{P} = \mathbf{P}_{\mathrm{bg}} \setminus \mathbf{P}_{\mathrm{out}} ]

**解释：** 从背景网格 [ \mathbf{P}_{\mathrm{bg}} ] 中去除所有外部单元 [ \mathbf{P}_{\mathrm{out}} ]，保留内部单元 [ \mathbf{P}_{\mathrm{in}} ] 和裁切单元 [ \mathbf{P}_{\mathrm{cut}} ]，构成参与计算的活跃集。这是公式 (1) 求和 [ \sum_{B \in \mathcal{P}} ] 的索引集合。

### [ \Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma ] 与 [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ] 的区别

| 符号 | 含义 | 用途 |
|------|------|------|
| [ \Gamma_{\mathrm{c,c}} = B_{\mathrm{cut}} \cap \Gamma ] | STL 曲面与单元包围盒的交（开放网格） | 用于公式 (2) 的边界积分（施加边界条件） |
| [ \Gamma_{\mathrm{c}} = \partial(B_{\mathrm{cut}} \cap \Omega) ] | 裁切域的完整封闭边界（封闭网格） | 用于公式 (7) 的矩计算（散度定理要求封闭） |

[ \Gamma_{\mathrm{c}} ] 由 [ \Gamma_{\mathrm{c,c}} ]（来自 STL 裁切的三角面片）加上**三角化的开放矩形面片**（单元 AABB 与 [ \Omega ] 相交的部分面片）构成：

$$\Gamma_{\mathrm{c}} = \Gamma_{\mathrm{c,c}} \cup \Gamma_{\mathrm{c,plane}}$$

其中 [ \Gamma_{\mathrm{c,plane}} ] 是通过第 5 节算法的 TrimmedDomainOnPlane 模块三角化生成的单元平面面片。

---

## 全文公式相互关系总图

```
        [公式(1)：体积分分解]
               |
        面临裁切单元积分困难
               |
        [公式(3)：矩拟合方程]  ←── [公式(4)：Legendre 基函数]
               |
        需计算右端项（矩）b_j
               |
        [公式(6)：构造 g_j]  ←── ∇·g_j = f_j（散度等于基函数）
               |
        [公式(5)：散度定理]  ──→  体积分 → 曲面积分
               |
        需要封闭曲面 Γ_c（来自第5节算法）
               |
        [公式(7)：B-Rep 离散]  ──→  曲面积分 → Σ 三角面片积分
               |
        高阶 Gauss 求积（单个三角面片，近机器精度）
               |
        ← 将计算的 b_j 代回公式(3)，NNLS 求解权重 ω_i →
               |
        [公式(2)：边界积分分解]（并行处理，直接用 Γ_{c,c}）
```

---

*文档生成日期：2026-04-04*
