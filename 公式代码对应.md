# 公式代码对应

说明：以下按文献结构分段列出每个带编号公式；如仓库未实现对应计算，会在“代码链接”处标注“未实现”，并给出仓库范围说明代码注释以说明缺失。

## 2. Embedded boundary method

### 2.1 Definition

#### 公式 (1)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.cpp#L188-L205
```cpp
    // QuadratureInfo.
    double represented_volume = 0.0;
    SizeType tot_num_points_full = 0;
    SizeType tot_num_points_trimmed = 0;
    const auto el_it_ptr_begin = mBackgroundGrid.ElementsBegin();
    #pragma omp parallel for reduction(+ : represented_volume, tot_num_points_full, tot_num_points_trimmed) schedule(guided)
    for( int i = 0; i < static_cast<int>(num_active_elements); ++i ){
        const auto el_ptr = (el_it_ptr_begin + i);
        const double det_j = el_ptr->DetJ();
        const auto& r_points = el_ptr->GetIntegrationPoints();
        represented_volume += std::accumulate(r_points.begin(), r_points.end(), 0.0,
            [det_j](double sum, const auto& point) {
                return sum + point.Weight() * det_j; }
        );
        auto& target_counter = el_ptr->IsTrimmed() ? tot_num_points_trimmed : tot_num_points_full;
        target_counter += r_points.size();
    }
```
$$
\int_{\Omega} (\cdot) \, d\Omega = \sum_{\mathcal{B} \in \mathcal{P}} \int_{\mathcal{B} \cap \Omega} (\cdot) \, d\Omega, \tag{1}
$$

#### 公式 (2)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/kratos_interface/weak_bcs.py#L285-L328
```python
    def apply(self, model_part: KM.ModelPart) -> None:
        """Applies the PressureLoad.

        Args:
            model_part (KM.ModelPart): Kratos model part.
        """
        id_counter = model_part.NumberOfConditions() + 1
        properties = model_part.GetProperties()[1]
        nurbs_volume = model_part.GetGeometry("NurbsVolume")

        for triangle_id in range(self.bcs_triangles.NumOfTriangles()):
            #Get points in physical space.
            points = self.bcs_triangles.GetIntegrationPointsGlobal(triangle_id, 1)

            #Create kratos condition on each point.
            for point in points:
                global_point = (point.X(), point.Y(), point.Z())

                # Map points to local space of B-Spline box
                local_point = point_from_global_to_param_space(global_point, self.bounds_xyz, self.bounds_uvw)

                # Create quadrature points
                integration_points = [[local_point[0], local_point[1], local_point[2], point.Weight()]]
                quadrature_point_geometries_boundary = KM.GeometriesVector()
                nurbs_volume.CreateQuadraturePointGeometries(quadrature_point_geometries_boundary, 2, integration_points)

                weight = point.Weight() # Weight contains all mapping terms.
                if weight < 1e-14:
                    continue  # Skip insignificant weights

                condition = model_part.CreateNewCondition(
                    "LoadCondition",
                    id_counter,
                    quadrature_point_geometries_boundary[0],
                    properties
                )

                normal = point.Normal()
                force = -1.0 * weight * self.modulus * np.array(normal)

                condition.SetValue(StructuralMechanicsApplication.POINT_LOAD_X, force[0]) # type: ignore
                condition.SetValue(StructuralMechanicsApplication.POINT_LOAD_Y, force[1]) # type: ignore
                condition.SetValue(StructuralMechanicsApplication.POINT_LOAD_Z, force[2]) # type: ignore
                id_counter += 1
```
$$
\int_{\Gamma_{\beta}} (\cdot) \, d\Gamma = \sum_{\mathcal{B} \in \mathcal{P}} \sum_{F_{\beta} \in \Gamma_{\beta}} \int_{F_{\beta} \cap \mathcal{B}} (\cdot) \, d\Gamma, \quad \beta \in \{D, N\}, \tag{2}
$$

## 3. Node-based shape optimization using implicit Vertex-Morphing

#### 公式 (3)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\begin{array}{rl}
\min & f(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \\
\mathrm{subject~to} & \boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \mathbf{0}, \quad \mathrm{on}~\Gamma, \\
& \boldsymbol{r}(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = \mathbf{0}, \quad \mathrm{in}~\Omega, \\
& h_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) = 0, \quad j = 1\dots m_h, \\
& g_j(\boldsymbol{x}_\Gamma, \boldsymbol{u}) \leq 0, \quad j = 1\dots m_g,
\end{array} \tag{3}
$$

### 3.1. Implicit Vertex-Morphing

#### 公式 (4)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\begin{array}{rl}
& p^j = -(r_\Gamma^H)^2 \nabla_\Gamma \cdot \nabla_\Gamma x_\Gamma^i + x_\Gamma^i - s_\Gamma^i = 0, \quad i \in \{1,2,3\}, \quad \text{on}~\Gamma, \\
& x_\Gamma^i = s_\Gamma^i, \quad \text{along}~\Gamma_D^H \text{ (fixed boundaries)},
\end{array} \tag{4}
$$

#### 公式 (5)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\nabla_\Gamma u = \nabla u - (\boldsymbol{n} \cdot \nabla u) \boldsymbol{n}, \tag{5}
$$

#### 公式 (6)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\boldsymbol{p}(\boldsymbol{x}_\Gamma, \boldsymbol{s}_\Gamma) = \boldsymbol{K}_\Gamma^H \boldsymbol{x}_\Gamma + \boldsymbol{M}_\Gamma^H \boldsymbol{x}_\Gamma - \boldsymbol{M}_\Gamma^H \boldsymbol{s}_\Gamma = \mathbf{0}, \tag{6}
$$

#### 公式 (7)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\boldsymbol{M}_\Gamma^H = \sum \int_{\Gamma^e} \boldsymbol{N}_\Gamma^T \boldsymbol{N}_\Gamma \, d\Gamma, \tag{7}
$$

#### 公式 (8)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\boldsymbol{K}_\Gamma^H = (r_\Gamma^H)^2 \sum \int_{\Gamma^e} (\nabla_\Gamma \boldsymbol{N}_\Gamma)^T \nabla_\Gamma \boldsymbol{N}_\Gamma \, d\Gamma, \tag{8}
$$

#### 公式 (9)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\boldsymbol{x}_\Gamma = \boldsymbol{A}_\Gamma^H \boldsymbol{s}_\Gamma; \quad \boldsymbol{A}_\Gamma^H = (\boldsymbol{K}_\Gamma^H + \boldsymbol{M}_\Gamma^H)^{-1} \boldsymbol{M}_\Gamma^H. \tag{9}
$$

### 3.2. Update rule

#### 公式 (10)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
{}^{k+1}\boldsymbol{s}_\Gamma = {}^{k}\boldsymbol{s}_\Gamma - \alpha \underbrace{[\boldsymbol{I} - \boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}\boldsymbol{P}^T]}_{\text{projection}} \underbrace{\frac{d f}{d \boldsymbol{s}_\Gamma}\bigg|_{\mathcal{C}}}_{\text{projection}} - \underbrace{[\boldsymbol{P}(\boldsymbol{P}^T\boldsymbol{P})^{-1}]}_{\text{projection}} \boldsymbol{g}^a, \tag{10}
$$

#### 公式 (11)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \boldsymbol{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c. \tag{11}
$$

## 4. Sensitivity analysis

#### 公式 (12)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/kratos_interface/model_part_utilities.py#L116-L151
```python
    def add_elements_to_model_part(
            KratosNurbsVolumeModelPart: KM.ModelPart,
            Elements: QuESo.ElementVector # type: ignore (TODO: add .pyi)
        ) -> None:
        """
        Adds the QuESo elements to the Kratos NurbsVolume ModelPart.

        Args:
            KratosNurbsVolumeModelPart (KM.ModelPart): The Kratos model part to which the elements will be added.
            Elements (QuESo.ElementVector): List of QuESo elements to add to the model part.
        """
        nurbs_volume = KratosNurbsVolumeModelPart.GetGeometry("NurbsVolume")
        volume_properties = KratosNurbsVolumeModelPart.GetProperties()[1]

        el_count = 0
        for element in Elements:
            # Collect valid integration points
            integration_points = [
                [point.X(), point.Y(), point.Z(), point.Weight()]
                for point in element.GetIntegrationPoints()
                if(point.Weight() > 0)
            ]

            # Create elements
            if integration_points:
                el_count += 1
                # Create quadrature_point_geometries
                quadrature_point_geometries = KM.GeometriesVector()
                nurbs_volume.CreateQuadraturePointGeometries(quadrature_point_geometries, 2, integration_points)
                KratosNurbsVolumeModelPart.CreateNewElement(
                    'SmallDisplacementElement3D8N',
                    el_count,
                    quadrature_point_geometries[0],
                    volume_properties
                )
```
$$
\mathbf{K} \mathbf{u} = \mathbf{F}, \tag{12}
$$

### 4.1. Semi-analytical sensitivity analysis on the background mesh

#### 公式 (13)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \frac{d\mathbf{u}}{d\boldsymbol{x}_p}. \tag{13}
$$

#### 公式 (14)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{d\mathbf{u}}{d\boldsymbol{x}_p} = \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right). \tag{14}
$$

#### 公式 (15)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{x}_p}\bigg|_d = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \left( \frac{d\mathbf{F}}{d\boldsymbol{x}_p} - \frac{d\mathbf{K}}{d\boldsymbol{x}_p} \mathbf{u} \right) = \frac{\partial J}{\partial \boldsymbol{x}_p} + \left(\frac{\partial J}{\partial \mathbf{u}}\right)^T \mathbf{K}^{-1} \mathbf{F}^*. \tag{15}
$$

#### 公式 (16)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\mathbf{F}^* = \frac{\mathbf{F}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{F}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} - \frac{\mathbf{K}(\boldsymbol{x}_p + \Delta \boldsymbol{x}_p) - \mathbf{K}(\boldsymbol{x}_p)}{\Delta \boldsymbol{x}_p} \mathbf{u}. \tag{16}
$$

#### 公式 (17)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dU}{d\boldsymbol{x}_p}\bigg|_d = \mathbf{u}^T \mathbf{F}^*. \tag{17}
$$

### 4.2.1. One-dimensional example

#### 公式 (18)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\overline{V} = A (\overline{x}_\Gamma - \overline{x}_{p1}), \tag{18}
$$

#### 公式 (19)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = A. \tag{19}
$$

#### 公式 (20)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{1}{A} \frac{d\overline{V}}{d\overline{x}_\Gamma}\bigg|_d = 1. \tag{20}
$$

#### 公式 (21)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (x_\Gamma - x_{p,2}). \tag{21}
$$

#### 公式 (22)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
V^{1} = A (x_{p,2} - x_{p,1}), \quad \text{and} \quad V^{2} = A (N_1(\xi_\Gamma) x_{p,2} + N_2(\xi_\Gamma) x_{p,3} - x_{p,2}). \tag{22}
$$

#### 公式 (23)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dV}{dx_{p,2}}\bigg|_d = \frac{dV^1}{dx_{p,2}}\bigg|_d + \frac{dV^2}{dx_{p,2}}\bigg|_d = A N_1(\xi_\Gamma). \tag{23}
$$

#### 公式 (24)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dV}{dx_{p,3}}\bigg|_d = \frac{dV^1}{dx_{p,3}}\bigg|_d + \frac{dV^2}{dx_{p,3}}\bigg|_d = A N_2(\xi_\Gamma). \tag{24}
$$

#### 公式 (25)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\mathcal{N}_{x_{p,2}} = \int N_1 \, d\Gamma = A N_1(\xi_\Gamma). \tag{25}
$$

#### 公式 (26)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\mathcal{N}_{x_{p,3}} = \int N_2 \, d\Gamma = A N_2(\xi_\Gamma). \tag{26}
$$

#### 公式 (27)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d = 1. \tag{27}
$$

#### 公式 (28)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1. \tag{28}
$$

#### 公式 (29)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dV}{dx_\Gamma}\bigg|_c = N_1(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,2}}} \frac{dV}{dx_{p,2}}\bigg|_d + N_2(\xi_\Gamma) \frac{1}{\mathcal{N}_{x_{p,3}}} \frac{dV}{dx_{p,3}}\bigg|_d = 1. \tag{29}
$$

### 4.2.2. Two-dimensional example

#### 公式 (30)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = t (\Delta \overline{A}^1 + \Delta \overline{A}^2) = t a, \tag{30}
$$

#### 公式 (31)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{1}{t a} \frac{d\overline{V}}{d\overline{x}_\Gamma^*}\bigg|_d = 1. \tag{31}
$$

#### 公式 (32)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dV}{dx_p^*}\bigg|_d = t (\Delta A^1 + \Delta A^2) = t \left( N_{\boldsymbol{x}_p^*}^1(\hat{\xi}, \hat{\eta}) \frac{a}{2} + N_{\boldsymbol{x}_p^*}^2(\hat{\xi}, \hat{\eta}) \frac{a}{2} \right). \tag{32}
$$

#### 公式 (33)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\mathcal{N}_{\boldsymbol{x}_p^*} = \int_{\Gamma} N_{\boldsymbol{x}_p^*}^1 \, d\Gamma + \int_{\Gamma} N_{\boldsymbol{x}_p^*}^2 \, d\Gamma. \tag{33}
$$

#### 公式 (34)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{1}{\mathcal{N}_{\boldsymbol{x}_p^*}} \frac{dV}{dx_p^*}\bigg|_d = 1. \tag{34}
$$

#### 公式 (35)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dV}{dx_\Gamma^*}\bigg|_c = \sum_{i=1}^{n=4} N_{\boldsymbol{x}_{pi}}(\hat{\xi}, \hat{\eta}) \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dV}{d\boldsymbol{x}_{pi}}\bigg|_d \right) = 1. \tag{35}
$$

### 4.2.3. Generalization and application to three-dimensional problems

#### 公式 (36)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c = \sum_{i \in \mathcal{I}} N_{\boldsymbol{x}_{pi}} \left( \frac{1}{\mathcal{N}_{\boldsymbol{x}_{pi}}} \frac{dJ}{d\boldsymbol{x}_{pi}}\bigg|_d \right), \tag{36}
$$

#### 公式 (37)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\mathcal{N}_{\boldsymbol{x}_{pi}} = \sum_{j \in \mathcal{J}} \int_{\Gamma} N_{\boldsymbol{x}_{pi}} \, d\Gamma, \tag{37}
$$

#### 公式 (38)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
e_{V} = \frac{\| \mathbf{V}_{\text{grad}} - \mathbf{N} \|_{L2}}{\| \mathbf{N} \|_{L2}}, \tag{38}
$$

## 5. Geometrical constraints

### 5.1. Active constraint detection

#### 公式 (39)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
d < 2 \Delta x_{\max} + t_{\min} \quad \text{(for wall thickness constraints)}, \qquad
d < 2 \Delta x_{\max} \quad \text{(for design space constraints)}, \tag{39}
$$

### 5.2. Constraint enforcement

#### 公式 (40)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c^{\text{proj}} = \mathbf{D}_\Gamma \frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c, \quad \text{with} \quad \mathbf{D}_\Gamma = [\mathbf{I} - \mathbf{N}(\mathbf{N}^T \mathbf{N})^{-1} \mathbf{N}^T]. \tag{40}
$$

#### 公式 (41)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\boldsymbol{x}_\Gamma = \mathbf{A}_\Gamma^H \mathbf{D}_\Gamma \boldsymbol{s}_\Gamma, \tag{41}
$$

#### 公式 (42)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\frac{dJ}{d\boldsymbol{s}_\Gamma}\bigg|_c = \mathbf{D}_\Gamma \mathbf{A}_\Gamma^H \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c. \tag{42}
$$

## 7. Numerical experiments

### 7.1. Volume maximization of a cube

#### 公式 (43)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
{}^{k+1}\boldsymbol{x}_\Gamma = {}^{k}\boldsymbol{x}_\Gamma + \alpha \frac{dJ}{d\boldsymbol{x}_\Gamma}\bigg|_c, \tag{43}
$$

#### 公式 (44)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
e_R = \frac{\max\{R\} - \min\{R\}}{\mathrm{avg}\{R\}}, \quad \text{with} \quad \mathrm{avg}\{R\} = \frac{\sum_{R \in \mathcal{R}} R}{|\mathcal{R}|}, \tag{44}
$$

### 7.2. Strain energy minimization of a hook

#### 公式 (45)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\begin{array}{rl}
\min & U, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& m - m_{\text{init}} = 0,
\end{array} \tag{45}
$$

### 7.3. Mass minimization of a jet engine bracket

#### 公式 (46)
代码链接：https://github.com/xingzhiyuan1229/QuESo/blob/main/queso/embedded_model.h#L34-L45（未实现；仓库仅包含积分点生成与几何裁剪）
```cpp
/**
 * @class  EmbeddedModel
 * @author Manuel Messmer
 * @brief  Main class of QuESo.
 *         Provides interface to create integration points for embedded volumes and embedded conditions (boundaries)
 *         based on the settings provided in mSettings. EmbeddedModel aggregates a BackgroundGrid, which stores the
 *         created active elements and conditions. Each element stores its integrations points. Each condition is
 *         split into ConditionSegment's (that conform to the boundaries of the elements in the background grid).
 *         The corresponding boundary integration points are stored on these ConditionSegments.
 *         EmbeddedModel also stores some information regearding the created model in mModelInfo.
 */
```
$$
\begin{array}{rl}
\min & m, \\
\text{subject to} & \mathbf{p} = \mathbf{0}, \quad \text{on } \Gamma, \\
& \mathbf{r} = \mathbf{0}, \quad \text{in } \Omega, \\
& U_j - U_j^{\text{init}} = 0, \quad j \in \{1,2,3,4\},
\end{array} \tag{46}
$$
